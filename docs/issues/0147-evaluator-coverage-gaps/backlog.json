{
  "version": 1,
  "issue": "docs/issues/0147-evaluator-coverage-gaps",
  "workflow": "agile-loop",
  "codemap": "# Codemap - issue 0147, round 6: the two pinned evaluator gaps\n\nEvery file this issue had to change, one line each, with the increment it belongs to. Round 6 opened on two acceptance criteria, one per pinned scenario. Both increments have landed, so every section below is measured fact rather than a searched guess.\n\nThe issue fences the scope hard: only `src/evaluator/` and its unit tests may change. Nothing under `docs/testing/` may be touched at all - and nothing was.\n\n## Increment `parent-any-offer-anchor` (criterion 2, `docs/testing/at-least-parent-any-reveal`) - DONE\n\n- `src/evaluator/offer.js` - the offer descent; a group `entryLink`'s own locally declared children were being dropped, so the four-level-deep `Wolf Lord` link was never reached.\n- `src/evaluator/countIndex.js` - the count index; the share a frame node contributes under the `null`/`any` target now lives in its own slot instead of the shared bucket.\n- `src/evaluator/query.js` - the query primitive; it is the only place that knows whether the frame is the querying node, and it is what decides which of those two count slots a query reads.\n- `src/evaluator/effectiveState.js` - visibility of a node and of the gates above it became two independently resolved tracks.\n- `src/evaluator/modifiers.js` - a visibility gate's `hidden` modifiers now write the gate's own slot rather than the member node's.\n- `src/evaluator/offer.test.js`, `src/evaluator/offer.hiddenGate.test.js`, `src/evaluator/effectiveState.baseHiddenInheritance.test.js`, `src/evaluator/countIndex.frameOwnAnyTarget.test.js` (new) - the unit tests that pin the four rules above.\n- `src/evaluator/CLAUDE.md` (new) - a suite doc the increment added; no criterion asked for it, and the review let it stand.\n- Proved untouched, dropped from this increment: `src/evaluator/evalTree.js`, `src/evaluator/fixpoint.js`, `src/evaluator/report.js`. The earlier guess that the anchor's node shape, the anchor post-pass and the report projection would need widening was wrong - the whole reveal came from the descent, the count and the visibility tracks.\n\n## Increment `unlimited-slot-count` (criterion 1, `docs/testing/greater-than-force-unlimited-gate`) - DONE, this is what it actually touched\n\n- `src/evaluator/report.js` - the report projection, and the only production file the increment changed. The `current` derivation in `toCapability` gained one further fallback step behind the category-anchor occupancy map: a real slot's own instance count, which is non-null only where the evaluation tree attached an instance. Its German doc block and the inline comment above the derivation were extended with the new rule.\n- `src/evaluator/report.test.js` - six new unit cases pinning the new fallback, the unchanged limit-supplied count, the own-count-not-frame-sum reading, and that a synthetic anchor without an instance still reports 0.\n- Proved untouched, dropped from this increment: `src/evaluator/constraints.js` (an unlimited bound still yields no limit result, and the category-anchor occupancy map is unchanged - the codemap's guess that either would need widening was wrong), `src/evaluator/evaluator.js` (no new wiring: the number is already on the node), `src/evaluator/countIndex.js` and `src/evaluator/query.js` (the 'possibly' guess measured out - no query is involved).\n\n## Read-only, never edited\n\n- `src/evaluator/e2e.testcatalog.test.js` - the manifest-driven runner that turns the scenarios into cases; it is the check, never an edit.\n- `docs/testing/greater-than-force-unlimited-gate/` and `docs/testing/at-least-parent-any-reveal/` (`scenario.json`, `rosters/*.ros`, `README.md`) - the specification, out of scope by the issue.\n- `src/evaluator/__fixtures__/whfb6-definitive/` - the frozen catalog data both scenarios are derived from; `src/evaluator/__fixtures__/rosParser.js` maps a roster's `number` attribute to the instance count the new fallback reports.\n- `docs/testing/campaign-state.json` - the campaign driver's state; it pins these two gaps and is outside the issue's fence, so the human moves them from `pinnedGaps` to `closed` when the campaign branch merges.\n- `src/evaluator/evalTree.js`, `src/evaluator/constraints.js`, `src/evaluator/evaluator.js`, `src/evaluator/model.js`, `src/evaluator/budget.js` - read for both increments and measured as unaffected.\n- `src/components/RosterCategorySection.jsx`, `src/components/RosterSidebar.jsx`, `src/components/OptionGroup.jsx` - the only readers of a capability's reported count outside the evaluator, and all three read it at synthetic anchors, so the app surface is unaffected. Out of the issue's fence in any case.\n\n## Known drift, outside this issue's fence\n\n- `docs/evaluator-architecture.md` (SS3.2 offer layer, SS4.8 `current` derivation) is stale and both increments made it staler: the offer descent, the `any`-target count rule, the two visibility tracks and the count derivation are all now different from what it describes. The issue bars changing anything outside `src/evaluator/`, so it stays for the human to schedule.\n- Named by the review of `unlimited-slot-count`, for the pull request rather than for this issue: where a limit result supplies the count it is that limit's frame-wide actual, whereas the new fallback reports the slot's own piece count, so the two readings can differ in a frame that holds one definition in two separate selections. No criterion of this issue decides between them; the new counter-check case pins the chosen reading deliberately.",
  "increments": [
    {
      "id": "parent-any-offer-anchor",
      "title": "An unselected deep option appears as an offer anchor, so its reveal is observable",
      "goal": "Make `docs/testing/at-least-parent-any-reveal` green: the report must carry an offer anchor for the `Wolf Lord` option in rosters where nothing in its group chain is selected, so the `set hidden=\"false\"` gated on `atLeast 1` in the parent frame can be seen flipping at all. This is criterion 2 of the issue. It went first because the offer layer projects every scenario's anchors: whatever widens the projection here can add or move anchors across the whole suite, and that was the risk this round had to learn early.",
      "criteria": [
        "`npx vitest run src/evaluator` passes the cases `Szenario: at-least-parent-any-reveal > rosters/01-empty-frame-wolf-lord-hidden.ros`, `> rosters/02-one-selection-reveals-wolf-lord.ros` and `> rosters/03-two-selections-reveal-unchanged.ros`.",
        "In those three rosters the report carries a slot with `anchorKind=\"offerAnchor\"` for the pair defId `b8be-a71f-569c-5cdc` / targetDefId `66bc-8fc1-81a2-9cd4`, reporting `isHidden` true at the empty frame and false at one resp. two selections, `current` 0 throughout.",
        "Rosters `04-wolf-lord-selected-self-reveal.ros` and `05-wolf-lord-twice-max-fires.ros` keep resolving the same pair as `anchorKind=\"occupied\"` and stay green.",
        "The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.",
        "Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched."
      ],
      "status": "done",
      "note": "Accepted by review after one correction round (0 findings). The three at-least-parent-any-reveal rosters 01/02/03 are green and 04/05 still resolve the pair as anchorKind=occupied; npx vitest run src/evaluator stood at 85 files / 1252 cases with exactly the two pre-existing greater-than-force-unlimited-gate failures left, which the second increment then closed. Landed: branch claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor merged fast-forward into claude/evaluator-abschluss-kampagne-tk44u4 and pushed (b93abbb).",
      "branch": "claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor",
      "steps": [],
      "attempts": [
        {
          "closedAs": "done",
          "at": "2026-08-10T12:07:10.803Z",
          "steps": [
            {
              "label": "research:parent-any-offer-anchor.0",
              "at": "2026-08-10T10:58:29.568Z",
              "return": {
                "needsTests": true,
                "plan": "GOAL (increment `parent-any-offer-anchor`, criterion 2 of issue 0147): make `docs/testing/at-least-parent-any-reveal` rosters 01/02/03 green, keep 04/05 green, keep the whole `npx vitest run src/evaluator` run green, and change nothing outside `src/evaluator/`.\n\nWHAT I MEASURED (throwaway node scripts against the frozen fixture, deleted again; no test run). Three separate engine defects stand between today and those three rosters. Each is reproduced below with the observation, not inferred.\n\nDEFECT 1 - the offer descent never reaches the Wolf Lord link.\n`optionDefinitionsUnder` in `src/evaluator/offer.js` (lines 127-143) descends, for a child that is an `entryLink` whose resolved target is a GROUP, into `child.resolved` only. It drops the link's OWN `children`. In the Vampire Counts catalogue the Thrall (`e37b-c827-99ac-b706`) carries `<entryLink id=\"2e0c-7fa1-642c-54b7\" type=\"selectionEntryGroup\" targetId=\"53e8-...\">` and that link element declares a nested `<entryLinks><entryLink id=\"85fb-0691-1ee6-37f8\" targetId=\"0719-24b8-19d4-c832\"/></entryLinks>` INSIDE itself (`Vampire Counts (6th definitive edition).cat` lines 3777-3792). The chain 85fb -> group 0719 -> link 614f-c7bb-2050-c603 -> group 84fd-049b-21b4-9075 -> link b8be-a71f-569c-5cdc therefore never gets walked, and rosters 01-03 carry no capability at all for that pair (measured: 245 capabilities, 105 under the Thrall, none for b8be/66bc; roster 04 has 246 and the pair resolves `occupied`). `Catalogue.xsd` line 396 (`EntryLink` extends `SelectionEntryBase`, lines 274-287) allows `selectionEntries`/`selectionEntryGroups`/`entryLinks` under an `entryLink`, and `catalogReader.js` line 914 already reads them into `def.children` - only the descent ignores them.\nFIX: when descending through a group link, walk BOTH the resolved target group's children and the link's own `children`, with the same gate chain `[...gates, child]`. Guard the two recursions with different keys in `visited` (the target group's id for the target, the link's own id for the link's children) so a shared group reached twice still lets each link contribute its local members and the recursion stays finite. Leave the `entryLink`-to-an-ENTRY branch alone: its children belong to that entry, not to the outer frame (pinned by `offer.test.js` \"bietet die Option einer Option NICHT im aeusseren Rahmen an\").\nMEASURED BLAST RADIUS of this widening, over all three fixture corpora the repo holds: in `src/evaluator/__fixtures__/whfb6-definitive` exactly 4 owner definitions gain options - `e37b-c827-99ac-b706` (Vampire Thrall) +33, `6822-0110-a7c9-cbb0` (Vampire Count) +33, `b77b-88d5-5e80-e178` (Vampire Lord) +33, `2679-58f4-1771-662d` (Ogre Tyrant) +8 - i.e. 107 new anchor slots, 41 distinct definition ids, `b8be-a71f-569c-5cdc` among them. `src/__fixtures__/whfb6` and `scripts/__fixtures__/showcase-empire` gain nothing. Across all 81 scenarios under `docs/testing/` there are 222 `capabilities` expectations, 56 of which name no `anchorKind`/`frameDefId`/`path`; none of those 56 collides with any of the 41 added ids or their targets, so no scenario becomes ambiguous. No scenario pins a slot `path` at all, so the index shift of anchors under those 4 owners cannot break one. A new offer anchor cannot add a violation either: `constraints.js` marks every result at an offer anchor `isReportable: false` (lines 15-18, 146-157), so `firing`/`absent` expectations are untouched.\n\nDEFECT 2 - `childId=\"any\"` counts the frame itself, so `atLeast 1` can never fail.\nA condition `atLeast 1 field=selections scope=parent childId=\"any\"` is read with `targetChildId: null` (`catalogReader.js` line 464) and queried as `scopeKey(frameKey, null)` = \"everything in this frame\". `countIndex.js` `indexNodeContribution` (lines 213-264) enters every node's contribution under EVERY target of `targetsOf`, `null` included, into EVERY frame of its ancestor chain - starting with the frame that IS the node (`isImmediate === true`). A selection frame therefore counts its own `instance.count` under `null`. Measured on a synthetic catalogue with reveal modifiers gated on `atLeast N ... scope=parent childId=any`: a unit holding 0/1/2 direct selections reads 1/2/3. The scenario derives 0/1/2 (`README.md`, \"Herleitung von Ist/Grenze\"), and `docs/battlescribe-data-format.md` 7.6 quotes the BSData wiki - the scope entity \"should sum up all field's values of DESCENDANT selections\". A force node is already excluded from the `null` target for exactly this reason (`countIndex.js` lines 106-108); a selection node is not. Consequence today: `atLeast 1 parent any` holds even at an empty frame, so roster 01 would report `isHidden: false` even once the anchor exists.\nFIX: in `indexNodeContribution`, at the immediate frame (`isImmediate === true`) contribute `selectionCount: 0`/`forceCount: 0` under the `null` target while keeping `costSums` intact. Keeping the costs is deliberate and load-bearing: `constraints.js` line 49 derives a percent limit's denominator with `query(ctx, limit.field, limit.scope, null, ...)`, and a percent COST limit at a selection frame must keep counting the frame's own cost. Every other target (own def id, `targetId`, `memberGroupIds`, raw `type`, effective category ids) stays exactly as it is - `scope=\"self\"` limits that count the carrier itself must keep working.\nREJECTED: subtracting `frame.instance.count` in `query.js` after the index read. Same numbers, but it duplicates knowledge of what the index holds into a second place and would need the same guard at each read site.\nMEASURED BLAST RADIUS: `childId=\"any\"` appears 92 times in the definitive corpus - 77 with `scope=\"roster\"` (root frame; the root is no node and forces contribute 0 to `null`, so unchanged), 1 with `scope=\"force\"` on a `limit::` field (resolved before any frame work, unchanged), and 3 with `scope=\"parent\"`: the two `atLeast 1` conditions of this scenario (`66bc-8fc1-81a2-9cd4` and the corpus-unreachable `c791-87b9-b00a-ddb4`) and one `<repeat>` on `6eac-4ed9-4511-ff14` (\"Grappling Hooks\", commented `1p/model`) in the `.gst`, which no scenario and no evaluator test uses. `src/__fixtures__/whfb6/Orcs and Goblins.cat` has 25, all on a category-id scope, and no test under `src/evaluator/` builds a roster from those entries. Watch `constraints.test.js`, `limitDerivation.test.js` and `modifiers.percentCondition.test.js` for a synthetic percent-SELECTION denominator at a selection frame; if one goes red it encodes the old self-inclusive count and is to be corrected against 7.6, not worked around.\n\nDEFECT 3 - a visibility bracket's reveal overrides the member's own written `hidden`.\n`offer.js` hands each anchor the chain of groups/group-links it descended through (`gates`), `effectiveState.js` `baseHiddenOf` (lines 114-119) ORs their base `hidden` into the node's base value, and `modifiers.js` `applyModifiersOfNodes` (lines 754-771) then runs each gate's `field=\"hidden\"` modifiers AT the node, outermost first, AFTER the node's own modifiers - last writer wins. Measured on a synthetic catalogue: a member with its own written `hidden=\"true\"` and NO modifier at all, held by a group link with base `hidden=\"true\"` plus a firing `set hidden=\"false\"`, reports `isHidden: false`. It must stay hidden - the bracket became visible, the member did not. For the Wolf Lord anchor the gate chain is `[2e0c-7fa1-642c-54b7, 85fb-0691-1ee6-37f8, 614f-c7bb-2050-c603]`, and 614f carries base `hidden=\"true\"` with a `set hidden=\"false\"` gated on the Von Carstein bloodline, which every one of the five rosters holds. So the innermost gate would write `false` in all five and roster 01 would fail even with defects 1 and 2 fixed.\nFIX: compose visibility as an OR of two independently resolved tracks instead of one shared boolean: the node's own effective hidden (own/target base value plus the node's own `hidden` modifiers) OR each gate's own effective hidden (that gate's base value plus that gate's `hidden` modifiers). A hidden bracket keeps hiding what it holds; a revealed bracket merely stops hiding and never overrules the member's own value. Mechanically this means `baseHiddenOf` no longer folds the gates' base values into the node's own base, and the gate modifiers no longer write the node's hidden slot directly - they resolve their own gate's visibility, which is then ORed into what the node reports. Pick the mechanism; the semantics above is the contract.\nWHAT MUST STAY GREEN, and does under that semantics: all five cases of `offer.hiddenGate.test.js` \"Sichtbarkeits-Klammer\" (every member there carries no own `hidden`, so own-track = visible and the gate track decides, exactly as today, including \"deckt die Option wieder auf, sobald ein Modifikator die Klammer sichtbar macht\"), and both real-data blocks of the same file (the Vampire-Coast slots and Heavy/Light Armour likewise carry no own `hidden`). `effectiveState.baseHiddenInheritance.test.js` pins the base-value rule and will need its wording and possibly its assertions restated to the two-track rule - that is a unit test of the engine and is yours to correct.\n\nORDER OF WORK: defect 1 first (the anchor has to exist before anything about it can be observed), then 2, then 3; after each, `npx vitest run src/evaluator` tells you what moved.\n\nDO NOT TOUCH: `evalTree.js` `selectionDefinitionsUnder` (lines 713-726) has the same link-own-children gap and feeds the mandatory phantoms and the occupied nodes' gate chains. Widening it would add mandatory phantoms and therefore VIOLATIONS across the suite, which none of this increment's criteria ask for. The occupied Wolf Lord in rosters 04/05 gets no gate chain today and passes; leave it that way. Also leave `report.js` and `fixpoint.js` alone: measured, an offer anchor already reports `current: 0`, gets its `targetDefId` and its effective name, and the post-pass already applies modifiers to it. No file under `docs/testing/` may change.",
                "moduleMap": "- `src/evaluator/offer.js` - the offer layer (ADR 0035/0036). `optionDefinitionsUnder` (lines 127-143) is the descent that has to also walk a group link's own `children`; `attachOfferAnchors` (line 209) and the dedup helpers around it stay as they are. The module doc's rule 2 (lines 20-24) states the descent and has to be restated with the change.\n- `src/evaluator/countIndex.js` - the counting index. `indexNodeContribution` (lines 213-264) enters a node's contribution under every target into every ancestor frame; `targetsOf` (line 132) defines the targets and already excludes a force node from the `null` target (doc lines 99-113). This file is MISSING from the planner's codemap and is where defect 2 lives.\n- `src/evaluator/effectiveState.js` - `baseHiddenOf` (lines 114-119) ORs the node's own base `hidden` with the base `hidden` of its visibility gates. One of the two places defect 3 lives.\n- `src/evaluator/modifiers.js` - `applyModifiersOfNodes` (lines 754-771) runs a node's own modifiers, then its info carriers, then each gate's `hidden` modifiers at the node (`hiddenOnly` guard at line 641). The other place defect 3 lives. Also MISSING from the planner's codemap.\n- `src/evaluator/offer.test.js` - unit tests of the offer set; rule 2's wording is pinned here (lines 146-181) and the new descent rule belongs beside it.\n- `src/evaluator/offer.hiddenGate.test.js` - pins how a hidden bracket above an offered definition behaves, synthetically (lines 90-178) and against the real Vampire Counts data (lines 242-325). The new \"a revealed bracket does not overrule the member's own hidden\" case belongs here.\n- `src/evaluator/effectiveState.baseHiddenInheritance.test.js` - pins the base-hidden inheritance the gate chain rests on; expect to restate it for the two-track rule.\n- `src/evaluator/constraints.js` - READ ONLY here, but two lines decide the blast radius: line 49 derives a percent limit's denominator with a `null` target, and lines 146-157 make every result at an offer anchor unreportable.\n- `src/evaluator/e2e.testcatalog.test.js` - the manifest-driven runner; the check, never an edit. Its capability selectors and the \"exactly one match\" rule are at lines 404-438.\n- `docs/testing/at-least-parent-any-reveal/` - the specification (`scenario.json`, `README.md`, `rosters/*.ros`). Out of scope by the issue; read only.\n- `src/evaluator/__fixtures__/whfb6-definitive/` - the frozen catalogue corpus the scenario is derived from. Read only.\n\nCORRECTIONS TO THE PLANNER'S CODEMAP for this increment: `evalTree.js`, `fixpoint.js` and `report.js` are listed there but are NOT to be changed - measured, the anchor node shape, the post-pass and the report projection already do their part, and widening `evalTree.js`'s `selectionDefinitionsUnder` would move violations. `countIndex.js` and `modifiers.js` are missing from it and are required.",
                "environment": "- Repository root: `/home/user/tome_of_battle`. Branch: `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`.\n- Node 22 and the installed `node_modules` are already in place; nothing needs installing. Vitest runs under the `jsdom` environment configured in `vitest.config.js`, which is what supplies the `DOMParser` the catalogue reader uses.\n- The whole check, and the only command anyone downstream runs for it: `npx vitest run src/evaluator` (from the repository root).\n- Single-file runs while working, all from the repository root: `npx vitest run src/evaluator/offer.test.js`, `npx vitest run src/evaluator/offer.hiddenGate.test.js`, `npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js`, `npx vitest run src/evaluator/effectiveState.baseHiddenInheritance.test.js`.\n- No linter, formatter, type check or build is asked for in this increment. `npm run lint`, `npm run typecheck`, `npm run knip`, `npm run depcruise` and the puppeteer E2E in `e2e/` exist but are not part of this increment's checks - the issue's own decision is that `npx vitest run src/evaluator` is the check command, and CLAUDE.md exempts an evaluator-only change from the full `npm test`.",
                "testPlan": "TESTS ARE NEEDED. The three E2E rosters are the specification and are already committed under `docs/testing/` - they must not be touched, and they are not enough on their own: each of the three engine rules below has to be pinned as a unit test at the layer it belongs to, so a later change that undoes one fails at that layer instead of only in a five-roster scenario.\n\nSUITE DOC: there is NO `CLAUDE.md` under `src/evaluator/` and none under `docs/testing/` - the only ones in the repository are `./CLAUDE.md` and `./scripts/lib/CLAUDE.md`. Write the missing suite doc for the evaluator unit tests as part of your step, from what the neighbouring test files actually do, and keep it short. What those files do today, for orientation and not as a substitute for reading them: every test file is a plain vitest `describe`/`it` with German titles, installs `globalThis.DOMParser = new JSDOM().window.DOMParser` at module top, builds a MINIMAL synthetic catalogue as an XML template string in the file (real fixture catalogues are used only where the point is the real data, and then read from `src/evaluator/__fixtures__/whfb6-definitive` via `readFileSync`), drives the public facade `prepareDataset` + `evaluate` from `./evaluator.js` (or, for a layer test, `PreparedDataset.contentsOf(prepareDataset(...))` plus `buildEvalTree`/`attachOfferAnchors`), and asserts against the returned report. Nothing is mocked or stubbed anywhere in this suite.\n\nCASES, per acceptance criterion.\n\nCRITERION \"the report carries an `offerAnchor` slot for `b8be-a71f-569c-5cdc`/`66bc-8fc1-81a2-9cd4` in rosters 01-03\" -> defect 1.\nFile: `src/evaluator/offer.test.js`, in the existing block \"Angebot unter einer belegten Auswahl: die direkten Optionen, nicht mehr\". Level: unit, against a new minimal synthetic catalogue in that file (the existing `CATALOGUE_XML` has no group link with local children; extend it or add a second template beside it, as the file already does for its mandatory-option case). Command: `npx vitest run src/evaluator/offer.test.js`.\n- Case 1.1: a selected unit carries an `entryLink type=\"selectionEntryGroup\"` to a shared group, and that link element declares its own nested `<entryLinks>` with a link to a SECOND shared group whose member is an entry. Expect: the offer anchors under the unit contain the member of the second group AND the members of the linked target group. Today only the latter appears.\n- Case 1.2 (depth): nest one level deeper - the locally declared group link itself carries a locally declared group link. Expect the innermost member is offered under the same unit frame. This is the shape the real data has (`2e0c` -> local `85fb` -> `0719` -> `614f` -> `84fd` -> `b8be`).\n- Case 1.3 (the boundary stays): an `entryLink` whose target is an ENTRY, carrying its own nested children, is still yielded as the option and its children are NOT offered in the outer frame. This is the existing rule \"die Optionen einer geschachtelten Auswahl gehoeren dieser\" and must not be widened.\n- Case 1.4 (repeat/cycle): two locally declared links on the same owner pointing at the SAME shared group, and a group whose local child links back to an ancestor group. Expect: the descent terminates, and no definition gets two anchors in one frame (the dedup already covers the second half - assert both, since the new `visited` keying is what makes it true).\n- Case 1.5 (gates carry): make the locally declared group link `hidden=\"true\"`. Expect the deep member's offer anchor reports `isHidden: true` - i.e. the bracket chain of the new path is the same chain the old path builds.\n- Case 1.6 (empty): a group link with no local children at all behaves exactly as today (control, so the change reads as an addition).\n\nCRITERION \"`isHidden` true at the empty frame, false at one resp. two selections\" -> defect 2, the counting half.\nFile: a new `src/evaluator/countIndex.frameOwnAnyTarget.test.js` (the suite names layer tests `<module>.<topic>.test.js`; `countIndex.costSumCarrierFrame.test.js` and `countIndex.syntheticAnchors.test.js` are the nearest neighbours). Level: unit through the public facade, reading the effect off a reveal modifier or a limit rather than off the index directly. Command: `npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js`.\n- Case 2.1: a unit whose option carries `set hidden=\"false\"` gated on `atLeast 1 field=\"selections\" scope=\"parent\" childId=\"any\"`. Roster with the unit holding NOTHING. Expect the option's slot reports `isHidden: true` (count 0). Today it reports false.\n- Case 2.2: same catalogue, unit holds one direct selection. Expect `isHidden: false` (count 1).\n- Case 2.3 (`atLeast` stays `atLeast`): unit holds two direct selections. Expect `isHidden: false` still.\n- Case 2.4 (the exact number): three options gated on `atLeast 1`, `atLeast 2`, `atLeast 3`. With the unit holding one selection, exactly the first two are revealed - this reads the count off as 1 and would catch an off-by-one in either direction.\n- Case 2.5 (nesting flag): a selection one level deeper does not count with `includeChildSelections=\"false\"` and does count with `\"true\"`.\n- Case 2.6 (control, own id target): a `scope=\"self\"` limit on the frame still counts the frame itself. This is the case the fix must NOT break, and the reason it removes only the `null` target's selection count.\n- Case 2.7 (control, cost): a percent limit on a COST field at a selection frame still derives its denominator including the frame's own cost. This is the second thing the fix must not break.\n- Case 2.8 (control, force): `scope=\"force\" childId=\"any\"` is unchanged - a force frame never counted itself.\n\nCRITERION \"`isHidden` true at the empty frame\" -> defect 3, the visibility half.\nFile: `src/evaluator/offer.hiddenGate.test.js`, beside the existing synthetic block. Level: unit through the facade, with the file's existing `catalogueWith`/`armyWithSquad`/`TOKEN_CONDITION` helpers. Command: `npx vitest run src/evaluator/offer.hiddenGate.test.js`.\n- Case 3.1: a member with its OWN `hidden=\"true\"` and no modifier, inside a shared group (or group link) that is `hidden=\"true\"` and carries a firing `set hidden=\"false\"`. Expect the member's offer anchor reports `isHidden: true` - the bracket became visible, the member did not. Today it reports false.\n- Case 3.2 (the reveal still works): the same catalogue with the member carrying no own `hidden`. Expect `isHidden: false` once the bracket's modifier fires, `true` while it does not. This is the existing rule and its regression guard.\n- Case 3.3 (own reveal wins over own base): a member with own `hidden=\"true\"` and its own firing `set hidden=\"false\"`, inside a visible bracket. Expect `isHidden: false`.\n- Case 3.4 (bracket still hides): a member with own `hidden=\"false\"` inside a bracket whose modifier SETS `hidden=\"true\"`. Expect `isHidden: true`.\n- Case 3.5 (cumulative): two nested brackets, the outer hidden and the inner revealed by a firing modifier. Expect `isHidden: true` - the outer still hides.\n`src/evaluator/effectiveState.baseHiddenInheritance.test.js` pins the current single-track base rule; restate its wording and its assertions to the two-track rule rather than deleting it.\n\nNOT TESTED, deliberately: the `.ros` rosters, `scenario.json` and `README.md` of `at-least-parent-any-reveal` are the specification and stay untouched - no new scenario is authored here (issue, Out of scope). The 107 extra offer anchors the widening produces in the definitive corpus are not asserted one by one; the closed check below covers them, and no expectation anywhere counts anchors. The other pinned scenario `greater-than-force-unlimited-gate` belongs to a later increment and gets no test here.\n\nWHAT COUNTS AS DONE - the closed list, verbatim, from the repository root:\n- `npx vitest run src/evaluator`\nNothing else is run. Not `npm test`, not the puppeteer E2E, not the linter, not the type check: the issue's own decision names this command, and CLAUDE.md exempts an evaluator-only change from the full run.\n\nWHAT IS ALREADY RED, stated from reading and not from a run - I did not run the check list, not even once. In `npx vitest run src/evaluator` today, two scenarios fail: `Szenario: at-least-parent-any-reveal` rosters 01, 02 and 03 (this increment closes them; the runner fails them as a manifest error, \"capabilities: kein Slot mit defId=...\", because the slot does not exist at all), and `Szenario: greater-than-force-unlimited-gate` rosters 02 and 03, which belong to the OTHER increment and must stay exactly as red as they are - neither worse nor fixed here. Everything else in that run is green today and has to stay green.\nTHE ONE EXCEPTION I took, and why: I ran three throwaway node scripts (outside vitest, deleted again, nothing committed) against the frozen fixture to settle questions this plan depends on and that reading could not answer - whether the missing anchor is the depth or the dropped link children (it is the dropped link children), what `childId=\"any\"` at a parent frame actually counts (frame plus its selections, 1/2/3 where the scenario derives 0/1/2), and whether a bracket's reveal overrides a member's own written `hidden` (it does). A wrong answer to any of the three would have cost a whole round.",
                "checks": [
                  "npx vitest run src/evaluator"
                ],
                "questions": [],
                "summary": "Three separate engine defects, each reproduced against the frozen fixture, stand between today and the three rosters of `at-least-parent-any-reveal`: the offer descent drops a group link's own nested children (so the Wolf Lord slot never exists), `childId=\"any\"` counts the parent frame itself (so `atLeast 1` holds even at an empty frame), and a visibility bracket's reveal overrides a member's own written `hidden`. The widening is contained - 107 new anchors on 4 owner definitions in one fixture corpus, no scenario made ambiguous, no new violations possible."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on its own branch: `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`, off `claude/evaluator-abschluss-kampagne-tk44u4`.\nBefore anything else, on `claude/evaluator-abschluss-kampagne-tk44u4`: record the name with the backlog helper's `branch` subcommand, as `branch docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`, commit that state change and push `claude/evaluator-abschluss-kampagne-tk44u4`. Then create the branch with `git checkout -b claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`, push it with `-u`, and work on it from there.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 1 is yours:\n  An unselected deep option appears as an offer anchor, so its reveal is observable — Make `docs/testing/at-least-parent-any-reveal` green: the report must carry an offer anchor for the `Wolf Lord` option in rosters where nothing in its group chain is selected, so the `set hidden=\"false\"` gated on `atLeast 1` in the parent frame can be seen flipping at all. This is criterion 2 of the issue. It goes first because the offer layer projects every scenario's anchors: whatever widens the projection here can add or move anchors across the whole suite, and that is the risk this round has to learn early.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: at-least-parent-any-reveal > rosters/01-empty-frame-wolf-lord-hidden.ros`, `> rosters/02-one-selection-reveals-wolf-lord.ros` and `> rosters/03-two-selections-reveal-unchanged.ros`.\n  - In those three rosters the report carries a slot with `anchorKind=\"offerAnchor\"` for the pair defId `b8be-a71f-569c-5cdc` / targetDefId `66bc-8fc1-81a2-9cd4`, reporting `isHidden` true at the empty frame and false at one resp. two selections, `current` 0 throughout.\n  - Rosters `04-wolf-lord-selected-self-reveal.ros` and `05-wolf-lord-twice-max-fires.ros` keep resolving the same pair as `anchorKind=\"occupied\"` and stay green.\n  - The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nDeliberately not yours, and not a gap — a later increment takes each of these: An occupied slot keeps its count when its max bound is lifted to unlimited.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/0147-evaluator-coverage-gaps/backlog.json`, before you research.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "tests:parent-any-offer-anchor.0",
              "at": "2026-08-10T11:15:09.712Z",
              "return": {
                "summary": "Added failing unit tests for all three defects the researcher's plan named (offer.js's group-link descent, the parent/any reveal count, and the hidden-bracket-vs-own-hidden composition), wrote the suite doc src/evaluator/CLAUDE.md that did not exist, and confirmed npx vitest run src/evaluator now fails the three at-least-parent-any-reveal rosters as manifest errors (missing offer anchor) exactly as the plan described, with 7 new unit-test failures pinning the underlying causes and the pre-existing greater-than-force-unlimited-gate failures unchanged.",
                "cases": [
                  {
                    "case": "1.1 - a selected unit carries an entryLink type=selectionEntryGroup to a shared group whose link element declares its own nested entryLinks to a SECOND shared group whose member is an entry; expect both the second group's member and the direct target's members to be offered.",
                    "file": "src/evaluator/offer.test.js",
                    "testName": "Angebot unter einer belegten Auswahl: die direkten Optionen, nicht mehr > bietet das Mitglied einer lokal am Gruppen-Verweis deklarierten zweiten Verweiskette zusaetzlich zu den Mitgliedern des direkten Ziels an",
                    "demands": "offeredIdsUnder(root, DEEP_UNIT_ID) contains both GROUP_A_MEMBER_ID (the direct target's member) and GROUP_B_MEMBER_ID (the member reached through the link's own locally-declared nested entryLinks).",
                    "got": "FAILED as expected: `expected [ 'entry-group-a-member' ] to include 'entry-group-b-member' at src/evaluator/offer.test.js:244`. Only the direct target's member is offered; the locally nested second link is dropped."
                  },
                  {
                    "case": "1.2 (depth) - nest one level deeper: the locally declared group link itself carries a locally declared group link; expect the innermost member offered under the same unit frame, matching the real-data shape (2e0c -> local 85fb -> 0719 -> 614f -> 84fd -> b8be).",
                    "file": "src/evaluator/offer.test.js",
                    "testName": "Angebot unter einer belegten Auswahl: die direkten Optionen, nicht mehr > steigt durch eine zweite Verschachtelungsebene lokal deklarierter Gruppen-Verweise ab",
                    "demands": "offeredIdsUnder(root, DEEP_UNIT_ID) contains DEEPEST_MEMBER_ID, reached through outer -> mid -> inner locally-declared group links.",
                    "got": "FAILED as expected: `expected [] to include 'entry-deepest-member' at src/evaluator/offer.test.js:280`. No anchors are produced at all through the second nesting level."
                  },
                  {
                    "case": "1.3 (the boundary stays) - an entryLink whose target is an ENTRY, carrying its own nested children, is still yielded as the option and its children are NOT offered in the outer frame.",
                    "file": "src/evaluator/offer.test.js",
                    "testName": "Angebot unter einer belegten Auswahl: die direkten Optionen, nicht mehr > bietet einen Verweis auf eine Auswahl mit eigenen lokalen Kindern nur als Verweis an — die Kinder bleiben der inneren Auswahl (Grenze bleibt bestehen)",
                    "demands": "The link itself (ENTRY_LINK_WITH_CHILDREN_ID) is offered; neither its target (SHARED_ENTRY_TARGET_ID) nor its own nested child (CHILD_OF_LINKED_ENTRY_ID) is offered at the outer frame.",
                    "got": "PASSED already: existing boundary behaviour is unaffected by the widening this round asks for; kept as a regression guard."
                  },
                  {
                    "case": "1.4 (repeat/cycle) - two locally declared links on the same owner pointing at the SAME shared group, and a group whose local child links back to an ancestor group; expect the descent to terminate and no definition to get two anchors in one frame.",
                    "file": "src/evaluator/offer.test.js",
                    "testName": "Angebot unter einer belegten Auswahl: die direkten Optionen, nicht mehr > laesst die Abstiegsschleife an einem Zyklus enden und vergibt trotz zweier Verweise auf dieselbe Gruppe nur einen Anker",
                    "demands": "The cycle-member's def id appears in offeredIdsUnder(root, DEEP_UNIT_ID) exactly once, despite two sibling links to the same shared group and that group's own local link back to itself.",
                    "got": "PASSED already: the current dedup/traversal already terminates and dedupes here; kept as a regression guard for the widening this round makes."
                  },
                  {
                    "case": "1.5 (gates carry) - make the locally declared group link hidden=true; expect the deep member's offer anchor to report isHidden true, i.e. the bracket chain of the new path is the same chain the old path builds.",
                    "file": "src/evaluator/offer.test.js",
                    "testName": "Angebot unter einer belegten Auswahl: die direkten Optionen, nicht mehr > vererbt an den Anker eines ueber die neue Abstiegskette erreichten Mitglieds dieselbe Sichtbarkeits-Klammer wie die alte Kette",
                    "demands": "The capability for GROUP_B_MEMBER_ID with anchorKind OFFER_ANCHOR exists and reports isHidden true, inherited from the outer locally-declared link's hidden=\"true\".",
                    "got": "FAILED as expected: `expected undefined to be defined at src/evaluator/offer.test.js:380` — no capability exists for the member at all, since the anchor is never produced (same root cause as 1.1/1.2)."
                  },
                  {
                    "case": "1.6 (empty) - a group link with no local children at all behaves exactly as today (control).",
                    "file": "src/evaluator/offer.test.js",
                    "testName": "Angebot unter einer belegten Auswahl: die direkten Optionen, nicht mehr > KONTROLLE: ein Gruppen-Verweis ohne eigene lokale Kinder bietet weiterhin nur die Mitglieder seines Ziels an",
                    "demands": "offeredIdsUnder(root, DEEP_UNIT_ID) equals exactly [GROUP_A_MEMBER_ID] for a plain, unnested group link.",
                    "got": "PASSED already, as intended: the baseline single-hop group descent is untouched by the widening."
                  },
                  {
                    "case": "2.1 - a unit whose option carries set hidden=false gated on atLeast 1 field=selections scope=parent childId=any; roster with the unit holding NOTHING; expect the option's offer anchor to report isHidden true.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": der leere Rahmen bleibt versteckt > haelt die Option versteckt, solange der Elternrahmen keine Auswahl haelt",
                    "demands": "offerCapabilityOf(report, OPTION_1_ID).isHidden is true when the unit has zero children.",
                    "got": "FAILED as expected: `expected false to be true` — the empty frame already reports the option as visible; the childId=\"any\" target is not counted at all."
                  },
                  {
                    "case": "2.2 - same catalogue, unit holds one direct selection; expect isHidden false.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": der leere Rahmen bleibt versteckt > deckt dieselbe Option auf, sobald der Elternrahmen eine Auswahl haelt",
                    "demands": "offerCapabilityOf(report, OPTION_1_ID).isHidden is false with one sibling selection in the unit.",
                    "got": "PASSED already."
                  },
                  {
                    "case": "2.3 (atLeast stays atLeast) - unit holds two direct selections; expect isHidden false still.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": der leere Rahmen bleibt versteckt > haelt die Option weiterhin aufgedeckt, wenn der Elternrahmen zwei Auswahlen haelt — atLeast bleibt atLeast",
                    "demands": "offerCapabilityOf(report, OPTION_1_ID).isHidden is false with two sibling selections.",
                    "got": "PASSED already."
                  },
                  {
                    "case": "2.4 (the exact number) - three options gated on atLeast 1/2/3; with the unit holding one selection, exactly the first two are revealed.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": der leere Rahmen bleibt versteckt > deckt bei genau einer Auswahl im Elternrahmen exakt die ersten beiden von drei gestaffelten Grenzen auf",
                    "demands": "With one sibling selection, OPTION_1 and OPTION_2 report isHidden false and OPTION_3 reports isHidden true.",
                    "got": "PASSED already (this off-by-one guard is not itself broken; only the empty-frame case in 2.1 is)."
                  },
                  {
                    "case": "2.5 (nesting flag) - a selection one level deeper does not count with includeChildSelections=false and does count with includeChildSelections=true.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": das Verschachtelungs-Flag entscheidet ueber die Tiefe > zaehlt eine Auswahl eine Ebene tiefer NICHT mit includeChildSelections=\"false\" — die Grenze bleibt unerfuellt",
                    "demands": "With a direct child (HOST) plus its own nested child selected, an atLeast-2 gate stays hidden (unmet) when includeChildSelections=\"false\" (only the direct child counts), and reveals when includeChildSelections=\"true\" (direct + nested child both count). Written as a pair of tests sharing the same catalogue/roster shape, varying only the flag.",
                    "got": "The includeChildSelections=\"false\" half FAILED as expected: `expected false to be true` (the nested child wrongly counts, or the direct child's own frame-count is wrong, revealing the option early). The includeChildSelections=\"true\" companion test 'zaehlt dieselbe Auswahl eine Ebene tiefer mit includeChildSelections=\"true\" mit — die Grenze wird erfuellt' PASSED already."
                  },
                  {
                    "case": "2.6 (control, own id target) - a scope=self limit on the frame still counts the frame itself; must not be broken by the fix.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": Kontrollen, die der Fix nicht brechen darf > KONTROLLE: eine scope=\"self\"-Grenze zaehlt weiterhin ihren eigenen Rahmen",
                    "demands": "A scope=\"self\" max-1 limit on a unit selected with count=2 fires with actual 2 / bound 1.",
                    "got": "PASSED already, as intended: this control is not part of the broken childId=\"any\" path."
                  },
                  {
                    "case": "2.7 (control, cost) - a percent limit on a COST field at a selection frame still derives its denominator including the frame's own cost.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": Kontrollen, die der Fix nicht brechen darf > KONTROLLE: eine Prozent-Grenze auf ein Kostenfeld zieht den eigenen Beitrag des Rahmens weiterhin in den Nenner",
                    "demands": "A max 50% percent-of-roster-cost limit on a 40-point Hero, alongside a 60-point Other unit (roster total 100), reports no violation (40/100 = 40% <= 50%); a denominator that wrongly excluded the Hero's own cost (Other only = 60) would give 40/60 = 66.7% and a false violation.",
                    "got": "PASSED already, as intended."
                  },
                  {
                    "case": "2.8 (control, force) - scope=force childId=any is unchanged; a force frame never counted itself.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": Kontrollen, die der Fix nicht brechen darf > KONTROLLE: scope=\"force\" childId=\"any\" bleibt unveraendert — ein Kontingent-Rahmen zaehlte sich nie selbst",
                    "demands": "An atLeast-1 scope=force childId=any reveal gate is already satisfied (isHidden false) once a single unit is selected in the force.",
                    "got": "PASSED already, as intended."
                  },
                  {
                    "case": "3.1 - a member with its OWN hidden=true and no modifier, inside a shared group/link that is hidden=true and carries a firing set hidden=false; expect the member's offer anchor to report isHidden true (the bracket became visible, the member did not).",
                    "file": "src/evaluator/offer.hiddenGate.test.js",
                    "testName": "Sichtbarkeits-Klammer UND eigenes hidden: beide Spuren muessen gelten (Kriterium 2, Defekt 3) > haelt ein Mitglied mit eigenem hidden=\"true\" versteckt, obwohl seine Klammer durch einen feuernden Modifikator sichtbar wird",
                    "demands": "offerCapabilityOf(report, MEMBER_ID).isHidden is true even though the enclosing shared group's own hidden=\"true\" is unconditionally flipped to false by its modifier.",
                    "got": "FAILED as expected: `expected false to be true` — a revealed bracket currently makes a self-hidden member visible too."
                  },
                  {
                    "case": "3.2 (the reveal still works) - the same catalogue with the member carrying no own hidden; expect isHidden false once the bracket's modifier fires, true while it does not. Plan itself calls this \"the existing rule and its regression guard\".",
                    "file": "",
                    "testName": "",
                    "demands": "n/a — not written as a new case.",
                    "got": "Not written: this exact assertion (member without own hidden, dynamic TOKEN_CONDITION-gated bracket reveal, isHidden true without the token / false with it) already exists verbatim as 'Sichtbarkeits-Klammer: Option einer versteckten Gruppe (Kriterium 2) > deckt die Option wieder auf, sobald ein Modifikator die Klammer sichtbar macht' in the same file and already passes. Writing a second, near-identical test next to it would duplicate coverage the plan itself names as already-existing; I left it unwritten rather than clone it. Flagged in openQuestions in case the researcher meant something more specific by this case."
                  },
                  {
                    "case": "3.3 (own reveal wins over own base) - a member with own hidden=true and its own firing set hidden=false modifier, inside a visible bracket; expect isHidden false.",
                    "file": "src/evaluator/offer.hiddenGate.test.js",
                    "testName": "Sichtbarkeits-Klammer UND eigenes hidden: beide Spuren muessen gelten (Kriterium 2, Defekt 3) > macht ein Mitglied mit eigenem feuerndem Aufdeck-Modifikator sichtbar, obwohl es selbst hidden=\"true\" traegt, in einer sichtbaren Klammer",
                    "demands": "offerCapabilityOf(report, MEMBER_ID).isHidden is false: the member's own firing modifier overrides its own base hidden=\"true\", inside a bracket that is itself visible.",
                    "got": "PASSED already: the member's own effective-hidden computation already applies its own modifier correctly; unaffected by the bracket-composition defect."
                  },
                  {
                    "case": "3.4 (bracket still hides) - a member with own hidden=false inside a bracket whose modifier SETS hidden=true; expect isHidden true.",
                    "file": "src/evaluator/offer.hiddenGate.test.js",
                    "testName": "Sichtbarkeits-Klammer UND eigenes hidden: beide Spuren muessen gelten (Kriterium 2, Defekt 3) > versteckt ein Mitglied mit eigenem hidden=\"false\", wenn ein Modifikator die Klammer per hidden=\"true\" versteckt",
                    "demands": "offerCapabilityOf(report, MEMBER_ID).isHidden is true when the bracket's own modifier sets hidden=\"true\", even though the member's own base hidden is \"false\".",
                    "got": "PASSED already: the bracket-hides-a-visible-member direction already works; only the reverse (3.1/3.5) is broken."
                  },
                  {
                    "case": "3.5 (cumulative) - two nested brackets, the outer hidden and the inner revealed by a firing modifier; expect isHidden true (the outer still hides).",
                    "file": "src/evaluator/offer.hiddenGate.test.js",
                    "testName": "Sichtbarkeits-Klammer UND eigenes hidden: beide Spuren muessen gelten (Kriterium 2, Defekt 3) > haelt ein Mitglied versteckt, dessen innere Klammer per feuerndem Modifikator aufgedeckt wird, solange die aeussere Klammer versteckt bleibt",
                    "demands": "offerCapabilityOf(report, INNER_MEMBER_ID).isHidden is true: the outer bracket's static hidden=\"true\" (no reveal modifier of its own) still wins even though the inner bracket's own firing modifier reveals it.",
                    "got": "FAILED as expected: `expected false to be true` — once the inner bracket's modifier fires, the outer bracket's still-hidden state is currently lost instead of composing with it."
                  },
                  {
                    "case": "effectiveState.baseHiddenInheritance.test.js's top comment restated to the two-track rule (Issue 0147), assertions kept unchanged.",
                    "file": "src/evaluator/effectiveState.baseHiddenInheritance.test.js",
                    "testName": "n/a (file-level top comment only, no test added or removed)",
                    "demands": "The file's own header comment must note that its single-track own-hidden composition (link/target/modifiers) is now only one of two tracks, the second (bracket visibility) living in offer.hiddenGate.test.js, without deleting or weakening any of its 20 existing pinned assertions.",
                    "got": "Comment updated; `npx vitest run src/evaluator/effectiveState.baseHiddenInheritance.test.js` still passes all 20 tests unchanged (comment-only edit)."
                  }
                ],
                "openQuestions": [
                  "Case 3.2's plan text frames it as 'the existing rule and its regression guard' — I read that as confirming the case is already covered by the passing test 'deckt die Option wieder auf, sobald ein Modifikator die Klammer sichtbar macht' in offer.hiddenGate.test.js and left it unwritten rather than duplicate it; if the researcher meant a genuinely new assertion (e.g. paired directly with 3.1's exact catalogue rather than the existing one), that needs to be named more specifically in a follow-up round.",
                  "The plan's case 1.4 asks to 'assert both' the same-shared-group dedup and the ancestor-cycle termination; I wrote one combined test (two entryLinks to the same group, one of which cycles back to its own group) with a single count-of-1 assertion, since both rules independently entail that same observable outcome in the catalogue I built — if the researcher wanted the two rules isolated into two separate catalogues/tests instead, that is a follow-up, not something I inferred was required."
                ],
                "questions": []
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 1 is yours:\n  An unselected deep option appears as an offer anchor, so its reveal is observable — Make `docs/testing/at-least-parent-any-reveal` green: the report must carry an offer anchor for the `Wolf Lord` option in rosters where nothing in its group chain is selected, so the `set hidden=\"false\"` gated on `atLeast 1` in the parent frame can be seen flipping at all. This is criterion 2 of the issue. It goes first because the offer layer projects every scenario's anchors: whatever widens the projection here can add or move anchors across the whole suite, and that is the risk this round has to learn early.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: at-least-parent-any-reveal > rosters/01-empty-frame-wolf-lord-hidden.ros`, `> rosters/02-one-selection-reveals-wolf-lord.ros` and `> rosters/03-two-selections-reveal-unchanged.ros`.\n  - In those three rosters the report carries a slot with `anchorKind=\"offerAnchor\"` for the pair defId `b8be-a71f-569c-5cdc` / targetDefId `66bc-8fc1-81a2-9cd4`, reporting `isHidden` true at the empty frame and false at one resp. two selections, `current` 0 throughout.\n  - Rosters `04-wolf-lord-selected-self-reveal.ros` and `05-wolf-lord-twice-max-fires.ros` keep resolving the same pair as `anchorKind=\"occupied\"` and stay green.\n  - The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nDeliberately not yours, and not a gap — a later increment takes each of these: An occupied slot keeps its count when its max bound is lifted to unlimited.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.0 --fields testPlan` — the test plan.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor tests:parent-any-offer-anchor.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor tests:parent-any-offer-anchor.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "implement:parent-any-offer-anchor.0",
              "at": "2026-08-10T11:26:49.645Z",
              "return": {
                "deviations": [
                  {
                    "planned": "Defect 3: 'Mechanically this means baseHiddenOf no longer folds the gates' base values into the node's own base, and the gate modifiers no longer write the node's hidden slot directly - they resolve their own gate's visibility, which is then ORed into what the node reports. Pick the mechanism; the semantics above is the contract.'",
                    "built": "Per-gate track in the existing carrier-keyed hidden map. `effectiveState.js` `baseHiddenOf` now returns only the carrier's own base value (the `visibilityGates` fold is gone), and `EffectiveState#isHidden(node)` composes: own effective hidden OR, for each gate, `isHidden(node, gate)`. `modifiers.js` `applyModifiersOfNodes` applies each gate's `hidden`-only modifiers with `carrier: gate` instead of `carrier: node`, so a gate writes its own slot. A carrier that is not the node (info element or gate) still reads exactly one track, unchanged.",
                    "why": "The plan left the mechanism open and named only the semantics. Reusing the existing (node, carrier) keying needs no new state and no new read site: `report.js`, `constraints.js` and `infoProjection.js` keep calling `isHidden(node)` / `isHidden(node, carrier)` untouched, and the gate-order dependency ('outermost first, innermost last writes') disappears instead of being reordered."
                  },
                  {
                    "planned": "'effectiveState.baseHiddenInheritance.test.js pins the base-value rule and will need its wording and possibly its assertions restated to the two-track rule - that is a unit test of the engine and is yours to correct.'",
                    "built": "Not touched. My role forbids writing or editing any test. The test-author had already restated that file's header comment to the two-track rule in the test step; all 20 of its assertions pass unchanged under the implementation (`npx vitest run src/evaluator/effectiveState.baseHiddenInheritance.test.js`, 20 passed, exit 0), so no assertion needed restating.",
                    "why": "Role boundary: a test I believe wrong, or that needs correcting, is a blocker in my return, not an edit."
                  }
                ],
                "commands": [
                  {
                    "command": "npx vitest run src/evaluator",
                    "covered": "85 test files, 1250 tests (the whole evaluator suite incl. the manifest-driven E2E runner over docs/testing/).",
                    "exitCode": 1,
                    "note": "1245 passed, 5 failed. Baseline before this step (same command, on the test-author's commit 3964682) was 12 failed / 1238 passed. Of the 5 remaining: 2 are pre-existing and untouched by this change (scenario `greater-than-force-unlimited-gate`, rosters 02 and 03 — they fail identically on the parent branch commit; the increment `parent-any-offer-anchor` touches no sentinel/limit code and `git diff claude/evaluator-abschluss-kampagne-tk44u4...HEAD` before my edits showed no production file changed at all), and 3 are red because they encode exactly the pre-fix counting rule this increment removes — see blockers. Everything the increment names is green: `at-least-parent-any-reveal` rosters 01/02/03 now pass and 04/05 stay green (5 passed, exit 0 with `-t \"at-least-parent-any-reveal\"`); `offer.test.js` 27/27, `offer.hiddenGate.test.js` 13/13, `effectiveState.baseHiddenInheritance.test.js` 20/20, `countIndex.frameOwnAnyTarget.test.js` 8/9."
                  }
                ],
                "blockers": [
                  "`countIndex.frameOwnAnyTarget.test.js` > 'deckt bei genau einer Auswahl im Elternrahmen exakt die ersten beiden von drei gestaffelten Grenzen auf' (line 136) is unsatisfiable together with case 2.1 in the same file: with ONE sibling selected it demands the `atLeast 2` option revealed, which requires the frame to count itself — the very off-by-one defect 2 removes; 2.1 demands the empty frame count 0. Measured after the fix (throwaway probe, deleted): 0 selections -> none revealed, 1 -> only atLeast 1, 2 -> atLeast 1+2. The test's roster is one selection short of its own title; it needs `armyWithUnit([SIBLING_A_ID, SIBLING_B_ID])`. I may not edit a test.",
                  "`infoProjection.test.js` > 'zeigt den effektiven Merkmalswert und den effektiven Namen, sobald sie haelt' (line 413) is red for the same reason: its condition is `atLeast field=\"selections\" scope=\"self\" value=\"2\"` with no `childId` (= the `any` target) on a unit that has no children at all, and the roster selects that unit twice. It therefore asserts that a frame counts its own instance count under `any`. `docs/battlescribe-data-format.md` 7.6 says the opposite ('Gezaehlt werden die Auswahlen *unterhalb* des Traegers der Grenze, nicht der Traeger selbst'). The plan foresaw this class ('if one goes red it encodes the old self-inclusive count and is to be corrected against 7.6, not worked around') but named three other files; I may not edit a test.",
                  "`report.test.js` > 'haengt die bedingte Meldung an den betreffenden Slot, wenn die Bedingung haelt' (line 246) is the identical pattern: `atLeast field=\"selections\" scope=\"self\" value=\"2\"`, no `childId`, on a childless unit selected twice. Same correction needed, same boundary.",
                  "Both remaining `greater-than-force-unlimited-gate` E2E failures (rosters 02/03) pre-date this increment and belong to code it never touched; left alone as scope not given."
                ],
                "questions": [],
                "summary": "Fixed the three engine defects behind the parent-any offer anchor: the offer descent now also walks a group link's own locally declared children, a selection frame no longer counts itself under the `childId=\"any\"` target, and a visibility bracket's reveal no longer overrides a member's own `hidden` (two independent tracks, ORed). All five `at-least-parent-any-reveal` rosters are green; `npx vitest run src/evaluator` is at 1245/1250 (exit 1) with two pre-existing failures and three tests that still encode the counting rule this change removes."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.0 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor tests:parent-any-offer-anchor.0 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor implement:parent-any-offer-anchor.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor implement:parent-any-offer-anchor.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "review:parent-any-offer-anchor.0",
              "at": "2026-08-10T11:34:14.802Z",
              "return": {
                "findings": [
                  {
                    "claim": "The new zeroing of a node's own contribution under the `null` target in its own frame (src/evaluator/countIndex.js:246-259) also zeroes the number a `scope=\"self\"` condition reads, so `condition type=\"atLeast\" field=\"selections\" scope=\"self\"` never holds any more and every modifier gated on it silently stops firing.",
                    "reproduction": "On this branch `npx vitest run src/evaluator/infoProjection.test.js src/evaluator/report.test.js` fails two cases that are green at the merge base. (a) src/evaluator/infoProjection.test.js:413, case 'zeigt den effektiven Merkmalswert und den effektiven Namen, sobald sie haelt': catalogue `cat-info-effective`, one unit `entry-unit` whose profile carries `set field=\"name\" value=\"Elite-Krieger\"` and `set field=\"<moveTypeId>\" value=\"6\"` gated on `condition type=\"atLeast\" field=\"selections\" scope=\"self\" value=\"2\"`; roster `{forces:[{defId:'entry-unit',count:2,children:[]}]}`. Expected profile.name 'Elite-Krieger', got 'Krieger' (and the characteristic stays at the base '4'). (b) src/evaluator/report.test.js:246, case 'haengt die bedingte Meldung an den betreffenden Slot, wenn die Bedingung haelt': catalogue `cat-cap-note`, entry `entry-banner-unit` with `add field=\"info\" value=\"Verbund erst ab zwei Einheiten\"` under the same condition, roster holding count 2. Expected `capability.authorMessages` `[{severity:'info',text:'Verbund erst ab zwei Einheiten'}]`, got `[]`. Cause confirmed: in a sandbox worktree checked out at this branch, restoring only `src/evaluator/countIndex.js` to its merge-base version turns both files green again (2 files, 42 cases, exit 0), with every other file of the diff unchanged. The `else if (isImmediate && targetId === null)` branch at countIndex.js:246 does not distinguish the frame-is-the-node case a `scope=\"self\"` query asks about from the descendant count a `scope=\"parent\" childId=\"any\"` query asks about.",
                    "criterion": "The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.",
                    "fix": "needs-plan"
                  },
                  {
                    "claim": "The increment's own new test case src/evaluator/countIndex.frameOwnAnyTarget.test.js:136 asserts a reveal that the very rule the file pins forbids, and fails.",
                    "reproduction": "`npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js`: the case 'deckt bei genau einer Auswahl im Elternrahmen exakt die ersten beiden von drei gestaffelten Grenzen auf' fails at line 136 with 'expected true to be false'. Input: catalogue `cat-parent-any` with three options gated on `atLeast 1 / 2 / 3 field=\"selections\" scope=\"parent\" childId=\"any\" shared=\"false\" includeChildSelections=\"true\"`, roster `armyWithUnit(['entry-sibling-a'])`, i.e. the unit frame holds exactly one selection. Under the rule this file pins (the frame's own node contributes nothing under the 'any' target), that frame counts 1, so `atLeast 2` does not hold and `entry-option-at-least-2` must report isHidden true; only the first of the three staggered limits is revealed, so the case title 'die ersten beiden von drei' is wrong as well. The other three cases in the same describe block, which assert only `entry-option-at-least-1`, pass.",
                    "criterion": "The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.",
                    "fix": "needs-plan"
                  },
                  {
                    "claim": "`src/evaluator/CLAUDE.md` cites `offer.hiddenGate.test.js` as an example of memoising `prepareDataset(...)` across a file's cases, and that file's own JSDoc claims the same ('einmal je Lauf'), but its `preparedVampireCounts()` re-parses the two real fixtures on every call.",
                    "reproduction": "src/evaluator/offer.hiddenGate.test.js:312-318 reads `/** Der aufbereitete DE-Datensatz (gst + Vampire Counts) — einmal je Lauf. */ function preparedVampireCounts() { return prepareDataset({...}); }` — a bare return with no cache, called at lines 322 and 385. The file `src/evaluator/CLAUDE.md` names it, together with `effectiveState.baseHiddenInheritance.test.js`, as the `preparedVampireCounts`/`vampireCountsDataset` memoisation pattern; only the latter memoises (`effectiveState.baseHiddenInheritance.test.js:327-333`, `preparedVampireCounts ??= prepareDataset(...)`). Right result: `preparedVampireCounts()` in offer.hiddenGate.test.js caches its dataset in a module-level variable the way effectiveState.baseHiddenInheritance.test.js does, so both its own JSDoc and the CLAUDE.md citation become true.",
                    "criterion": "none",
                    "fix": "direct"
                  }
                ],
                "findingCount": 3,
                "allDirect": false,
                "reason": "Two evaluator tests that were green before this change now fail: a condition of the form 'at least N selections, counted on the carrier itself' no longer holds, so a gated profile name and a gated author message disappear from the report. On top of that one of the increment's own new test cases asserts that an 'at least 2' reveal fires while the parent frame holds only one selection, and fails. The criterion that the whole evaluator run be green is therefore missed, even though the three target rosters of at-least-parent-any-reveal now pass.",
                "questions": [],
                "summary": "`npx vitest run src/evaluator` on the branch: 85 files, 1250 cases, exit 1 with 5 failures; at the merge base the same command gives 84 files, 1231 cases, exit 1 with 5 failures. The three at-least-parent-any-reveal rosters are indeed fixed and rosters 04/05 stay green, and the two greater-than-force-unlimited-gate failures are the pre-existing second pinned gap (unchanged, a later increment's work) - but three new failures appeared: two regressions caused by the countIndex change (scope=\"self\" selection conditions stop holding) and one self-contradictory case in the increment's own new test file. Observation, no correction needed: the diff also adds a brand-new 58-line suite doc `src/evaluator/CLAUDE.md` that no acceptance criterion asked for; it stays inside `src/evaluator/`, so it breaks no scope rule, but it is prose the increment was not asked to write."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nReview round 0 of increment 1. The increment's diff is its branch against the merge-base with `claude/evaluator-abschluss-kampagne-tk44u4`: judge `git diff claude/evaluator-abschluss-kampagne-tk44u4...HEAD` (three dots), whole, and nothing outside it.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 1 is yours:\n  An unselected deep option appears as an offer anchor, so its reveal is observable — Make `docs/testing/at-least-parent-any-reveal` green: the report must carry an offer anchor for the `Wolf Lord` option in rosters where nothing in its group chain is selected, so the `set hidden=\"false\"` gated on `atLeast 1` in the parent frame can be seen flipping at all. This is criterion 2 of the issue. It goes first because the offer layer projects every scenario's anchors: whatever widens the projection here can add or move anchors across the whole suite, and that is the risk this round has to learn early.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: at-least-parent-any-reveal > rosters/01-empty-frame-wolf-lord-hidden.ros`, `> rosters/02-one-selection-reveals-wolf-lord.ros` and `> rosters/03-two-selections-reveal-unchanged.ros`.\n  - In those three rosters the report carries a slot with `anchorKind=\"offerAnchor\"` for the pair defId `b8be-a71f-569c-5cdc` / targetDefId `66bc-8fc1-81a2-9cd4`, reporting `isHidden` true at the empty frame and false at one resp. two selections, `current` 0 throughout.\n  - Rosters `04-wolf-lord-selected-self-reveal.ros` and `05-wolf-lord-twice-max-fires.ros` keep resolving the same pair as `anchorKind=\"occupied\"` and stay green.\n  - The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nDeliberately not yours, and not a gap — a later increment takes each of these: An occupied slot keeps its count when its max bound is lifted to unlimited.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\nRead nothing out of docs/issues/0147-evaluator-coverage-gaps/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor review:parent-any-offer-anchor.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor review:parent-any-offer-anchor.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "research:parent-any-offer-anchor.1",
              "at": "2026-08-10T11:44:38.747Z",
              "return": {
                "needsTests": true,
                "plan": "## What is left of this increment\n\nRound 0 landed the offer descent fix (`offer.js`), the visibility-track split (`effectiveState.js` / `modifiers.js`) and a frame-own counting fix (`countIndex.js`), and all five rosters of `docs/testing/at-least-parent-any-reveal` resolve. The review found nothing against `offer.js`, `effectiveState.js` or `modifiers.js` — leave those three files exactly as they are. Two things are left, and only one of them is production code.\n\n## Fix (finding 1): the frame's own share must be readable again at a self frame\n\nThe defect. `countIndex.js:246` books a node's own contribution into its own frame under the `null` target with the counts zeroed:\n\n```js\n} else if (isImmediate && targetId === null) {\n  c = { selectionCount: 0, forceCount: 0, costSums: contribution.costSums };\n}\n```\n\nThat is right for `scope=\"parent\" childId=\"any\"` (the empty unit frame must count 0, which is what the scenario demands) and wrong for `scope=\"self\" field=\"selections\"`, because both queries land on the *same* index key. `catalogReader.js:464` maps `childId=\"any\"` and a missing `childId` alike to the target `null`, and `query.js:204-217` resolves `scope=\"self\"` (and every `shared=\"false\"` query) to the frame `ctx.node`. So `scopeKey(frameKeyOf(node), null)` is reached both by \"how many of me are there\" and by \"how much is inside that ancestor\", and zeroing at index time answers 0 to both. That is why `infoProjection.test.js:413` and `report.test.js:246` went red.\n\nThe rule to implement: **the frame node's own share under the `null` target counts only when the query is asked at that very node.** When the frame is a *different* node — always a proper ancestor: `scope=\"parent\"`, `roster`, `force`, `unit`, an ancestor id — `childId=\"any\"` asks what is *inside* the frame and the frame itself is not inside itself. When the frame *is* the querying node (`scope=\"self\"`, or any scope with `shared=\"false\"`, which `query.js:206` binds to the carrier instance), the query is the identity reading Battlescribe gives `self` and the node's own count is the answer. This is the narrowest rule that satisfies both pins: measured against the merge base, the only queries whose answer changes are those whose frame is a node other than the querying node, reading the `null` target — exactly the scenario's case.\n\nDo it in two places.\n\n**`src/evaluator/countIndex.js`** — split the frame's own share out of the crossing buckets instead of discarding it.\n\n1. Add a module-level key `const FRAME_OWN = 'frameOwn';` and a fifth tally under it in `emptyBuckets()` (line 68). Document it as *not* a crossing bucket: `bucketFor` never returns it, `addContribution` writes it by explicit key. Do not add it to the frozen `Bucket` enum (line 44) — that enum means \"boundary crossed on the way to the frame\" and this slot does not.\n2. In the `isImmediate && targetId === null` branch of `indexNodeContribution` (line 246): keep booking the costs-only contribution into the crossing bucket exactly as today, and additionally book a counts-only contribution into `FRAME_OWN` for the same key — `addContribution(tallies, scopeKey(frameKey, targetId), FRAME_OWN, { selectionCount: contribution.selectionCount, forceCount: contribution.forceCount, costSums: new Map() })`. The costs stay unconditional in the crossing bucket, so no reader of a percent denominator changes and nothing is counted twice.\n3. Give `combineBuckets` (line 294) a fifth parameter `includeFrameOwn` and `addTally(result, buckets[FRAME_OWN])` when it is true.\n4. Give `buildIndex().get` (line 334) a fifth parameter `includeFrameOwn = true` and pass it through. **Default true** — that is the merge-base answer, so `budget.js:59` (the only other caller, roster frame, cost sums) and every test that reads the index directly keep their values; only a caller that says otherwise narrows.\n5. Update the `@returns` JSDoc of `buildIndex` (line 321) for the new parameter, and rewrite the comment block at 246-258 to state the rule above instead of the current absolute one. Also correct the paragraph added to `targetsOf`'s JSDoc (lines 110-112), which now claims the frame never counts under `null`.\n\n**`src/evaluator/query.js`** — at the index read (line 412) pass `frame === ctx.node` as the fifth argument, and say in a comment why: the frame's own share is part of the answer only when the query is asked at the frame; from an ancestor frame `childId=\"any\"`/no `childId` asks for what is inside it. `ctx.node` is already in scope; `frame` is the value `resolveFrame` returned at line 393.\n\nWhy this and not the alternatives:\n\n- *Distinguish by the literal `childId=\"any\"` versus an absent `childId`.* Rejected: `docs/battlescribe-data-format.md` (`readCondition`'s cited rule, §7.7) says a missing `childId` means \"everything in the frame\", the same as `any`, and `catalogReader.js:464` collapses them on purpose. Making behaviour depend on whether an optional attribute was written is drift waiting to happen.\n- *Leave the index as round 0 made it and add the count back inside `query.js` from `frame.instance.count`.* Rejected: it duplicates `contributionOf`'s knowledge in a second place, and it would invent a count for synthetic nodes (offer anchors, phantoms) that `buildIndex` deliberately never indexes — a `scope=\"self\"` condition on an offer anchor would suddenly read 1.\n- *Revert round 0's `countIndex.js` change altogether.* Rejected: then the empty unit frame counts itself again and `rosters/01-empty-frame-wolf-lord-hidden.ros` goes red.\n\nKnown limitation, stated deliberately: `scope=\"self\" childId=\"any\"` keeps the merge-base reading (the node's own count is included). Nothing in the fixtures, the scenarios or the suite exercises it, and this increment is not the place to decide it.\n\n## Fix (finding 2): no production change\n\n`countIndex.frameOwnAnyTarget.test.js:132` asserts a reveal the rule forbids. With one selection in the unit frame the frame counts **1** — the frame node itself does not count, the three option entries are unselected offer anchors and are never counted — so `atLeast 1` reveals and `atLeast 2` does not. The engine is right and the case is wrong; it is corrected in the test plan. Do not touch the engine to make the old assertion pass.\n\n## Fix (finding 3): no production change\n\n`offer.hiddenGate.test.js`'s `preparedVampireCounts()` re-parses two real fixtures on every call. It is a test file; the correction is in the test plan.\n\n## Fence\n\nNothing outside `src/evaluator/` changes, and no file under `docs/testing/` is touched.",
                "moduleMap": [
                  "src/evaluator/countIndex.js - the count index. `Bucket` (44) and `bucketFor` (52) are the four crossing buckets; `contributionOf` (89) is the one source of what a node contributes; `targetsOf` (136) lists the targets a node counts under, `null` among them; `indexNodeContribution` (217) books every node into every ancestor frame and holds the `isImmediate && targetId === null` branch (246) this round rewrites; `combineBuckets` (294) and `buildIndex().get` (334) are the read side.",
                  "src/evaluator/query.js - the query primitive. `resolveFrame` (204) picks the frame node (`parent` first, then `shared=\"false\"` -> the carrier node, then the scope keyword); `query` (371) reads the index at `scopeKey(frameKeyOf(frame), targetId)` (411-417). The only place that knows whether the frame is the querying node.",
                  "src/evaluator/catalogReader.js - `readCondition` (443); line 464 maps `childId=\"any\"` and a missing `childId` alike to the target `null`. Read-only here; it explains why the two readings collide on one key.",
                  "src/evaluator/budget.js - `plannedRosterSum` (57) is the only other caller of `index.get`, positional, roster frame, cost sums only. Unaffected by a trailing defaulted parameter. Read-only.",
                  "src/evaluator/countIndex.frameOwnAnyTarget.test.js - round 0's pin of the frame-own rule, driven through `prepareDataset` + `evaluate`. Case at 132 asserts the wrong reveal; the `KONTROLLE` block (212-318) pins the constraint path, the percent denominator and the force frame, but not the condition path with `scope=\"self\"` and no `childId` - the gap that let the defect through.",
                  "src/evaluator/infoProjection.test.js - condition `atLeast selections scope=\"self\"` at 381/386, case at 413. Regression guard for the self frame; must go green untouched.",
                  "src/evaluator/report.test.js - condition `atLeast selections scope=\"self\"` at 234, case at 246. Same guard on the author-message channel; must go green untouched.",
                  "src/evaluator/offer.hiddenGate.test.js - `preparedVampireCounts()` (312-318) re-parses the two real fixtures on every call; call sites at 322 and 385.",
                  "src/evaluator/effectiveState.baseHiddenInheritance.test.js - the memoisation pattern to copy: module-level `preparedVampireCounts` plus `vampireCountsDataset()` with `??=` (327-334).",
                  "src/evaluator/CLAUDE.md - the suite doc written in round 0; its memoisation bullet cites `offer.hiddenGate.test.js`, which is what makes finding 3 a contradiction rather than a nicety.",
                  "src/evaluator/offer.js, src/evaluator/effectiveState.js, src/evaluator/modifiers.js - round 0's other two fixes live here (offer descent into an entryLink's local children; visibility as the OR of two independently resolved tracks). The review found nothing against them: leave untouched.",
                  "CODEMAP CORRECTION: the planner's codemap for this increment names offer.js, evalTree.js, effectiveState.js, fixpoint.js, report.js and three test files. The two files this correction round actually changes - src/evaluator/countIndex.js and src/evaluator/query.js - are not on it, and evalTree.js, fixpoint.js and report.js are not touched by this increment at all."
                ],
                "environment": [
                  "Repository root: /home/user/tome_of_battle. Branch: claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor.",
                  "Node v22.22.2; dependencies are installed (node_modules/.bin/vitest exists). No install step is needed.",
                  "Test framework: vitest, run through npx from the repository root.",
                  "`npx vitest run src/evaluator` - the whole evaluator suite: unit/layer tests plus the manifest-driven scenario runner over docs/testing/.",
                  "`npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js` - one file.",
                  "`npx vitest run src/evaluator/infoProjection.test.js src/evaluator/report.test.js` - the two regression-guard files.",
                  "`npx vitest run src/evaluator/offer.hiddenGate.test.js` - one file; it parses two real fixtures from src/evaluator/__fixtures__/whfb6-definitive/ and is the slowest file of the three.",
                  "There is no linter or type check in this increment's command list; do not run one.",
                  "Every evaluator test installs `globalThis.DOMParser` from jsdom at module top - jsdom is a devDependency and already installed."
                ],
                "testPlan": "## Whether\n\nTests are needed. Two of the three review findings are test-side work, and the production fix has to be pinned where the rule lives.\n\n## What is already red\n\nI ran nothing. Per the review's measurement, `npx vitest run src/evaluator` on this branch fails exactly three cases before the fix:\n- `src/evaluator/infoProjection.test.js:413` - 'zeigt den effektiven Merkmalswert und den effektiven Namen, sobald sie haelt'\n- `src/evaluator/report.test.js:246` - 'haengt die bedingte Meldung an den betreffenden Slot, wenn die Bedingung haelt'\n- `src/evaluator/countIndex.frameOwnAnyTarget.test.js:132` - 'deckt bei genau einer Auswahl im Elternrahmen exakt die ersten beiden von drei gestaffelten Grenzen auf'\n\nEverything else in the directory, the five rosters of `at-least-parent-any-reveal` included, is green. Leave the first run to whoever runs it downstream.\n\n## The suite doc\n\n`src/evaluator/CLAUDE.md` exists (round 0 wrote it) and its conventions hold - load it and follow it; do not restate it here. One contradiction to note and to resolve by the change below, not by editing the doc: the doc cites `offer.hiddenGate.test.js` as an example of memoising `prepareDataset(...)`, and that file does not memoise. Once case C lands, the citation is true and the doc needs no edit.\n\n## What\n\nThe increment has one testable criterion: the whole `npx vitest run src/evaluator` run is green, the three `at-least-parent-any-reveal` rosters and the two occupied ones included. The cases below are what proves the production rule and what closes the two review findings.\n\n**A - the frame-own rule, in `src/evaluator/countIndex.frameOwnAnyTarget.test.js` (the file that owns it).**\n\n- **A1 (correction of the failing case at line 132).** Same catalogue `cat-parent-any`, same roster `armyWithUnit(['entry-sibling-a'])` - the unit frame holds exactly one selection. Expected: `entry-option-at-least-1` `isHidden` false, `entry-option-at-least-2` `isHidden` **true**, `entry-option-at-least-3` `isHidden` true. The frame counts 1: the frame node itself does not count under the `any` target, and the three option entries are unselected offer anchors, which are never counted. Retitle the case to match - it reveals the first of three staggered limits, not the first two.\n- **A2 (new, the rule finding 1 broke).** A catalogue with one hidden entry that carries `<modifier type=\"set\" field=\"hidden\" value=\"false\">` gated on `<condition type=\"atLeast\" value=\"2\" field=\"selections\" scope=\"self\"/>` - **no `childId` attribute**, which is the exact shape `infoProjection.test.js:381` and `report.test.js:234` use. Roster: that entry selected with `count: 2` under the force. Expected: its capability record (any anchor kind - it is occupied) reports `isHidden` false. This is the case whose absence let the defect through: the block's existing `scope=\"self\"` control uses a *constraint*, whose counted target is the definition id, so it never touches the `null` target.\n- **A3 (new, the edge of A2).** Same catalogue, roster with `count: 1`. Expected: `isHidden` true. One is not at least two - it pins that A2 reads a real count rather than a constant.\n\nA2 and A3 belong in the block 'Kontrollen, die der Fix nicht brechen darf' next to the existing three controls. The file's `offerCapabilityOf` helper filters on `AnchorKind.OFFER_ANCHOR` and will not find an occupied slot; a sibling helper that looks up a capability by `defId` regardless of anchor kind is the smallest way through.\n\n**B - the two existing regression guards.** `src/evaluator/infoProjection.test.js:413` and `src/evaluator/report.test.js:246` already pin the self-frame condition path on the profile/name channel and on the author-message channel. Write nothing new for them and **edit neither file**: they must go green again by themselves. If either still fails after the production change, the production change is wrong, not the test.\n\n**C - finding 3, in `src/evaluator/offer.hiddenGate.test.js`.** Make `preparedVampireCounts()` (312-318) cache its dataset in a module-level variable, the way `effectiveState.baseHiddenInheritance.test.js:327-334` does, so its own JSDoc ('einmal je Lauf') and the `CLAUDE.md` citation become true. No assertion and no case title changes; both call sites (322, 385) keep working unchanged.\n\n**Settled from the round before, so nothing further is asked for:**\n- The earlier case 3.2 needs no new test. The passing case 'deckt die Option wieder auf, sobald ein Modifikator die Klammer sichtbar macht' in `offer.hiddenGate.test.js` is that regression guard; do not duplicate it.\n- The earlier case 1.4 stays as one combined test (two `entryLink`s to the same group, one cycling back to its own group, one count-of-1 assertion). Do not split it into two catalogues.\n\n**Left untested, on purpose:**\n- `scope=\"self\" childId=\"any\"` - the production plan keeps the merge-base reading there and this increment does not decide it; pinning it would freeze an undecided reading.\n- The percent denominator and the force frame - the two existing controls at `countIndex.frameOwnAnyTarget.test.js:247` and `:291` already cover them and the production change does not move either (costs stay unconditional in the crossing bucket; a force node has no `null` target at all).\n- No new scenario under `docs/testing/` - out of scope by the issue's fence.\n\n## How\n\n- A1, A2, A3: integration level through the public facade (`prepareDataset` + `evaluate` from `./evaluator.js`), synthetic inline catalogues, observed through `report.capabilities`. File: `src/evaluator/countIndex.frameOwnAnyTarget.test.js`. Framework: vitest. Command: `npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js`.\n- B: no authoring. Command that shows them green: `npx vitest run src/evaluator/infoProjection.test.js src/evaluator/report.test.js`.\n- C: no level change, a refactor inside the file. File: `src/evaluator/offer.hiddenGate.test.js`. Framework: vitest. Command: `npx vitest run src/evaluator/offer.hiddenGate.test.js`.\n\n## What counts as done\n\nExactly one command, run from the repository root, judges this increment:\n\n- `npx vitest run src/evaluator`\n\nIt covers the unit and layer tests and the manifest-driven scenario runner, so the three `at-least-parent-any-reveal` rosters, the two occupied ones and every scenario the earlier rounds closed are inside it. Nothing else is to be run: no other suite, no linter, no type check, no screenshot.",
                "checks": [
                  "npx vitest run src/evaluator"
                ],
                "questions": [],
                "summary": "The parent-any fix zeroed the frame's own share for every reader of the `null` target, which also silenced `scope=\"self\"` conditions; the plan books that share into a separate index slot and lets the query include it only when the frame is the querying node, so the empty unit frame still counts 0 while `scope=\"self\"` counts itself again. Two of the three review findings are test-side: the new staggered-limits case asserts a reveal its own rule forbids, and `offer.hiddenGate.test.js` still re-parses its fixture on every call."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 1 is yours:\n  An unselected deep option appears as an offer anchor, so its reveal is observable — Make `docs/testing/at-least-parent-any-reveal` green: the report must carry an offer anchor for the `Wolf Lord` option in rosters where nothing in its group chain is selected, so the `set hidden=\"false\"` gated on `atLeast 1` in the parent frame can be seen flipping at all. This is criterion 2 of the issue. It goes first because the offer layer projects every scenario's anchors: whatever widens the projection here can add or move anchors across the whole suite, and that is the risk this round has to learn early.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: at-least-parent-any-reveal > rosters/01-empty-frame-wolf-lord-hidden.ros`, `> rosters/02-one-selection-reveals-wolf-lord.ros` and `> rosters/03-two-selections-reveal-unchanged.ros`.\n  - In those three rosters the report carries a slot with `anchorKind=\"offerAnchor\"` for the pair defId `b8be-a71f-569c-5cdc` / targetDefId `66bc-8fc1-81a2-9cd4`, reporting `isHidden` true at the empty frame and false at one resp. two selections, `current` 0 throughout.\n  - Rosters `04-wolf-lord-selected-self-reveal.ros` and `05-wolf-lord-twice-max-fires.ros` keep resolving the same pair as `anchorKind=\"occupied\"` and stay green.\n  - The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nDeliberately not yours, and not a gap — a later increment takes each of these: An occupied slot keeps its count when its max bound is lifted to unlimited.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/0147-evaluator-coverage-gaps/backlog.json`, before you research.\nThis is correction loop 1 of 2 for this increment, and what the round before produced is your work order. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor review:parent-any-offer-anchor.0 --fields findings` — the review's findings.\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor tests:parent-any-offer-anchor.0 --fields openQuestions` — what the test-author left open, and it is yours to settle.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "tests:parent-any-offer-anchor.1",
              "at": "2026-08-10T11:51:58.127Z",
              "return": {
                "cases": [
                  {
                    "case": "A1 (correction of the failing case at line 132) — with exactly one selection in the unit's frame, the reveal limits atLeast 1/2/3 on scope=\"parent\" childId=\"any\" uncover only the first of the three staggered limits: the frame itself does not count under the \"any\" target, and the three option entries are unselected offer anchors, which never count.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": der leere Rahmen bleibt versteckt > deckt bei genau einer Auswahl im Elternrahmen exakt die erste von drei gestaffelten Grenzen auf",
                    "demands": "offerCapabilityOf(report, OPTION_1_ID).isHidden === false; offerCapabilityOf(report, OPTION_2_ID).isHidden === true; offerCapabilityOf(report, OPTION_3_ID).isHidden === true, on armyWithUnit([SIBLING_A_ID]) against PARENT_ANY_CATALOGUE_XML. Retitled from '...die ersten beiden von drei...' to '...die erste von drei...' to match.",
                    "got": "This is a correction of a wrong expectation the round-0 test carried (it asserted OPTION_2 isHidden === false), not a pin of missing behaviour: the production code on this branch already returns isHidden === true for OPTION_2 at this roster (confirmed by running the file before the edit: 'AssertionError: expected true to be false' at the old assertion, line 136). After the correction the case is green already — nothing further is asked of the implementer for A1; it only guards against the assertion regressing back to the wrong reading."
                  },
                  {
                    "case": "A2 (new, the rule finding 1 broke) — a hidden entry revealed by atLeast 2 field=\"selections\" scope=\"self\", no childId attribute (the exact shape infoProjection.test.js:381 and report.test.js:234 use), selected with count: 2, must reveal (isHidden false). The block's existing scope=\"self\" control uses a constraint, whose counted target is the definition id, so it never exercises the null target that scope=\"self\" without childId and childId=\"any\" collide on.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": Kontrollen, die der Fix nicht brechen darf > KONTROLLE: eine Bedingung mit scope=\"self\" und OHNE childId liest den eigenen Bestand, nicht die Konstante null",
                    "demands": "capabilityOf(report, SELF_SCOPE_NO_CHILDID_ID).isHidden === false on a roster where the entry itself is selected with count 2 (the atLeast 2 threshold), read through a new capabilityOf(report, defId) helper (added next to offerCapabilityOf) that returns a capability by defId regardless of anchor kind, since this entry's slot is occupied, not an offer anchor.",
                    "got": "Fails as required: `npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js` — 'AssertionError: expected true to be false // Object.is equality' at line 374 (capabilityOf(...).isHidden). The slot stays hidden at count 2 even though the atLeast-2 condition is met, the same defect infoProjection.test.js and report.test.js already show on their own channels."
                  },
                  {
                    "case": "A3 (new, the edge of A2) — same catalogue, roster with count: 1 (below the atLeast-2 threshold), must stay hidden (isHidden true). Pins that A2 reads a real count rather than a constant.",
                    "file": "src/evaluator/countIndex.frameOwnAnyTarget.test.js",
                    "testName": "Aufdeck-Grenze mit scope=\"parent\" childId=\"any\": Kontrollen, die der Fix nicht brechen darf > KONTROLLE: dieselbe Bedingung haelt die Auswahl versteckt, solange ihr eigener Bestand die Grenze verfehlt",
                    "demands": "capabilityOf(report, SELF_SCOPE_NO_CHILDID_ID).isHidden === true on the same catalogue with count 1.",
                    "got": "Passes already, both before and after the implementer's fix is expected to land: with today's defect the entry is unconditionally hidden (the null-target bug never reveals it, whatever the count), so this edge case cannot demonstrate the missing behaviour by itself — only A2 does. It is written anyway because the plan asks for it as the paired edge of A2, and it will keep pinning the correct reason (1 < 2) once the fix makes A2 pass for the right reason instead of by accident."
                  },
                  {
                    "case": "B - the two existing regression guards on the self-frame condition path (profile/name channel and author-message channel) must go green again by themselves; write nothing new for them and edit neither file.",
                    "file": "",
                    "testName": "",
                    "demands": "",
                    "got": "Not authored, per the plan. Confirmed both still red exactly as the plan's 'already red' list states, untouched: `npx vitest run src/evaluator/infoProjection.test.js src/evaluator/report.test.js` — infoProjection.test.js:413 'AssertionError: expected 'Krieger' to be 'Elite-Krieger'' (zeigt den effektiven Merkmalswert und den effektiven Namen, sobald sie haelt); report.test.js:246 'AssertionError: expected [] to deeply equal [ { severity: 'info', ... } ]' (haengt die bedingte Meldung an den betreffenden Slot, wenn die Bedingung haelt). Both fail for the same self-scope-without-childId defect A2 pins; if either is still red once the implementer's fix lands, the plan says the fix is wrong, not the test."
                  },
                  {
                    "case": "C - finding 3: preparedVampireCounts() in offer.hiddenGate.test.js must cache its dataset in a module-level variable (the effectiveState.baseHiddenInheritance.test.js pattern), so its own JSDoc ('einmal je Lauf') and the CLAUDE.md citation become true. No assertion and no case title changes; both call sites keep working unchanged.",
                    "file": "src/evaluator/offer.hiddenGate.test.js",
                    "testName": "",
                    "demands": "preparedVampireCounts() returns a dataset cached across calls within the file's run instead of re-parsing the two real fixtures every time it is called; both call sites (evaluateVampireIn, evaluateThrall) keep calling it unchanged.",
                    "got": "Not a new assertion — a refactor of the helper only, so there is no red state to prove: added a module-level `let cachedVampireCounts = null;` and rewrote the function body to `cachedVampireCounts ??= prepareDataset(...); return cachedVampireCounts;`, keeping the function's name and signature. `npx vitest run src/evaluator/offer.hiddenGate.test.js` — 13 tests, all still green, confirming both call sites are unaffected. src/evaluator/CLAUDE.md needed no edit: its citation of the preparedVampireCounts/vampireCountsDataset pattern in this file is now accurate."
                  }
                ],
                "openQuestions": [],
                "questions": [],
                "summary": "Wrote the three planned cases in src/evaluator/countIndex.frameOwnAnyTarget.test.js (corrected A1, new A2/A3 with a new capabilityOf helper) and refactored offer.hiddenGate.test.js's dataset caching for finding 3 (C); left the two existing regression guards (B) untouched as instructed. `npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js` shows exactly A2 red ('expected true to be false' at line 374) with A1/A3 and all prior cases green; `npx vitest run src/evaluator/offer.hiddenGate.test.js` is 13/13 green after the refactor; the full `npx vitest run src/evaluator` (85 files, 1252 tests) shows 5 red: A2, the two untouched B guards (infoProjection.test.js:413, report.test.js:246), and the two greater-than-force-unlimited-gate cases confirmed (by stashing my changes) to fail identically before this round's edits — they belong to the separate unlimited-slot-count increment and are unchanged by this work."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 1 is yours:\n  An unselected deep option appears as an offer anchor, so its reveal is observable — Make `docs/testing/at-least-parent-any-reveal` green: the report must carry an offer anchor for the `Wolf Lord` option in rosters where nothing in its group chain is selected, so the `set hidden=\"false\"` gated on `atLeast 1` in the parent frame can be seen flipping at all. This is criterion 2 of the issue. It goes first because the offer layer projects every scenario's anchors: whatever widens the projection here can add or move anchors across the whole suite, and that is the risk this round has to learn early.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: at-least-parent-any-reveal > rosters/01-empty-frame-wolf-lord-hidden.ros`, `> rosters/02-one-selection-reveals-wolf-lord.ros` and `> rosters/03-two-selections-reveal-unchanged.ros`.\n  - In those three rosters the report carries a slot with `anchorKind=\"offerAnchor\"` for the pair defId `b8be-a71f-569c-5cdc` / targetDefId `66bc-8fc1-81a2-9cd4`, reporting `isHidden` true at the empty frame and false at one resp. two selections, `current` 0 throughout.\n  - Rosters `04-wolf-lord-selected-self-reveal.ros` and `05-wolf-lord-twice-max-fires.ros` keep resolving the same pair as `anchorKind=\"occupied\"` and stay green.\n  - The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nDeliberately not yours, and not a gap — a later increment takes each of these: An occupied slot keeps its count when its max bound is lifted to unlimited.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.1 --fields testPlan` — the test plan.\nThis is correction round 1 of this increment.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor tests:parent-any-offer-anchor.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor tests:parent-any-offer-anchor.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "implement:parent-any-offer-anchor.1",
              "at": "2026-08-10T11:56:52.459Z",
              "return": {
                "deviations": [],
                "commands": [
                  {
                    "command": "npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js src/evaluator/infoProjection.test.js src/evaluator/report.test.js",
                    "when": "before the change (red-first check)",
                    "exit": 1,
                    "note": "53 cases, 3 failed exactly as the test plan states: countIndex.frameOwnAnyTarget.test.js:374 (case A2, capabilityOf(SELF_SCOPE_NO_CHILDID_ID).isHidden expected true to be false), infoProjection.test.js:413 ('Krieger' vs 'Elite-Krieger'), report.test.js:246 ([] vs the conditional author message). All three are the one self-scope-without-childId defect finding 1 names."
                  },
                  {
                    "command": "npx vitest run src/evaluator/countIndex.frameOwnAnyTarget.test.js src/evaluator/infoProjection.test.js src/evaluator/report.test.js",
                    "when": "after the change",
                    "exit": 0,
                    "note": "53 cases, all green. The two regression guards (infoProjection.test.js, report.test.js) went green untouched, as the plan demanded; cases A1 and A3 stayed green."
                  },
                  {
                    "command": "npx vitest run src/evaluator",
                    "when": "after the change (the command that counts)",
                    "exit": 1,
                    "note": "85 files, 1252 cases: 1250 passed, 2 failed. Nothing skipped or excluded. Both failures are in src/evaluator/e2e.testcatalog.test.js, scenario 'greater-than-force-unlimited-gate', rosters 02 and 03: 'Slot 0ff3-ec4d-1c6b-6d53: current: expected +0 to deeply equal 1' resp. '... to deeply equal 2'. They are NOT mine: I ran the same file on the untouched branch head (git stash) and got the identical two failures with byte-identical case titles and assertion messages (2 failed | 321 passed), then the identical pair again with the change applied. They belong to the unlimited-gate/`current` reporting gap of issue 0147, which this increment neither plans nor touches, so they are reported here and left alone."
                  }
                ],
                "blockers": [],
                "questions": [],
                "summary": "The frame node's own share under the `null` target is now indexed in a separate FRAME_OWN slot of countIndex.js and read only when the query is asked at the frame itself (query.js passes `frame === ctx.node`), so `scope=\"self\"` without a childId reads its own count again while `scope=\"parent\" childId=\"any\"` still refuses to count the frame; findings 2 and 3 needed no production change, as the plan said. The evaluator suite is green except two pre-existing failures in the 'greater-than-force-unlimited-gate' e2e scenario that fail identically on the untouched branch head."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor research:parent-any-offer-anchor.1 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor tests:parent-any-offer-anchor.1 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor implement:parent-any-offer-anchor.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor implement:parent-any-offer-anchor.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "review:parent-any-offer-anchor.1",
              "at": "2026-08-10T12:04:06.082Z",
              "return": {
                "findings": [],
                "findingCount": 0,
                "allDirect": false,
                "reason": "",
                "questions": [],
                "summary": "Increment 1 is accepted: `npx vitest run src/evaluator` (85 files, 1252 cases, exit non-zero with exactly 2 failures) turns the three at-least-parent-any-reveal rosters green — they fail at the merge base 6f1f605 and pass at HEAD — while rosters 04/05 keep resolving the pair as anchorKind=occupied; the only red left is the pre-existing second pinned gap greater-than-force-unlimited-gate (rosters 02/03, `Slot 0ff3-ec4d-1c6b-6d53: current expected 1 resp. 2, got 0`), identical at the merge base and explicitly a later increment's work. The change stays inside src/evaluator/ (countIndex.js, query.js, effectiveState.js, modifiers.js, offer.js plus unit tests), touches no file under docs/testing/, and matches the bsdata reference §7.6 (\"Gezählt werden die Auswahlen unterhalb des Trägers, nicht der Träger selbst\") for the null/`any` target, with the frame-own share kept only for the query at the frame itself and the cost denominator unchanged. Two observations, neither worth a correction round: the diff adds a new suite doc src/evaluator/CLAUDE.md that no criterion asked for (it follows the existing scripts/lib/CLAUDE.md convention and describes the suite accurately), and its claim \"Every file installs globalThis.DOMParser\" holds for 73 of the 85 test files — the 12 that do not parse XML omit it."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nReview round 1 of increment 1. The increment's diff is its branch against the merge-base with `claude/evaluator-abschluss-kampagne-tk44u4`: judge `git diff claude/evaluator-abschluss-kampagne-tk44u4...HEAD` (three dots), whole, and nothing outside it.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 1 is yours:\n  An unselected deep option appears as an offer anchor, so its reveal is observable — Make `docs/testing/at-least-parent-any-reveal` green: the report must carry an offer anchor for the `Wolf Lord` option in rosters where nothing in its group chain is selected, so the `set hidden=\"false\"` gated on `atLeast 1` in the parent frame can be seen flipping at all. This is criterion 2 of the issue. It goes first because the offer layer projects every scenario's anchors: whatever widens the projection here can add or move anchors across the whole suite, and that is the risk this round has to learn early.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: at-least-parent-any-reveal > rosters/01-empty-frame-wolf-lord-hidden.ros`, `> rosters/02-one-selection-reveals-wolf-lord.ros` and `> rosters/03-two-selections-reveal-unchanged.ros`.\n  - In those three rosters the report carries a slot with `anchorKind=\"offerAnchor\"` for the pair defId `b8be-a71f-569c-5cdc` / targetDefId `66bc-8fc1-81a2-9cd4`, reporting `isHidden` true at the empty frame and false at one resp. two selections, `current` 0 throughout.\n  - Rosters `04-wolf-lord-selected-self-reveal.ros` and `05-wolf-lord-twice-max-fires.ros` keep resolving the same pair as `anchorKind=\"occupied\"` and stay green.\n  - The whole `npx vitest run src/evaluator` run is green - every scenario the earlier rounds closed included, and the second pinned scenario no worse than it is today.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nDeliberately not yours, and not a gap — a later increment takes each of these: An occupied slot keeps its count when its max bound is lifted to unlimited.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\nRead nothing out of docs/issues/0147-evaluator-coverage-gaps/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor review:parent-any-offer-anchor.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor review:parent-any-offer-anchor.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `questions`\n  - `summary`\n"
            }
          ]
        }
      ]
    },
    {
      "id": "unlimited-slot-count",
      "title": "An occupied slot keeps its count when its max bound is lifted to unlimited",
      "goal": "Make `docs/testing/greater-than-force-unlimited-gate` green: the occupied Slaughtermaster slot must keep reporting how many selections its frame holds after the `set -1` modifier has opened its only max limit. This is criterion 1 of the issue, and the only work left in it. Today rosters 02 and 03 are the sole red cases of the whole evaluator suite, failing as `Slot 0ff3-ec4d-1c6b-6d53: current: expected +0 to deeply equal 1` resp. `... to deeply equal 2` - measured identically before and after the landed increment, which touched no limit or sentinel code.",
      "criteria": [
        "`npx vitest run src/evaluator` passes the cases `Szenario: greater-than-force-unlimited-gate > rosters/02-tyrant-one-slaughtermaster-unlimited.ros` and `> rosters/03-tyrant-two-slaughtermasters-unlimited.ros`.",
        "In those two rosters the slot defId `0ff3-ec4d-1c6b-6d53` in frame `729f-9246-5cd3-5044` reports `current` 1 resp. 2, with `effectiveMax` null, `headroom` null and `isBlocked` false.",
        "Roster `01-no-tyrant-slaughtermaster-max0.ros` keeps reporting `current` 1 with `effectiveMax` 0 and `isBlocked` true, and the firing and the silence of limit `c70d-c292-36ee-21b5` are unchanged in all four rosters.",
        "`npx vitest run src/evaluator` ends at exit 0 with no failure left at all - the scenarios the earlier rounds closed, the unlimited-sentinel unit tests, and the `at-least-parent-any-reveal` scenario the landed increment fixed, all included.",
        "The count a slot reports keeps meaning what it means today wherever a limit result still supplies it: no case of `report.test.js`, `constraints.test.js` or `constraints.unlimitedSentinel.test.js` is weakened to make the two rosters pass.",
        "Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched."
      ],
      "status": "done",
      "note": "Accepted by review with 0 findings on the first attempt. npx vitest run src/evaluator ends at exit 0 with 85 files / 1258 cases, no skips: rosters 02 and 03 of greater-than-force-unlimited-gate are green, roster 01 keeps current 1 with effectiveMax 0 and isBlocked true, and the at-least-parent-any-reveal scenario the earlier increment fixed stays green. The whole change is one fallback step in the reported count in src/evaluator/report.js plus six unit cases in report.test.js. Landed: branch claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count merged fast-forward into claude/evaluator-abschluss-kampagne-tk44u4 and pushed (72f4486).",
      "branch": "claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count",
      "steps": [],
      "attempts": [
        {
          "closedAs": "done",
          "at": "2026-08-10T12:42:41.305Z",
          "steps": [
            {
              "label": "research:unlimited-slot-count.0",
              "at": "2026-08-10T12:24:55.369Z",
              "return": {
                "needsTests": true,
                "plan": "GOAL: an occupied slot keeps reporting how many selections it holds after its only max limit has been lifted to unlimited. Today `report.js` derives a slot's `current` from the MAX result, else the MIN result, else the category-anchor occupancy map, else the literal 0 — and `constraints.js:115` returns no result at all for an UNLIMITED bound, so an occupied slot whose only limit went unlimited falls through to 0.\n\nTHE CHANGE (one production line plus its documentation, in `src/evaluator/report.js` only):\n\n1. In `toCapability` (line 441) extend the fallback chain by the slot's own instance count:\n     current: maxResult?.actual ?? minResult?.actual ?? anchorOccupancies.get(node) ?? node.instance?.count ?? 0,\n   Nothing else in the record changes: `effectiveMax`, `headroom`, `isBlocked`, `isMandatoryUnmet`, `effectiveMin` all keep reading the (absent) limit results and are already correct in rosters 02/03 — the only failing field there is `current`.\n\n2. Update the prose that states the rule: the JSDoc block above `toCapability` (lines 366-372, the sentence 'traegt der Slot keine (nicht suspendierte) Grenze, ist er an einem Kategorie-Anker ... an jeder anderen Ankerart 0') and the inline comment at lines 437-440. New wording, in the file's existing German for that block (new explanatory comments in English per the suite doc): where a limit was evaluated its `actual` is the reported number; without one, a category anchor reports the occupancy counted in its frame, and a **real (occupied) slot reports its own instance count** — the piece count of its selection; every synthetic anchor (mandatory phantom, group anchor, offer anchor) carries no instance and stays 0. Name the case that forces the rule: a max limit lifted to the written sentinel `-1` yields no result (issue 079, `constraints.js`), and an occupied slot must not answer 0 for selections it demonstrably holds.\n\nWHY THIS EXPRESSION WORKS, precisely:\n- `node.instance` is non-null exactly for real nodes. `evalTree.js` `attachInstance` (line ~87) is the only place that sets an instance, and it also sets `anchorKind: OCCUPIED`; `attachPhantom` (line 144), the offer anchor (line 186) and the group anchor (line 883) all set `instance: null`. So `node.instance?.count ?? 0` is exactly 'occupied real slot -> its own count, every synthetic anchor -> 0'. No `anchorKind` test is needed.\n- Category anchors keep their occupancy map: they precede in the `??` chain and carry no instance, so their branch is untouched (and a missing map still lands on 0).\n- `__fixtures__/rosParser.js:56` maps the `.ros` `number` attribute to `count`, so `instance.count` is the piece count the scenario's README derives its numbers from ('`current` am Slot ist die Stueckzahl der Selektion (`number`)'). Roster 02 -> 1, roster 03 (one selection, `number=\"2\"`) -> 2, roster 04 -> 1 per slot (deliberately unasserted). Roster 01 is untouched: its max limit still yields a result, so `current` still comes from `maxResult.actual` = 1 with `effectiveMax` 0 and `isBlocked` true.\n- The firing and silence of limit `c70d-c292-36ee-21b5` are decided in `constraints.js`, which this change does not touch at all.\n\nINTENDED SIDE EFFECT, do not suppress it: force nodes are real nodes, so a force slot's `current` goes from 0 to its instance count (normally 1). That is consistent with the rest of the engine — `countIndex.js:258` already books a force's instance count as `selectionCount` under its own def id, so a limit declared at a force type already reports exactly that number today. Keep the rule uniform; do not special-case `node.isForce`.\n\nNO OTHER FILE CHANGES. `constraints.js` (`evaluateLimit`, `categoryAnchorOccupancies`) and `evaluator.js` (which builds the occupancy map at line 322 and hands it to the report at line 358) stay as they are; the codemap's guess that they would need widening is wrong, and so is its 'possibly `countIndex.js`/`query.js`' — the number is already on the node and needs no query. Correct the codemap accordingly.\n\nBLAST RADIUS, measured by reading every assertion rather than assumed:\n- Only slots that reach the fallback change, i.e. real slots with no MIN and no MAX result. Every such slot reports 0 today.\n- No committed E2E scenario under `docs/testing/` asserts `current: 0` on an `occupied` slot: all 134 `current` assertions across the scenarios are either offer anchors (unoccupied, 0 either way), mandatory phantoms and category/group anchors (no instance), or occupied slots that carry a limit result (every occupied assertion with a non-zero `current` is limit-supplied today, since the fallback is 0 and those cases are green).\n- No unit test asserts `current: 0` on an occupied slot either: `report.test.js:190` is a mandatory phantom with a MIN result, `report.test.js:567` a category anchor, `offer.test.js:556` an offer anchor with a MIN result, `fixpoint.test.js:392` a slot with `effectiveMin: 1`. The two report-fingerprint tests (`evaluator.preparedDataset.test.js:75`, `resolver.immutability.test.js:197`) compare one run against another run, never against a literal. There is no snapshot assertion anywhere in `src/evaluator`.\n- Outside the evaluator, `capability.current` is read only by `RosterCategorySection.jsx:141` (category anchor), `RosterSidebar.jsx:39,46` (category anchor rows only) and `OptionGroup.jsx:119,131` (group anchor) — all synthetic anchors with no instance, so the app surface sees no change. `isSelectable`/`mandatoryOpenSlots` are test-local helpers in `report.test.js` and read `isHidden`/`isBlocked`/`isMandatoryUnmet`, not `current`.\n\nALTERNATIVES CONSIDERED AND REJECTED:\n- (C) Let `evaluateLimit` return a result for an UNLIMITED bound so the limit path keeps supplying the count with its original scope/field/flags. Rejected: `UNLIMITED` is `Infinity` (`model.js:472`), so `bound`, `delta`, `headroom` (`Math.max(0, Infinity - actual)`), `effectiveMax` and `isBlocked` would each need an explicit unlimited case, and a MIN result would compute `satisfied = actual >= Infinity` -> false and inject a spurious violation for every `min=\"-1\"`. It would also change the max-over-min precedence at any slot carrying both an unlimited max and a real min, i.e. move numbers that are green today. That is a rewrite of the sentinel contract for a two-roster gap, and it brushes against the criterion that no case of `constraints.unlimitedSentinel.test.js` be weakened.\n- (A) Generalise `categoryAnchorOccupancies` to every anchor with the same `scope=\"parent\"` / `shared` / `includeChildSelections` / `includeChildForces` query. Rejected: it makes every sibling slot of the same definition report the frame's sum (two Staffs under one Shaman would both report 2, where `parent-max-include-child-selections` roster 04 shows the intended reading is 1 each), it would flip an offer anchor that coexists with a selected instance of the same target away from 0, and it spends a query per node on a number the node already carries. The scenario's own derivation names the slot's piece count, not the frame sum.\n\nSCOPE FENCE: change nothing outside `src/evaluator/`; touch no file under `docs/testing/`. `docs/evaluator-architecture.md` SS4.8 documents the old derivation and is known, out-of-fence drift — leave it.",
                "moduleMap": "- `/home/user/tome_of_battle/src/evaluator/report.js` — the report projection. `toCapability` (line 414) builds one capability record per slot; line 441 is the `current` derivation chain that this increment extends, lines 366-372 and 437-440 the prose that states the rule. The only production file that changes.\n- `/home/user/tome_of_battle/src/evaluator/report.test.js` — the unit tests of the capability record (vitest, German titles, JSDOM `DOMParser` installed at the top, inline synthetic catalogues driven through `prepareDataset` + `evaluate`). Local helpers: `slotByDefId(report, defId, {phantom})` — note it returns the FIRST match, so a case with two sibling slots of one definition must filter `report.capabilities` itself; `isSelectable`, `remainingAllowed`, `mandatoryOpenSlots`. Where the new cases go.\n- `/home/user/tome_of_battle/src/evaluator/constraints.js` — read-only for this increment. `evaluateLimit` line 115 returns `null` for an UNLIMITED bound (this is why rosters 02/03 already have `effectiveMax` null, `headroom` null, `isBlocked` false); `categoryAnchorOccupancies` line 238 counts frame occupancy for category anchors only. Neither changes.\n- `/home/user/tome_of_battle/src/evaluator/evaluator.js` — read-only. Builds the occupancy map (line 322) and passes it to `buildReport` (line 358). No new wiring is needed, because the count comes off the node.\n- `/home/user/tome_of_battle/src/evaluator/evalTree.js` — read-only, the node shape. `attachInstance` (~line 87) is the only producer of `node.instance` and sets `anchorKind: OCCUPIED`; `attachPhantom` (line 144), the offer anchor (line 186) and the group anchor (line 883) set `instance: null`. `selectableSlotsOf` (line 1091) = `allNodes`, which excludes the synthetic root but includes force nodes.\n- `/home/user/tome_of_battle/src/evaluator/countIndex.js` — read-only. Line 258 books a force node's instance count as `selectionCount` under its own def id; that is the precedent making a force slot's `current` = its instance count consistent. The codemap's 'possibly countIndex/query' guess is measured out: neither is involved.\n- `/home/user/tome_of_battle/src/evaluator/model.js` — read-only. `UNLIMITED = Infinity` (line 472), `unlimitedFromSentinel` (line 486), `AnchorKind` (line 256).\n- `/home/user/tome_of_battle/src/evaluator/e2e.testcatalog.test.js` — read-only, the manifest-driven runner. Its capability selector (lines 405-435) matches on `defId`/`targetDefId`/`path`/`anchorKind`/`frameDefId` and fails loudly when a selector is ambiguous; it asserts only the fields the manifest names.\n- `/home/user/tome_of_battle/src/evaluator/__fixtures__/rosParser.js` — read-only. Line 56 maps the `.ros` `number` attribute to the instance `count` (default 1).\n- `/home/user/tome_of_battle/docs/testing/greater-than-force-unlimited-gate/` — the specification (`scenario.json`, `README.md`, four `.ros`). Read-only by the issue's fence.\n- `/home/user/tome_of_battle/src/evaluator/CLAUDE.md` — the suite doc for the evaluator tests; current and consistent with what `report.test.js` actually does.",
                "environment": "Repository root `/home/user/tome_of_battle`, branch `claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count` (created, pushed with `-u`, and already carrying the backlog `branch` record). Node v22.22.2, vitest 3.2.7, `node_modules` present — no install step needed.\n\nCommands this plan asks anyone to run, all from the repository root:\n- `npx vitest run src/evaluator` — the whole evaluator suite: the unit and layer tests plus the manifest-driven E2E runner over `docs/testing/`. This is the issue's decided check command and the only entry of the done-list.\n- `npx vitest run src/evaluator/report.test.js` — the single file the new cases live in, for the test-author's and implementer's own loop.\n\nNothing else is asked for: no linter, no typecheck, no `npm test`, no screenshot (this increment changes no UI-visible surface).",
                "testPlan": "WHETHER: tests are needed. The two acceptance rosters are already covered by the committed E2E scenario (which is red today and must not be touched), so the new work is unit coverage in `report.test.js` that pins the rule the fix introduces and the rule it must leave alone.\n\nWHAT — six cases, all in `/home/user/tome_of_battle/src/evaluator/report.test.js`, all driven through the public facade (`prepareDataset` + `evaluate`) against inline synthetic catalogues. Cases 1, 2 and 5 share one skeleton, so build them from one per-describe catalogue factory (suite doc: factory from three cases on).\n\n1. The rule (criterion 1 in miniature). Catalogue: entry `warrior` with `<constraint id=\"max-warriors\" type=\"max\" value=\"1\" field=\"selections\" scope=\"roster\"/>` and one `<modifier type=\"set\" field=\"max-warriors\" value=\"-1\">` gated on `<condition type=\"atLeast\" field=\"selections\" scope=\"roster\" childId=\"entry-archer\" value=\"1\"/>`; plus a bare `archer` entry. Roster: warrior with count 2 and one archer. Expect on the warrior slot: `current` 2, `effectiveMax` null, `headroom` null, `isBlocked` false, `isMandatoryUnmet` false, `anchorKind` occupied. This is the unit analogue of roster 03 — the lifted cap must not cost the slot its count.\n\n2. `KONTROLLE:` gate closed, limit-supplied count unchanged. Same catalogue, roster: warrior with count 2 and no archer. Expect `current` 2, `effectiveMax` 1, `isBlocked` true. Pins that the MAX result still supplies `current` where it exists (the unit analogue of roster 01). Expected green before and after the fix.\n\n3. Occupied slot with no limit at all. Catalogue: one entry with no constraints and no modifiers. Roster: that entry with count 3. Expect `current` 3. Pins the generalised rule beyond the unlimited case.\n\n4. Own count, not the frame's sum. Same constraint-free catalogue, roster: the same entry twice in one force, counts 1 and 2. Expect two occupied slots reporting 1 and 2 — not 3 and 3. Do not use `slotByDefId` here (it returns the first match); filter `report.capabilities` for the occupied records of that `defId` and compare the multiset of their `current` values. This is the case that pins the rejected frame-sum reading out.\n\n5. `KONTROLLE:` synthetic anchors stay 0. Same constraint-free catalogue, roster: the entry NOT selected (an empty force). Expect the offer-anchor slot for it to report `current` 0. Pins that no instance means no count. Expected green before and after the fix.\n\n6. Force slot. A force with `count: 1` and no limits on its definition: expect its slot to report `current` 1. This is the intended consequence of the uniform rule (see the plan) and is red before the fix; pin it rather than let it surface as a surprise.\n\nLeft untested on purpose: the four `.ros` rosters of `greater-than-force-unlimited-gate` themselves (the committed manifest runner already asserts them, and `docs/testing/` is out of scope); the category-anchor occupancy branch and the cost-measure limits that supply `current` (unchanged code paths, already pinned by the category block of `report.test.js` and by `countIndex.costSumCarrierFrame.test.js`/`countIndex.costSumUnderCarrier.test.js`); MIN-supplied `current` (unchanged, pinned at `report.test.js:146,162` and `fixpoint.test.js:392`).\n\nHOW: unit level, vitest, file `/home/user/tome_of_battle/src/evaluator/report.test.js`; run just that file with `npx vitest run src/evaluator/report.test.js`. The file's conventions — JSDOM `DOMParser` at module top, inline synthetic catalogues, German `describe`/`it` titles, `KONTROLLE:` for regression pins, English for new code comments — are the suite doc's (`/home/user/tome_of_battle/src/evaluator/CLAUDE.md`); I read that doc and it matches what `report.test.js` actually does, so it needs no correction.\n\nWHAT COUNTS AS DONE — closed list, run from the repository root, nothing else:\n- `npx vitest run src/evaluator`\n\nWHAT IS ALREADY RED: I ran nothing, not even a baseline. Per the increment brief the only red cases in the whole evaluator suite today are `Szenario: greater-than-force-unlimited-gate > rosters/02-tyrant-one-slaughtermaster-unlimited.ros` and `> rosters/03-tyrant-two-slaughtermasters-unlimited.ros`, both failing on `current` alone. Of the six new cases, 1, 3, 4 and 6 will be red until `report.js` line 441 is changed; 2 and 5 must be green the moment they are written.",
                "checks": [
                  "npx vitest run src/evaluator"
                ],
                "questions": [],
                "summary": "The occupied Slaughtermaster slot loses its count because an unlimited max yields no limit result and `report.js`'s `current` chain then falls to the literal 0; the fix is one line — let the chain fall back to the slot's own instance count (`node.instance?.count ?? 0`), which is non-null only for real occupied slots, so every synthetic anchor and every limit-supplied count stays exactly as it is. Six unit cases in `report.test.js` pin the new fallback and the untouched limit path; the closed check is `npx vitest run src/evaluator`."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on its own branch: `claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count`, off `claude/evaluator-abschluss-kampagne-tk44u4`.\nBefore anything else, on `claude/evaluator-abschluss-kampagne-tk44u4`: record the name with the backlog helper's `branch` subcommand, as `branch docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count`, commit that state change and push `claude/evaluator-abschluss-kampagne-tk44u4`. Then create the branch with `git checkout -b claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count`, push it with `-u`, and work on it from there.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 2 is yours:\n  An occupied slot keeps its count when its max bound is lifted to unlimited — Make `docs/testing/greater-than-force-unlimited-gate` green: the occupied Slaughtermaster slot must keep reporting how many selections its frame holds after the `set -1` modifier has opened its only max limit. This is criterion 1 of the issue, and the only work left in it. Today rosters 02 and 03 are the sole red cases of the whole evaluator suite, failing as `Slot 0ff3-ec4d-1c6b-6d53: current: expected +0 to deeply equal 1` resp. `... to deeply equal 2` - measured identically before and after the landed increment, which touched no limit or sentinel code.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: greater-than-force-unlimited-gate > rosters/02-tyrant-one-slaughtermaster-unlimited.ros` and `> rosters/03-tyrant-two-slaughtermasters-unlimited.ros`.\n  - In those two rosters the slot defId `0ff3-ec4d-1c6b-6d53` in frame `729f-9246-5cd3-5044` reports `current` 1 resp. 2, with `effectiveMax` null, `headroom` null and `isBlocked` false.\n  - Roster `01-no-tyrant-slaughtermaster-max0.ros` keeps reporting `current` 1 with `effectiveMax` 0 and `isBlocked` true, and the firing and the silence of limit `c70d-c292-36ee-21b5` are unchanged in all four rosters.\n  - `npx vitest run src/evaluator` ends at exit 0 with no failure left at all - the scenarios the earlier rounds closed, the unlimited-sentinel unit tests, and the `at-least-parent-any-reveal` scenario the landed increment fixed, all included.\n  - The count a slot reports keeps meaning what it means today wherever a limit result still supplies it: no case of `report.test.js`, `constraints.test.js` or `constraints.unlimitedSentinel.test.js` is weakened to make the two rosters pass.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/0147-evaluator-coverage-gaps/backlog.json`, before you research.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count research:unlimited-slot-count.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count research:unlimited-slot-count.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "tests:unlimited-slot-count.0",
              "at": "2026-08-10T12:31:37.717Z",
              "return": {
                "cases": [
                  {
                    "case": "1. The rule (criterion 1 in miniature): warrior with max-warriors=1 (scope roster) plus a conditional `set max-warriors=-1` gated on atLeast 1 archer; roster warrior count 2 + one archer. Expect on the warrior slot: current 2, effectiveMax null, headroom null, isBlocked false, isMandatoryUnmet false, anchorKind occupied.",
                    "file": "src/evaluator/report.test.js",
                    "testName": "Bericht: ein belegter Slot behaelt seinen Stand, wenn sein einziges Max per set -1 zu unbegrenzt wird > haelt den Stand eines belegten Slots, nachdem set -1 sein einziges Max zu unbegrenzt gehoben hat",
                    "demands": "The warrior slot keeps reporting its own count (2) once the only max bound on it is lifted to unlimited by a conditional `set -1`, with effectiveMax/headroom both null and the slot neither blocked nor mandatory-unmet.",
                    "got": "AssertionError: expected { defId: 'entry-warrior', ...(20) } to match object { current, effectiveMax, ... } -- Expected current: 2, Received current: 0 (all other fields already matched: effectiveMax null, headroom null, isBlocked false, isMandatoryUnmet false, anchorKind 'occupied')."
                  },
                  {
                    "case": "2. KONTROLLE: gate closed, limit-supplied count unchanged. Same catalogue, roster warrior count 2, no archer. Expect current 2, effectiveMax 1, isBlocked true. Expected green before and after the fix.",
                    "file": "src/evaluator/report.test.js",
                    "testName": "Bericht: ein belegter Slot behaelt seinen Stand, wenn sein einziges Max per set -1 zu unbegrenzt wird > KONTROLLE: bei geschlossenem Tor liefert das Grenzergebnis den Stand weiterhin (Analogie Roster 01)",
                    "demands": "With the conditional gate closed (no archer), the MAX result still supplies current (2) unchanged while effectiveMax stays 1 and the slot is blocked.",
                    "got": "Passes today, as the plan requires: run confirmed green (no failure) alongside the other 29 cases in the file."
                  },
                  {
                    "case": "3. Occupied slot with no limit at all. Catalogue: one entry with no constraints/modifiers. Roster: that entry with count 3. Expect current 3.",
                    "file": "src/evaluator/report.test.js",
                    "testName": "Bericht: der Stand eines Slots ohne jede Grenze > meldet an einem belegten Slot ohne jede Grenze trotzdem seinen Stand",
                    "demands": "An occupied slot with no limit result behind it at all still reports its own count (3), not 0.",
                    "got": "AssertionError: expected +0 to be 3 // Object.is equality."
                  },
                  {
                    "case": "4. Own count, not the frame's sum. Same constraint-free catalogue, roster: the same entry twice in one force, counts 1 and 2. Expect two occupied slots reporting 1 and 2, not 3 and 3 -- checked via the multiset of report.capabilities occupied records for that defId, not slotByDefId.",
                    "file": "src/evaluator/report.test.js",
                    "testName": "Bericht: der Stand eines Slots ohne jede Grenze > GEGENPROBE: jede Instanz meldet ihren eigenen Stand, nicht die Summe des Rahmens",
                    "demands": "Each of two occupied instances of the same definition reports its own count (1 and 2 respectively), which rules out a frame-sum reading (3 and 3).",
                    "got": "AssertionError: expected [ +0, +0 ] to deeply equal [ 1, 2 ]."
                  },
                  {
                    "case": "5. KONTROLLE: synthetic anchors stay 0. Same constraint-free catalogue, roster: the entry NOT selected (an empty force). Expect the offer-anchor slot for it to report current 0. Expected green before and after the fix.",
                    "file": "src/evaluator/report.test.js",
                    "testName": "Bericht: der Stand eines Slots ohne jede Grenze > KONTROLLE: der Angebots-Anker einer nicht gewaehlten Instanz meldet Stand 0",
                    "demands": "A synthetic offer-anchor slot for an unselected instance keeps reporting current 0 -- no instance means no count, regardless of the fix.",
                    "got": "Passes today, as the plan requires: run confirmed green (no failure) alongside the other 29 cases in the file."
                  },
                  {
                    "case": "6. Force slot. A force with count: 1 and no limits on its definition: expect its slot to report current 1. Red before the fix; pinned rather than left as a surprise.",
                    "file": "src/evaluator/report.test.js",
                    "testName": "Bericht: Faehigkeitsdatensatz eines Kontingent-Slots ohne jede Grenze > meldet an seinem Kontingent-Slot dessen Stand, obwohl das Kontingent keine eigene Grenze traegt",
                    "demands": "A force-level slot (a contingent itself, not a unit under it) with no limit on its own definition reports its own count (1), the same uniform rule applied at the force level.",
                    "got": "AssertionError: expected +0 to be 1 // Object.is equality."
                  }
                ],
                "openQuestions": [
                  "The test plan's overview ('WHAT') states cases 1, 2 and 5 share one catalogue skeleton, but the per-case detail for case 5 says 'Same constraint-free catalogue' -- i.e. the catalogue of cases 3 and 4, not the one built for cases 1 and 2 (which needs an archer entry and a conditional set -1 modifier that case 5 has no use for). I followed the per-case detail, which is concrete and internally consistent with cases 3/4's own text: case 5 shares its catalogue and describe block with 3 and 4 (three cases, matching the suite doc's 'factory from three cases on' rule), and cases 1/2 share a separate, smaller one (two cases, no factory needed). The overview sentence is stale relative to its own per-case detail; a future round should correct or drop it.",
                  "Case 5 and case 6 both need a slot to exist at all for an unselected/force-only instance to be observable. Neither the overview nor the per-case text for 3/4/5 mentions a forceEntry, but the offer-anchor mechanism (attachOfferAnchors, confirmed against crossCatalog.rootEntryScope.test.js and offer.test.js) only attaches a root-level entry's offer anchor as a child of an existing force node in the tree -- an entry cannot be offered under a wholly empty roster ({ forces: [] }) with no force present at all. I added a bare <forceEntry> to the case 3/4/5 catalogue (unused by cases 3/4, which reference the constraint-free entry directly as a top-level roster force the way the rest of this file already does) and a second, dedicated one-entry catalogue for case 6's own force slot. This is a detail the plan left open, decided in the suite's existing style rather than guessed at the level of an expected value."
                ],
                "questions": [],
                "summary": "Added six unit cases to src/evaluator/report.test.js pinning that an occupied or force-level slot keeps reporting its own count when no limit supplies one (raw or via a set -1 unlimited gate) and that a synthetic anchor for an unselected instance still reports 0; ran `npx vitest run src/evaluator/report.test.js` (30 cases, exit 1) and confirmed the four planned-red cases (1, 3, 4, 6) fail on the bare count (`current: expected +0 to be N`), while the two planned-green KONTROLLE cases (2, 5) already pass."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 2 is yours:\n  An occupied slot keeps its count when its max bound is lifted to unlimited — Make `docs/testing/greater-than-force-unlimited-gate` green: the occupied Slaughtermaster slot must keep reporting how many selections its frame holds after the `set -1` modifier has opened its only max limit. This is criterion 1 of the issue, and the only work left in it. Today rosters 02 and 03 are the sole red cases of the whole evaluator suite, failing as `Slot 0ff3-ec4d-1c6b-6d53: current: expected +0 to deeply equal 1` resp. `... to deeply equal 2` - measured identically before and after the landed increment, which touched no limit or sentinel code.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: greater-than-force-unlimited-gate > rosters/02-tyrant-one-slaughtermaster-unlimited.ros` and `> rosters/03-tyrant-two-slaughtermasters-unlimited.ros`.\n  - In those two rosters the slot defId `0ff3-ec4d-1c6b-6d53` in frame `729f-9246-5cd3-5044` reports `current` 1 resp. 2, with `effectiveMax` null, `headroom` null and `isBlocked` false.\n  - Roster `01-no-tyrant-slaughtermaster-max0.ros` keeps reporting `current` 1 with `effectiveMax` 0 and `isBlocked` true, and the firing and the silence of limit `c70d-c292-36ee-21b5` are unchanged in all four rosters.\n  - `npx vitest run src/evaluator` ends at exit 0 with no failure left at all - the scenarios the earlier rounds closed, the unlimited-sentinel unit tests, and the `at-least-parent-any-reveal` scenario the landed increment fixed, all included.\n  - The count a slot reports keeps meaning what it means today wherever a limit result still supplies it: no case of `report.test.js`, `constraints.test.js` or `constraints.unlimitedSentinel.test.js` is weakened to make the two rosters pass.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count research:unlimited-slot-count.0 --fields testPlan` — the test plan.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count tests:unlimited-slot-count.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count tests:unlimited-slot-count.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "implement:unlimited-slot-count.0",
              "at": "2026-08-10T12:35:41.135Z",
              "return": {
                "deviations": [
                  "Language of the extended inline comment. The plan said the new wording goes 'in the file's existing German for that block (new explanatory comments in English per the suite doc)' for both the JSDoc block above `toCapability` and the inline comment above the `current` line. I wrote both in German: each is an extension of an existing contiguous German comment, and switching language mid-block would leave one comment in two languages. Nothing else about the wording departs from the plan; if the reviewer reads the parenthetical as binding for the inline comment, translating that one block is a one-file, no-behaviour change."
                ],
                "commands": [
                  {
                    "command": "npx vitest run src/evaluator/report.test.js",
                    "when": "before the change, to confirm the planned tests fail for the right reason",
                    "exit": 1,
                    "note": "30 cases, 4 failed / 26 passed. The four reds are exactly the test-author's cases 1, 3, 4 and 6, each failing only on `current` (expected 2 / 3 / [1,2] / 1, received 0 / 0 / [0,0] / 0); every other asserted field of case 1 (effectiveMax null, headroom null, isBlocked false, isMandatoryUnmet false, anchorKind 'occupied') already matched. The two KONTROLLE cases (2 and 5) were green before the change, as the test plan required."
                  },
                  {
                    "command": "npx vitest run src/evaluator",
                    "when": "after the change — the one command that counts for this increment",
                    "exit": 0,
                    "note": "85 test files, 1258 cases, all passed, nothing skipped or excluded. This covers the evaluator unit and layer tests plus the manifest-driven E2E runner over docs/testing/ (e2e.testcatalog.test.js, crossCatalog.test.js). Exit code captured on a separate clean run of the same command."
                  }
                ],
                "blockers": [],
                "questions": [],
                "summary": "An occupied slot now reports the number of selections it actually holds even when its only max limit was lifted to unlimited by the `-1` sentinel: `toCapability` in src/evaluator/report.js falls back to the node's own instance count after the category-anchor occupancy map, so synthetic anchors (which carry no instance) stay at 0 and the rule holds uniformly up to the force slot. The whole evaluator suite is green at 1258 cases."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count research:unlimited-slot-count.0 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count tests:unlimited-slot-count.0 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count implement:unlimited-slot-count.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count implement:unlimited-slot-count.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "review:unlimited-slot-count.0",
              "at": "2026-08-10T12:40:34.370Z",
              "return": {
                "findings": [],
                "findingCount": 0,
                "allDirect": true,
                "reason": "",
                "questions": [],
                "summary": "`npx vitest run src/evaluator` is green (85 test files, 1258 tests, no failures, no skips at file level; the targeted re-run of the scenario exited 0), and the two formerly red cases `greater-than-force-unlimited-gate > rosters/02-tyrant-one-slaughtermaster-unlimited.ros` and `> rosters/03-tyrant-two-slaughtermasters-unlimited.ros` now pass, as do rosters 01 and 04 with the limit's firing and silence unchanged. The change is one fallback step in the reported count of an evaluator slot plus its doc comment, and five added unit cases; nothing outside `src/evaluator/` and its unit tests is touched, no docs/testing file is touched, and no existing test case was removed or weakened. Observation for the pull request: with a limit result the reported count is that limit's frame-wide actual, while the new fallback reports the slot's own piece count, so the two can differ where one frame holds the same definition in two separate selections (the shape of roster 04, which asserts no slot) - the new counter-check test pins that choice deliberately and no criterion demands otherwise."
              },
              "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nThis increment is worked on branch `claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count`. Before anything else make sure you are on it: `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count` and check it out; if the remote does not have it, create it from `claude/evaluator-abschluss-kampagne-tk44u4` and push it with `-u`.\nReview round 0 of increment 2. The increment's diff is its branch against the merge-base with `claude/evaluator-abschluss-kampagne-tk44u4`: judge `git diff claude/evaluator-abschluss-kampagne-tk44u4...HEAD` (three dots), whole, and nothing outside it.\nThis run works docs/issues/0147-evaluator-coverage-gaps/issue.md one increment at a time, and increment 2 is yours:\n  An occupied slot keeps its count when its max bound is lifted to unlimited — Make `docs/testing/greater-than-force-unlimited-gate` green: the occupied Slaughtermaster slot must keep reporting how many selections its frame holds after the `set -1` modifier has opened its only max limit. This is criterion 1 of the issue, and the only work left in it. Today rosters 02 and 03 are the sole red cases of the whole evaluator suite, failing as `Slot 0ff3-ec4d-1c6b-6d53: current: expected +0 to deeply equal 1` resp. `... to deeply equal 2` - measured identically before and after the landed increment, which touched no limit or sentinel code.\nWhat it has to satisfy:\n  - `npx vitest run src/evaluator` passes the cases `Szenario: greater-than-force-unlimited-gate > rosters/02-tyrant-one-slaughtermaster-unlimited.ros` and `> rosters/03-tyrant-two-slaughtermasters-unlimited.ros`.\n  - In those two rosters the slot defId `0ff3-ec4d-1c6b-6d53` in frame `729f-9246-5cd3-5044` reports `current` 1 resp. 2, with `effectiveMax` null, `headroom` null and `isBlocked` false.\n  - Roster `01-no-tyrant-slaughtermaster-max0.ros` keeps reporting `current` 1 with `effectiveMax` 0 and `isBlocked` true, and the firing and the silence of limit `c70d-c292-36ee-21b5` are unchanged in all four rosters.\n  - `npx vitest run src/evaluator` ends at exit 0 with no failure left at all - the scenarios the earlier rounds closed, the unlimited-sentinel unit tests, and the `at-least-parent-any-reveal` scenario the landed increment fixed, all included.\n  - The count a slot reports keeps meaning what it means today wherever a limit result still supplies it: no case of `report.test.js`, `constraints.test.js` or `constraints.unlimitedSentinel.test.js` is weakened to make the two rosters pass.\n  - Nothing outside `src/evaluator/` and its unit tests changes; no file under `docs/testing/` is touched.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\nRead nothing out of docs/issues/0147-evaluator-coverage-gaps/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count review:unlimited-slot-count.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count review:unlimited-slot-count.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `questions`\n  - `summary`\n"
            }
          ]
        }
      ]
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-10T10:39:32.592Z",
        "return": {
          "summary": "Round 6 of issue 0147 is cut into two increments, one per acceptance criterion: `parent-any-offer-anchor` delivers criterion 2 (the missing offer anchor for the unselected, four-groups-deep `Wolf Lord` option in `docs/testing/at-least-parent-any-reveal`) and `unlimited-slot-count` delivers criterion 1 (an occupied slot losing its `current` once its max limit is lifted to the written `-1` sentinel in `docs/testing/greater-than-force-unlimited-gate`). The offer-anchor increment runs first because the offer layer projects the anchors of every scenario in the suite, so a widening there is the round's load-bearing risk and has to be learned early; the sentinel count is the narrower change and sits downstream of the same `report.js` `current` derivation an earlier round already widened for category anchors. The two are not merged into one increment: they touch different layers (offer projection and visibility vs. constraint results and the count fallback), each is separately reviewable, and each leaves the suite green on its own. Both criteria state the observation to reach but not the cause - the issue itself defers the diagnosis of the missing projection to this round - so each increment's researcher answers that when its turn comes; no code fact had to be settled to decide the cut. The codemap is written into the backlog and names eight evaluator modules and unit files for the offer increment and five for the count increment, plus the read-only scenario data, the manifest runner and the fixture corpus, and it records that `docs/evaluator-architecture.md` will drift further while the issue's own out-of-scope rule bars touching it.",
          "questions": []
        },
        "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nOpen the run state for docs/issues/0147-evaluator-coverage-gaps/issue.md: map the issue against the codebase, decide the cut, and write both into docs/issues/0147-evaluator-coverage-gaps/backlog.json with the `init` subcommand of the backlog helper your shared brief names, with workflow \"agile-loop\" and the whole codemap in the payload's `codemap` field.\nThis run works at most 8 increments, so a cut that needs more than that is a cut that is too fine.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json - decompose <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json - decompose <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `summary`\n"
      },
      {
        "label": "replan:parent-any-offer-anchor",
        "at": "2026-08-10T12:08:22.532Z",
        "return": {
          "questions": [],
          "summary": "Landed the accepted increment parent-any-offer-anchor (fast-forward merge of its branch into claude/evaluator-abschluss-kampagne-tk44u4, pushed as b93abbb) and closed it done; the backlog now holds one open increment, unlimited-slot-count, unchanged in scope but sharpened to the two rosters that are the evaluator suite's only remaining red cases, and the codemap records what the landed increment really touched (countIndex.js and query.js, which the map had missed; evalTree.js, fixpoint.js and report.js, which it named for nothing)."
        },
        "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nIncrement parent-any-offer-anchor — An unselected deep option appears as an offer anchor, so its reveal is observable — has been worked. The review accepted it.\nLand it first: check out `claude/evaluator-abschluss-kampagne-tk44u4`, run `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--parent-any-offer-anchor`, merge that branch and push `claude/evaluator-abschluss-kampagne-tk44u4`.\nWhat this increment turned up is what you re-cut against, and it is in the run state. Read it with the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json parent-any-offer-anchor` — every step this increment recorded, whole.\n  - `index docs/issues/0147-evaluator-coverage-gaps/backlog.json` — the rest of the cut, with no step content in it.\n  - `codemap docs/issues/0147-evaluator-coverage-gaps/backlog.json` — the map as it stands.\nThen close that increment with `close` and re-cut what is still open with `init`, the whole codemap in its `codemap` field. 1 of at most 8 increments are spent.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json - replan:parent-any-offer-anchor <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json - replan:parent-any-offer-anchor <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `summary`\n"
      },
      {
        "label": "replan:unlimited-slot-count",
        "at": "2026-08-10T12:43:56.799Z",
        "return": {
          "questions": [],
          "summary": "The unlimited-slot-count increment landed (fast-forward merge of its branch into claude/evaluator-abschluss-kampagne-tk44u4, pushed as 72f4486) and is closed as done with 0 review findings; with it both of the issue's acceptance criteria are delivered, so the re-cut opens no new increment and the round-6 backlog is two increments, both done. The codemap is rewritten to measured fact: the second increment touched only src/evaluator/report.js and report.test.js, and constraints.js, evaluator.js, countIndex.js and query.js are dropped from it as proved untouched."
        },
        "prompt": "Issue directory: docs/issues/0147-evaluator-coverage-gaps\nIncrement unlimited-slot-count — An occupied slot keeps its count when its max bound is lifted to unlimited — has been worked. The review accepted it.\nLand it first: check out `claude/evaluator-abschluss-kampagne-tk44u4`, run `git fetch origin claude/evaluator-abschluss-kampagne-tk44u4--unlimited-slot-count`, merge that branch and push `claude/evaluator-abschluss-kampagne-tk44u4`.\nWhat this increment turned up is what you re-cut against, and it is in the run state. Read it with the backlog helper your shared brief names:\n  - `steps docs/issues/0147-evaluator-coverage-gaps/backlog.json unlimited-slot-count` — every step this increment recorded, whole.\n  - `index docs/issues/0147-evaluator-coverage-gaps/backlog.json` — the rest of the cut, with no step content in it.\n  - `codemap docs/issues/0147-evaluator-coverage-gaps/backlog.json` — the map as it stands.\nThen close that increment with `close` and re-cut what is still open with `init`, the whole codemap in its `codemap` field. 2 of at most 8 increments are spent.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/0147-evaluator-coverage-gaps/backlog.json - replan:unlimited-slot-count <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/0147-evaluator-coverage-gaps/backlog.json - replan:unlimited-slot-count <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `summary`\n"
      }
    ],
    "attempts": []
  }
}
