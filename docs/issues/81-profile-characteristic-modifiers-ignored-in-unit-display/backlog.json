{
  "version": 1,
  "issue": "",
  "workflow": "",
  "codemap": "# Codemap - issue 81 (profile characteristic modifiers ignored in unit display)\n\nOne line per file the issue has to change or read as evidence. Reasons only; what changes inside a file is the researcher's call. Revised after increment 1, which proved the entry-id scope path correct end to end and narrowed the remaining work to condition scope resolution.\n\n## The scope sweep (increment 2)\n\n- `src/evaluator/query.js` - Resolves a condition's scope to a frame node (roster, force, parent, self, unit, ancestor, primary-catalogue, entry id, category id) and answers the count the condition compares against; it is where a scope can be left unresolved, and after increment 1 the only place a gated characteristic modifier can still be silently dropped.\n- `src/evaluator/model.js` - Declares the ScopeKeyword and ScopeKind enumerations the closed scope list has to be reconciled against.\n- `src/evaluator/modifiers.js` - Applies modifiers per node and per info carrier and gates them on their conditions and condition groups; the caller of the scope resolution above.\n- `src/evaluator/catalogReader.js` - Reads modifiers, modifierGroups and their conditions out of the XML, including the scope attribute the closed list is about.\n- `src/evaluator/countIndex.js` - Books nodes under their effective categories per frame; a scope resolves to a frame here, so a scope that resolves but counts nothing fails the same way as one that does not resolve.\n- `src/evaluator/fixpoint.js` - Rebuilds the count index per round, which is what lets a condition gated on a dynamically added category hold at all; relevant to any scope whose gate depends on another modifier.\n- `src/evaluator/query.unitScope.test.js` - Existing unit tests for scope=\"unit\" resolution; the sweep extends or is measured against them.\n- `src/evaluator/query.selfGatedInstanceOf.test.js` - Existing tests for instanceOf conditions gated on a self/entry-id scope, the shape increment 1 proved correct.\n- `src/evaluator/modifiers.test.js` - Unit tests of the modifier layer including characteristic targets; a plausible home for gated-modifier cases per scope.\n- `docs/battlescribe-data-format.md` - The canonical source for the closed list of scope values (7.6/7.7 and the scope boxes); it is corrected here if the list it documents proves incomplete.\n- `src/parser/schema/Catalogue.xsd` - The upstream schema the closed scope list is derived against; read-only evidence.\n- `docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md` - Holds the record of which scope values cannot be covered from the catalogue data in the repository with the reason for each, and the record of what increment 1 established.\n- `docs/testing/modifier-characteristic-value/README.md` - Declares instanceOf with scope=\"unit\" on characteristic fields and non-numeric set values as deliberately uncovered by its E2E scenarios; read to see whether that statement still holds, but scenario authorship in this tree belongs to the e2e-testcase-author (ADR 0033).\n- `src/components/editor/CLAUDE.md` - Suite doc written in increment 1; its guard-assert sentence is wider than the tests in that directory actually are.\n- `package.json` - Version bump for a user-visible fix before the issue is merged, per the project's release rule; only if increment 2 lands one.\n\n## Landed in increment 1 (kept as the regression pins this issue is judged by)\n\n- `src/evaluator/evaluator.bloodlineProfileFixture.test.js` - Facade-level pin on the frozen fixture: Blood Dragon, Strigoi's non-numeric '5+', Necrarch's decrement, the no-bloodline control, and the separation of profile name from unit name.\n- `src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx` - Component-level pin fed by a real evaluator report; the path the user actually sees.\n\n## Read as evidence, not changed\n\n- `src/evaluator/__fixtures__/whfb6-definitive/Vampire Counts (6th definitive edition).cat` and the accompanying .gst - Frozen fixture carrying the reported case (selectionEntry 6822-0110-a7c9-cbb0, infoLink a106-4a05-36ea-cb01, shared profile fabd-ef67-72f5-6b3f); read-only, never edited.\n- `src/evaluator/effectiveState.js`, `src/evaluator/infoProjection.js`, `src/evaluator/report.js` - The write/read/assemble chain for characteristic values; increment 1 measured it correct for gated modifiers, so it is background unless the sweep finds a scope whose value is computed but not projected.\n- `src/components/editor/UnitSelectionCard.jsx` - Renders the profile table from capability.infoElements verbatim; proved correct in increment 1.\n- `src/components/play/PlayUnitDetails.jsx` and its evaluator test - Read the identical report field through the identical helper; proved to need no separate change.\n- `src/evaluator/CLAUDE.md` - Suite doc governing the evaluator tests.\n\n## Dropped from the map\n\n- `src/__fixtures__/whfb6-lexicanum/vampire-selfscope-bloodline.cat.xml` - Increment 1 showed the frozen definitive fixture carries the case in full; the reduced excerpt is not needed.\n- `src/evaluator/infoElements.test.js`, `src/evaluator/infoProjection.test.js` - The rule under test spans the whole chain and belongs at the facade, so neither file is a home for this issue's cases.\n",
  "increments": [
    {
      "id": "inc-1",
      "title": "Gated profile characteristic modifiers reach the unit profile display",
      "goal": "A characteristic modifier whose condition holds for a unit changes the value the user sees in the unit profile table, for numeric and non-numeric values alike, with the reported Vampire Counts bloodline case as the worked example.",
      "criteria": [
        "The unit profile display shows the modified characteristic value whenever a profile modifier's condition holds for that unit (issue AC 1).",
        "With the frozen whfb6-definitive Vampire Counts fixture, a Vampire Count in a force that has chosen the Blood Dragon bloodline reports WS as the catalogue base value plus 2, and reports the Sv, Sv+ and A values the same modifierGroup sets (issue AC 2).",
        "The same Vampire Count in a force without that bloodline still reports the unmodified base values (issue AC 3).",
        "A non-numeric characteristic value written by a gated set modifier, such as an armour save 5+, reaches the display unchanged in form (issue AC 4).",
        "The regression is pinned by a test that exercises the path the user actually sees - the evaluator report and the component fed by it - and not only the legacy profileCollector (issue AC 8).",
        "The evaluator unit tests and the evaluator E2E suite pass, and no existing test is weakened or skipped to make the new ones green."
      ],
      "depth": "full",
      "status": "done",
      "note": "Accepted with zero findings and no production diff. What it settled: for a condition whose scope is the unit's own selectionEntry id, the engine already applies gated characteristic modifiers correctly end to end - the report carries WS base+2, the non-numeric set values '4+' and '5+' arrive verbatim, and UnitSelectionCard renders them; the reviewer confirmed this through the app's own roster-construction and evaluation path, not only through a hand-built roster. Criteria 1-4 were therefore already satisfied and the deliverable was the regression pin (AC 8), now landed as src/evaluator/evaluator.bloodlineProfileFixture.test.js (5 cases) and src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx (2 cases), plus a new suite doc src/components/editor/CLAUDE.md. The reporter's symptom therefore does not live on the entry-id scope path, which makes the untested scope shapes of AC 5-7 the remaining candidate.",
      "branch": "claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1",
      "steps": [],
      "attempts": [
        {
          "closedAs": "done",
          "at": "2026-08-12T14:38:24.203Z",
          "steps": [
            {
              "label": "research:inc-1.0",
              "at": "2026-08-12T14:00:13.743Z",
              "return": {
                "needsTests": true,
                "plan": "GOAL OF THIS INCREMENT (issue 81, increment 1): a gated profile characteristic modifier must reach the unit profile table the user sees, numeric and non-numeric alike, with the Vampire Counts bloodline case as the worked example (issue AC 1-4, 8).\n\n=== MEASURED STATE OF THE CODE (read this first) ===\nI ran two throwaway node probes (not the test suite) through the public evaluator facade against the frozen fixture `src/evaluator/__fixtures__/whfb6-definitive/` (gst + `Vampire Counts (6th definitive edition).cat`), with a roster `force e989-15b8-7eb6-9668 (Standard (VC-AB))` holding the Vampire Count `6822-0110-a7c9-cbb0` and, as a sibling, `Bloodlines a56a-eb32-5a45-16fd` with one bloodline child. The report's occupied slot `0/0` carries `infoElements` with these characteristics (profile entry id `a106-4a05-36ea-cb01`, name 'Vampire Count'):\n\n| roster | WS | A | Sv | Sv+ | capability.name |\n| no bloodline | 7 | 4 | 7 | 7 | Vampire Count |\n| Blood Dragon 9fd9-e05c-ffcb-2c4d | 9 | 4 | 4+ | 7 | Vampire Count of Clan Blood Dragon |\n| Strigoi ddfa-0d72-8557-6906 | 7 | 5 | 7 | 5+ | Vampire Count of Clan Strigoi |\n| Necrarch 5017-296d-edef-4562 | 5 | 4 | 7 | 7 | Vampire Count of Clan Necrarch |\n\nSame result with the single catalogue and with all 11 definitive catalogues loaded. So the engine already applies the gated modifiers correctly today, the non-numeric `set` values ('4+', '5+') already arrive verbatim, and the display component renders `characteristic.value` verbatim (`UnitSelectionCard.jsx` `renderProfileCell` -> `{c.value}`; `PlayUnitDetails.jsx:111-113` reads the same field). The reported symptom does not reproduce on the path the issue names.\n\nCONSEQUENCE FOR THE IMPLEMENTER: expect NO production change in this increment. The deliverable is the regression pin (issue AC 8) that makes this behaviour a fact the suite defends, at both observation points the AC names: the evaluator report and the component fed by it. Write no production code unless a pin the test-author wrote comes out red; then fix the engine, do not weaken the test.\n\n=== WHY IT WORKS TODAY (the chain, so a red pin can be diagnosed without re-deriving it) ===\nThe catalogue shape (fixture, `selectionEntry 6822-0110-a7c9-cbb0`, offset ~210237):\n- `infoLink a106-4a05-36ea-cb01` -> shared profile `fabd-ef67-72f5-6b3f` carries ONE `modifierGroup type=\"and\"` with five members, each gated by `condition type=\"instanceOf\" field=\"selections\" scope=\"6822-0110-a7c9-cbb0\" childId=<bloodline category> shared=\"true\" includeChildSelections=\"true\"`:\n  * `set value=\"4+\" field=f1be-e66c-d5e1-673c (Sv)` gated on category `4cae-a20e-8374-b6cb` (Blood Dragon)\n  * `increment value=\"2\" field=f95b-da01-0578-3bdc (WS)` gated on `4cae` (Blood Dragon)\n  * `decrement value=\"2\" field=f95b (WS)` gated on `fc4b-a86d-5897-9e4c` (Necrarch)\n  * `set value=\"5+\" field=d4a9-0ed4-d041-e54b (Sv+)` gated on `bf30-4ff0-a4d8-3909` (Strigoi)\n  * `increment value=\"1\" field=6b9f-c8fe-8998-27e3 (A)` gated on `bf30` (Strigoi)\n  READ THIS CAREFULLY: under Blood Dragon only WS and Sv change; Sv+ and A are Strigoi-gated and keep their base values 7 and 4. Any expectation that Blood Dragon yields Sv+ 5+ or A 5 is a misreading of the data.\n- The unit's own `modifierGroups` (BLOODLINE bracket, five sibling groups) each carry `modifier type=\"add\" value=<bloodline category> field=\"category\"` plus an `append` on `name`, gated by `condition type=\"atLeast\" value=\"1\" field=\"selections\" scope=\"force\" childId=<bloodline upgrade id> shared=\"true\" includeChildSelections=\"true\"`. The category therefore exists only after the force-wide bloodline choice.\n\nThe engine chain, layer by layer:\n1. `fixpoint.js` `evaluateToFixpoint` rebuilds the count index from the effective state each round (MAX_FIXPOINT_ROUNDS = 5). Round 1 adds category `4cae` to the unit node; round 2's index sees it. This two-round dependency is what makes the gate work at all.\n2. `countIndex.js` `targetsOf` books a node under `effective.categoryIdsOf(node)`, so the unit counts under `4cae` in its own frame (BASE bucket).\n3. `query.js` `resolveFrame` -> `resolveSharedFrame` default branch -> `nearestAncestorWithDefId(node, '6822-0110-a7c9-cbb0')` resolves the entry-id scope to the unit node itself; `index.get(scopeKey(frame,'4cae'), includeChildSelections=true, false, true, includeFrameOwn = (frame===ctx.node))` returns 1; `COMPARATORS[instanceOf] = actual > 0` holds (the condition's `value=\"0\"` is correctly ignored for membership types).\n4. `modifiers.js` `applyCarrierModifiers` runs the infoLink's `modifierGroups` with `carrier = the infoLink`; `MODIFIER_HANDLERS[SET][CHARACTERISTIC]` is `textSetHandler` (deliberately a TEXT setter, which is why '4+'/'5+' survive), `INCREMENT`/`DECREMENT` are `numericHandler(..., asText)`.\n5. `effectiveState.js` writes the override under the key pair (node, carrier=infoLink); `characteristicEntriesOf(node, carrier)` reads the base list through `baseCharacteristicsOf`, which falls back to `carrier.resolved.characteristics` (the shared profile). Writer and reader use the same carrier - that pairing is the load-bearing part.\n6. `infoProjection.js` `buildProfileEntry` turns that into `{ typeId, name, value }` triples; `report.js` puts them on `capability.infoElements`.\n7. App side: `evaluationCache.evaluateAppRoster` -> `rosterAdapter.toEvaluatorRoster` (defId = `entryLinkId ?? selectionEntryId`; the Vampire Count has no entryLink anywhere in the catalogue, so its defId is the entry id and the entry-id scope resolves) -> `UnitSelectionCard.jsx:182-198` reads `capabilities.get(pathBySelectionId.get(selection.id)).infoElements`, filters `kind === 'profile'`, groups via `groupProfilesByType` (`src/roster/rulesEvaluator.js`, groups by `profileTypeName`, does not touch values) and renders `{c.value}` in `renderProfileCell`.\n\nIF A PIN COMES OUT RED, diagnose in this order: (a) does the report show the modified value at all (evaluator-level pin red too) or only the component (then the fault is in the component/adapter seam); (b) is category `4cae` on the unit's effective categories (fixpoint round 2 / `targetsOf`); (c) does the entry-id scope resolve (`nearestAncestorWithDefId`, would emit an `unresolvedScope` diagnostic); (d) do writer and reader agree on the carrier (`effectiveState` key pair).\n\n=== EXPLICITLY NOT IN THIS INCREMENT ===\n- The scope sweep (issue AC 5-7: `scope=\"unit\"`, the closed scope list from the format doc and the XSD, the uncoverable-scope record in issue.md) - a later increment.\n- `docs/testing/modifier-characteristic-value/README.md` and any `docs/testing/` scenario: E2E scenario authorship is delegated to the `e2e-testcase-author` agent (ADR 0033, root CLAUDE.md). Do not edit that tree here.\n- `SelectionConfigurator.jsx:96-104`, `upgradeDetails.jsx:62-93`, `UnitChips.jsx:41` - the issue declares these separate display paths out of scope.\n- The legacy `src/roster/profileCollector.js` and its tests - not touched, not deleted, not weakened.\n- No version bump in `package.json`: this increment adds tests only and changes nothing a user can see.\n- Cosmetic: the report's characteristic entries carry no `originalValue`, so `getModificationState` never marks a modified cell as changed and no tooltip breakdown appears. That is a display nicety outside every criterion of this increment - do not implement it, and do not assert on cell CSS classes.",
                "moduleMap": "FILES THIS INCREMENT CHANGES (both new, both tests):\n- `src/evaluator/evaluator.bloodlineProfileFixture.test.js` (NEW) - facade-level regression pin on the frozen whfb6-definitive fixture; naming follows the existing `evaluator.<topic>Fixture.test.js` files.\n- `src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx` (NEW) - component-level pin fed by a real evaluator report.\n\nFILES READ AS EVIDENCE (no change):\n- `src/evaluator/modifiers.js` - condition/modifier-group evaluation, per-carrier application; `MODIFIER_HANDLERS` maps SET+CHARACTERISTIC to `textSetHandler` (non-numeric values survive), INCREMENT/DECREMENT to `numericHandler(..., asText)`.\n- `src/evaluator/query.js` - `resolveFrame`/`resolveSharedFrame`/`nearestAncestorWithDefId` resolve the entry-id scope of the gate; `nearestAncestorInCategory` covers a category-id scope.\n- `src/evaluator/countIndex.js` - `targetsOf` books a node under its EFFECTIVE category ids; `FRAME_OWN`/`includeFrameOwn` govern the frame node's own share.\n- `src/evaluator/fixpoint.js` - rebuilds the index per round from the effective state; the dynamically added category becomes countable in round 2 (MAX_FIXPOINT_ROUNDS = 5).\n- `src/evaluator/effectiveState.js` - `writeCharacteristic`/`characteristicValue`/`characteristicEntriesOf` keyed by (node, carrier); base values fall back to the link target's characteristics.\n- `src/evaluator/infoProjection.js` - `buildProfileEntry` emits `{ typeId, name, value }` per characteristic.\n- `src/evaluator/report.js` - assembles `capability.infoElements`.\n- `src/evaluator/catalogReader.js` - `readEntryBase` gives an `infoLink` its own `modifiers` and `modifierGroups`, so the Vampire Count's gate is read at all.\n- `src/evaluation/rosterAdapter.js` / `src/evaluation/evaluationCache.js` - app roster -> facade seam; defId = `entryLinkId ?? selectionEntryId`.\n- `src/components/editor/UnitSelectionCard.jsx:182-198` - profile table from `capability.infoElements`; `src/components/play/PlayUnitDetails.jsx:111-113` reads the same field.\n- `src/roster/rulesEvaluator.js` `groupProfilesByType` - groups by `profileTypeName`, passes values through untouched.\n- `src/evaluator/__fixtures__/whfb6-definitive/*` - read-only evidence (Vampire Counts .cat + the .gst).\n- `src/components/editor/UnitSelectionCard.evaluator.test.jsx`, `src/components/editor/SelectionConfigurator.groupMembership.test.jsx` - the two existing patterns the new component test is built from.\n\nCORRECTIONS TO THE PLANNER'S CODEMAP (for increment 1 only):\n- `src/__fixtures__/whfb6-lexicanum/vampire-selfscope-bloodline.cat.xml` is NOT needed: the frozen definitive fixture carries the case in full and the ACs name it explicitly. Leave the reduced excerpt untouched.\n- `docs/testing/modifier-characteristic-value/README.md` must NOT change here (E2E scenario track, ADR 0033).\n- `src/evaluator/model.js`, `src/parser/schema/Catalogue.xsd`, `docs/battlescribe-data-format.md`, `src/evaluator/query.unitScope.test.js`, `src/evaluator/query.selfGatedInstanceOf.test.js` and the issue.md scope record belong to the later scope-sweep increment, not to this one.\n- `package.json` is not touched (no user-visible change in this increment).\n- `src/components/play/PlayUnitDetails.jsx` and its evaluator test are not touched (same field, same projection - see the omission note in the test plan).\n- `src/evaluator/modifiers.test.js`, `infoElements.test.js`, `infoProjection.test.js` are not extended: the rule under test spans fixpoint + index + query + modifiers + projection + report and needs a real fixture, so it belongs at the facade, in its own file.",
                "environment": "Repository root: `/home/user/tome_of_battle`. Node v22.22.2, npm 10.9.7, package type `module`.\n\nPREREQUISITE (run once, before anything else): `npm ci` - a fresh checkout has NO `node_modules`, so vitest, jsdom and @testing-library/react are missing until it has run. I ran it in this container: exit 0.\n\nCommands the test plan asks for (all from the repository root, after `npm ci`):\n- `npx vitest run src/evaluator` - the whole evaluator suite: unit/layer tests plus the manifest-driven E2E runner over `docs/testing/` (`e2e.testcatalog.test.js`, `crossCatalog.test.js`), which is what the criterion calls 'the evaluator E2E suite'.\n- `npx vitest run <path/to/one.test.js>` - a single file.\n\nNot part of this increment, do not run: `npm test` (it also starts the puppeteer app E2E in `e2e/`, which needs a build and a browser; the root CLAUDE.md exempts evaluator-only work from it), `npm run lint` (oxlint), `npm run knip`, `npm run depcruise`, `npm run typecheck`, `node scripts/measure-evaluator.js`, `node scripts/generate_screenshots.js`.\n\nParsing the frozen fixture costs about 1.4 s per `prepareDataset(gst + Vampire Counts .cat)`; a subsequent `evaluate` costs about 25-35 ms. Prepare the dataset once per test file (module scope or `beforeAll`), never per case.",
                "testPlan": "WHETHER: tests, yes - they are the whole deliverable of this increment (issue AC 8).\n\nWHAT I RAN, AND WHAT I DID NOT: I ran no test suite and took no baseline; nothing here is a claim about which tests are currently green or red. I did run two throwaway node scripts (deleted again) that called the public facade `prepareDataset`/`evaluate` on the frozen fixture, because the whole increment turns on whether the reported case still fails - it does not. The measured values are in `plan` and are the source of the expectations below. Whoever runs the commands below runs them first.\n\nCONVENTIONS: `src/evaluator/CLAUDE.md` is the suite doc for the evaluator test file - load it and follow it (German `describe`/`it` titles, `globalThis.DOMParser = new JSDOM().window.DOMParser` at module top, drive only the public facade `prepareDataset` + `evaluate`, assert against `report.capabilities`, nothing mocked, memoise the fixture parse across the file's cases, mark a case that pins already-correct behaviour with `KONTROLLE:` in its title). Nothing I read contradicts it. There is NO suite doc for `src/components/editor/` (no `CLAUDE.md` anywhere under `src/components/`): write a short one as part of your step, describing the conventions the directory already follows and that the new file follows - vitest + @testing-library/react `render`, `vi.mock` for `lucide-react`, `./BottomSheet` and the child components that have their own tests, real fixtures loaded in `beforeAll` through the production seam (`processImportedData` + `prepareDataset`/`evaluate` + `toEvaluatorRoster`), German test titles, naming `<Component>.<topic>.test.jsx`, and the single-file run command.\n\nWHAT - CASE 1 (issue AC 2, 3, 4): evaluator report on the frozen fixture.\nFile: `src/evaluator/evaluator.bloodlineProfileFixture.test.js` (new). Level: facade/integration. Framework: vitest. Run: `npx vitest run src/evaluator/evaluator.bloodlineProfileFixture.test.js`.\nDataset: `prepareDataset({ gameSystem: <'Warhammer Fantasy Battles (6th definitive edition).gst'>, catalogues: [<'Vampire Counts (6th definitive edition).cat'>] })` from `src/evaluator/__fixtures__/whfb6-definitive/`, parsed ONCE for the file.\nRoster per case: `{ forces: [{ defId: 'e989-15b8-7eb6-9668', count: 1, children: [ { defId: '6822-0110-a7c9-cbb0', count: 1, children: [] }, <bloodline block> ] }] }` where `<bloodline block>` is `{ defId: 'a56a-eb32-5a45-16fd', count: 1, children: [{ defId: <bloodline id>, count: 1, children: [] }] }` and is omitted for the no-bloodline case.\nObservation point: the capability whose `defId === '6822-0110-a7c9-cbb0'` and `anchorKind === 'occupied'` (slot path `0/0`), its `infoElements` entry with `kind === 'profile'` and `id === 'a106-4a05-36ea-cb01'`, read by characteristic NAME ('WS', 'A', 'Sv', 'Sv+').\nDerive the base values from the raw fixture XML inside the test file (the file's own `new DOMParser().parseFromString(...)`, as `evaluator.corpusLinkLocalChildren.test.js` does): the `<profile id=\"fabd-ef67-72f5-6b3f\">` characteristics, keyed by `typeId` - WS `f95b-da01-0578-3bdc`, A `6b9f-c8fe-8998-27e3`, Sv `f1be-e66c-d5e1-673c`, Sv+ `d4a9-0ed4-d041-e54b`. Let no DOM element survive into module scope; keep only the derived plain values. Today those base values are WS 7, A 4, Sv 7, Sv+ 7, but assert against the derived numbers, not against literals, wherever the AC speaks of the base value.\nCases:\n  1a KONTROLLE (AC 3) - roster WITHOUT any bloodline: WS, A, Sv, Sv+ all equal their derived base values.\n  1b (AC 2, AC 4) - bloodline `9fd9-e05c-ffcb-2c4d` (Blood Dragon): WS equals base WS + 2 (i.e. 9); Sv equals the literal string '4+' (the `value` of the gated `set`, non-numeric, unchanged in form); Sv+ and A stay at their base values (7 and 4) because those two members of the same modifierGroup are gated on the Strigoi category, not on Blood Dragon. Assert all four in one case - the point is that the group discriminates.\n  1c (AC 4, the '5+' armour save the criterion names) - bloodline `ddfa-0d72-8557-6906` (Strigoi): Sv+ equals the literal string '5+'; A equals base A + 1 (i.e. 5); WS and Sv stay at their base values.\n  1d (edge, the opposite arithmetic sign) - bloodline `5017-296d-edef-4562` (Necrarch): WS equals base WS - 2 (i.e. 5); A, Sv, Sv+ stay at their base values.\n  1e (edge, exclusivity) - in case 1b assert additionally that the profile ENTRY name is still 'Vampire Count' while `capability.name` is 'Vampire Count of Clan Blood Dragon': the `append` on the unit's name must not leak into the profile row, and the profile row's own name must not swallow the unit name.\nAssert values as strings exactly as the report carries them ('9', '4+', '5+'); do not coerce to numbers except where the AC states an arithmetic relation, and then compare `Number(effective) === Number(base) + 2`.\n\nWHAT - CASE 2 (issue AC 1, AC 4, AC 8): the component fed by that report.\nFile: `src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx` (new). Level: component. Framework: vitest + @testing-library/react. Run: `npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx`.\nBuild it from the two existing patterns: the props and mocks of `src/components/editor/UnitSelectionCard.evaluator.test.jsx` (mock `lucide-react`, `./BottomSheet`, `./SelectionConfigurator`, `./UnitChips`; the card gets `capabilities` and `pathBySelectionId` as props already), and the real-fixture seam of `src/components/editor/SelectionConfigurator.groupMembership.test.jsx` (`processImportedData([{name,content}] gst, [{name,content}] cat).system` plus `prepareDataset` on the same two XML strings, both in `beforeAll`).\nApp roster: hand-built, in the shape `rosterAdapter` reads - `{ catalogueId: <the .cat root id '4d73-5ab0-9020-403c'>, name, costLimit: 3000, costLimitType: 'ecfa-8486-4f6c-c249', forces: [{ id: 'force-1', forceEntryId: 'e989-15b8-7eb6-9668', catalogueId, selections: [ <VC>, <bloodline> ] }] }` with `<VC> = { id: 'sel-vc', name: 'Vampire Count', entryLinkId: null, selectionEntryId: '6822-0110-a7c9-cbb0', number: 1, category: null, selections: [] }` and `<bloodline> = { id: 'sel-bloodlines', name: 'Bloodlines', entryLinkId: null, selectionEntryId: 'a56a-eb32-5a45-16fd', number: 1, category: null, selections: [ { id: 'sel-bd', name: 'Bloodline of Clan Blood Dragon', entryLinkId: null, selectionEntryId: '9fd9-e05c-ffcb-2c4d', number: 1, category: null, selections: [] } ] }`. Feed it through `toEvaluatorRoster` -> `evaluate(prepared, evalRoster)` and render the card for the `sel-vc` selection.\nCases:\n  2a (AC 1, AC 2, AC 4) - with the Blood Dragon selection present: guard-assert first against the real report that the slot's profile entry carries WS '9' and Sv '4+' (the same guard style the existing evaluator card test uses), then assert the rendered `.profile-table`: the header row contains 'WS' and 'Sv', and the cell under 'WS' reads '9' and the cell under 'Sv' reads '4+'. Match cell to header by column index within the model stat-block table, not by scanning all cell texts - '9' also occurs as the Ld value and a bare `toContain('9')` would pass on the wrong cell.\n  2b KONTROLLE (AC 3) - the same roster without the bloodline selections: the same cells read '7' and '7'.\nDo not assert on cell CSS classes or tooltips; the report carries no `originalValue`, so no cell is marked as modified and that is outside this increment.\n\nDELIBERATELY UNTESTED, with the reason:\n- `src/components/play/PlayUnitDetails.jsx` - it reads the identical field (`slotCapability.infoElements`, filtered to `kind === 'profile'`) through the identical `groupProfilesByType` helper; a second component test would restate case 2 verbatim and buy no new failure mode.\n- The remaining bloodlines (Lahmia `4f07-e982-6665-70b7`, Von Carstein `f557-097a-d26b-9363`) - their groups carry no characteristic modifier at all for this unit, so a case over them would assert only base values already covered by case 1a.\n- Every scope keyword other than the entry-id scope this catalogue uses - the scope sweep is a later increment, by the increment's own boundary.\n- The legacy `src/roster/profileCollector.js` path and `UnitChips` - the issue puts them out of scope.\n\nWHAT COUNTS AS DONE - the closed list, run from the repository root after `npm ci`, nothing else:\n1. `npx vitest run src/evaluator`\n2. `npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx src/components/editor/UnitSelectionCard.evaluator.test.jsx src/components/editor/UnitSelectionCard.test.jsx`\nCommand 1 covers the evaluator unit tests and the manifest-driven evaluator E2E runner (the criterion's two suites) plus the new facade pin; command 2 covers the new component pin and the two existing test files of the component it touches. Report each command, what it covered and its exit code. No existing test may be weakened, skipped or deleted to make a new one green.",
                "checks": [
                  "npx vitest run src/evaluator",
                  "npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx src/components/editor/UnitSelectionCard.evaluator.test.jsx src/components/editor/UnitSelectionCard.test.jsx"
                ],
                "questions": [],
                "summary": "The reported bug does not reproduce: measured against the frozen whfb6-definitive fixture through the public facade, a Vampire Count in a force with the Blood Dragon bloodline already reports WS 9 (base 7 + 2) and Sv '4+' in capability.infoElements, and the profile table renders that value verbatim. Increment 1 therefore delivers the regression pin the issue asks for - one facade-level test on the frozen fixture (Blood Dragon, Strigoi's non-numeric '5+', Necrarch's decrement, and the no-bloodline control) and one component test that renders UnitSelectionCard from a real evaluator report - and no production change unless a pin turns red."
              },
              "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on its own branch: `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1`, off `claude/profilwert-modifikatoren-bloodlines-zlw2p9`.\nBefore anything else, on `claude/profilwert-modifikatoren-bloodlines-zlw2p9`: record the name with the backlog helper's `branch` subcommand, as `branch docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1`, commit that state change and push `claude/profilwert-modifikatoren-bloodlines-zlw2p9`. Then create the branch with `git checkout -b claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1`, push it with `-u`, and work on it from there.\nThis run works docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md one increment at a time, and increment 1 is yours:\n  Gated profile characteristic modifiers reach the unit profile display - A characteristic modifier whose condition holds for a unit changes the value the user sees in the unit profile table, for numeric and non-numeric values alike, with the reported Vampire Counts bloodline case as the worked example.\nWhat it has to satisfy:\n  - The unit profile display shows the modified characteristic value whenever a profile modifier's condition holds for that unit (issue AC 1).\n  - With the frozen whfb6-definitive Vampire Counts fixture, a Vampire Count in a force that has chosen the Blood Dragon bloodline reports WS as the catalogue base value plus 2, and reports the Sv, Sv+ and A values the same modifierGroup sets (issue AC 2).\n  - The same Vampire Count in a force without that bloodline still reports the unmodified base values (issue AC 3).\n  - A non-numeric characteristic value written by a gated set modifier, such as an armour save 5+, reaches the display unchanged in form (issue AC 4).\n  - The regression is pinned by a test that exercises the path the user actually sees - the evaluator report and the component fed by it - and not only the legacy profileCollector (issue AC 8).\n  - The evaluator unit tests and the evaluator E2E suite pass, and no existing test is weakened or skipped to make the new ones green.\nDeliberately not yours, and not a gap - a later increment takes each of these: Every condition scope on a characteristic modifier is evaluated or recorded as uncoverable.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json`, before you begin.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 research:inc-1.0 <the prompt file>` - the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 research:inc-1.0 <the return file> <the prompt file>` - your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "tests:inc-1.0",
              "at": "2026-08-12T14:09:36.855Z",
              "return": {
                "cases": [
                  {
                    "case": "1a KONTROLLE (AC 3) - roster WITHOUT any bloodline: WS, A, Sv, Sv+ all equal their derived base values.",
                    "file": "src/evaluator/evaluator.bloodlineProfileFixture.test.js",
                    "testName": "Echte Katalogdaten: gegatterte Profilwert-Modifikatoren am Vampire Count (Issue 81) > KONTROLLE: ohne Blutlinie meldet der Slot alle vier Basiswerte unveraendert (Kriterium 3)",
                    "demands": "Evaluates the Vampire Count in the Standard force with no bloodline selection, reads the occupied slot at path 0/0, its infoElements profile entry a106-4a05-36ea-cb01, and asserts WS/A/Sv/Sv+ equal the values derived from the raw fixture profile fabd-ef67-72f5-6b3f (not literals).",
                    "got": "Passes against the unmodified codebase (npx vitest run src/evaluator/evaluator.bloodlineProfileFixture.test.js, exit 0). This is the one case the plan itself marks KONTROLLE, so a green result is expected."
                  },
                  {
                    "case": "1b (AC 2, AC 4) - bloodline 9fd9-e05c-ffcb-2c4d (Blood Dragon): WS equals base WS + 2; Sv equals the literal string '4+'; Sv+ and A stay at their base values.",
                    "file": "src/evaluator/evaluator.bloodlineProfileFixture.test.js",
                    "testName": "Echte Katalogdaten: gegatterte Profilwert-Modifikatoren am Vampire Count (Issue 81) > meldet mit der Blood-Dragon-Blutlinie WS als Basiswert + 2 und Sv als \"4+\"; Sv+ und A bleiben Basiswert (Kriterium 2, 4)",
                    "demands": "With the Blood Dragon bloodline selected under the Bloodlines group, the profile entry reports WS = base + 2, Sv = '4+' unchanged in form, and Sv+/A unchanged at base - the modifierGroup discriminates by bloodline category.",
                    "got": "Passes against the unmodified codebase (same command, exit 0) - not a currently-failing case. See openQuestions: the plan implicitly expects this case to fail until the fix lands, but the evaluator facade already computes it correctly with no code change on this branch."
                  },
                  {
                    "case": "1c (AC 4, the '5+' armour save the criterion names) - bloodline ddfa-0d72-8557-6906 (Strigoi): Sv+ equals the literal string '5+'; A equals base A + 1; WS and Sv stay at their base values.",
                    "file": "src/evaluator/evaluator.bloodlineProfileFixture.test.js",
                    "testName": "Echte Katalogdaten: gegatterte Profilwert-Modifikatoren am Vampire Count (Issue 81) > meldet mit der Strigoi-Blutlinie Sv+ als \"5+\" und A als Basiswert + 1; WS und Sv bleiben Basiswert (Kriterium 4)",
                    "demands": "With the Strigoi bloodline selected, the profile entry reports Sv+ = '5+' unchanged in form and A = base + 1; WS/Sv unchanged at base.",
                    "got": "Passes against the unmodified codebase (same command, exit 0) - not a currently-failing case; same discrepancy as case 1b."
                  },
                  {
                    "case": "1d (edge, the opposite arithmetic sign) - bloodline 5017-296d-edef-4562 (Necrarch): WS equals base WS - 2; A, Sv, Sv+ stay at their base values.",
                    "file": "src/evaluator/evaluator.bloodlineProfileFixture.test.js",
                    "testName": "Echte Katalogdaten: gegatterte Profilwert-Modifikatoren am Vampire Count (Issue 81) > meldet mit der Necrarch-Blutlinie WS als Basiswert - 2; A, Sv und Sv+ bleiben Basiswert (Kriterium 2, Vorzeichen-Randfall)",
                    "demands": "With the Necrarch bloodline selected, the profile entry reports WS = base - 2 (decrement, opposite sign from the Blood Dragon increment); A/Sv/Sv+ unchanged at base.",
                    "got": "Passes against the unmodified codebase (same command, exit 0) - not a currently-failing case; same discrepancy as case 1b."
                  },
                  {
                    "case": "1e (edge, exclusivity) - in case 1b assert additionally that the profile ENTRY name is still 'Vampire Count' while capability.name is 'Vampire Count of Clan Blood Dragon': the append on the unit's name must not leak into the profile row, and the profile row's own name must not swallow the unit name.",
                    "file": "src/evaluator/evaluator.bloodlineProfileFixture.test.js",
                    "testName": "Echte Katalogdaten: gegatterte Profilwert-Modifikatoren am Vampire Count (Issue 81) > haelt den effektiven Namen des Profil-Eintrags von dem der Einheit getrennt (Randfall Exklusivitaet)",
                    "demands": "With Blood Dragon selected, the profile entry's own name stays 'Vampire Count' while the slot capability's name carries the appended 'of Clan Blood Dragon' text - the two names stay independent.",
                    "got": "Passes against the unmodified codebase (same command, exit 0). Note for whoever next touches this: the catalogue's append modifier joins with a non-breaking space (U+00A0) between 'Count' and 'of', not a regular space - the test asserts the literal character the fixture carries, documented inline."
                  },
                  {
                    "case": "CASE 2 (issue AC 1, AC 4, AC 8): the component fed by that report. 2a (AC 1, AC 2, AC 4) - with the Blood Dragon selection present, the rendered .profile-table shows WS '9' and Sv '4+' matched by column index, not by scanning cell text.",
                    "file": "src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx",
                    "testName": "UnitSelectionCard: gegatterte Profilwert-Modifikatoren der Blood-Dragon-Blutlinie erreichen die Statblock-Tabelle (Issue 81) > zeigt WS \"9\" und Sv \"4+\" mit gewaehlter Blood-Dragon-Blutlinie (Kriterien 1, 2, 4)",
                    "demands": "Builds the hand-built app roster with Vampire Count + Bloodlines/Blood Dragon selections, guard-asserts the real report's infoElements carry WS '9' and Sv '4+' at the slot's own path, then renders UnitSelectionCard and asserts the .profile-table WS/Sv cells (matched by header column index) read '9' and '4+'.",
                    "got": "Passes against the unmodified codebase (npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx, exit 0) - not a currently-failing case. This is the case AC 8 names as the one that must exercise 'the path the user actually sees'; see openQuestions."
                  },
                  {
                    "case": "2b KONTROLLE (AC 3) - the same roster without the bloodline selections: the same cells read '7' and '7'.",
                    "file": "src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx",
                    "testName": "UnitSelectionCard: gegatterte Profilwert-Modifikatoren der Blood-Dragon-Blutlinie erreichen die Statblock-Tabelle (Issue 81) > KONTROLLE: zeigt WS \"7\" und Sv \"7\" ohne Blutlinie (Kriterium 3)",
                    "demands": "With no bloodline selection, the rendered WS and Sv cells read the unmodified base values '7' and '7'.",
                    "got": "Passes against the unmodified codebase (same command, exit 0). This is the case the plan marks KONTROLLE, so a green result is expected."
                  }
                ],
                "openQuestions": [
                  "All seven planned cases across both new files pass against the unmodified codebase (no production code touched on this branch beyond the prior research commit; `git log` shows no implementation commit). Only two of them (1a, 2b) are marked KONTROLLE by the plan - the other five (1b, 1c, 1d, 1e, 2a) pin exactly the Blood Dragon/Strigoi/Necrarch bloodline cases the issue reports as broken, at both the evaluator facade and the UnitSelectionCard component, and none of them fail. The researcher's own note ('the whole increment turns on whether the reported case still fails - it does not') covered only the facade via a throwaway script; my component case (2a) shows the same is true one layer up, at the rendered .profile-table. Either the reported regression is already resolved for this self-scope-by-entry-id condition shape (the fixture's conditions use `scope=\"6822-0110-a7c9-cbb0\"`, the Vampire Count's own selectionEntry id, not the literal keyword `scope=\"unit\"` that issue AC 5 calls out separately and this increment excludes), or the true regression lives on a code path this scenario does not exercise (e.g. a stale/cached report, a different roster-building path than the one `toEvaluatorRoster` produces here, or `PlayUnitDetails.jsx` specifically rather than `UnitSelectionCard.jsx`). The next research round should re-verify against a live build/UI reproduction of the reporter's exact steps before deciding whether increment 1 needs an implementation change at all, or whether these seven cases already stand as sufficient regression pins."
                ],
                "questions": [],
                "summary": "Wrote the two planned test files (src/evaluator/evaluator.bloodlineProfileFixture.test.js, 5 cases; src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx, 2 cases) plus a new src/components/editor/CLAUDE.md, exactly per the test plan; all 7 cases pass unmodified (no red case), which is an open question for research rather than a writing error on this side."
              },
              "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on branch `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1`. Before anything else make sure you are on it: `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1` and check it out; if the remote does not have it, create it from `claude/profilwert-modifikatoren-bloodlines-zlw2p9` and push it with `-u`.\nThis run works docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md one increment at a time, and increment 1 is yours:\n  Gated profile characteristic modifiers reach the unit profile display — A characteristic modifier whose condition holds for a unit changes the value the user sees in the unit profile table, for numeric and non-numeric values alike, with the reported Vampire Counts bloodline case as the worked example.\nWhat it has to satisfy:\n  - The unit profile display shows the modified characteristic value whenever a profile modifier's condition holds for that unit (issue AC 1).\n  - With the frozen whfb6-definitive Vampire Counts fixture, a Vampire Count in a force that has chosen the Blood Dragon bloodline reports WS as the catalogue base value plus 2, and reports the Sv, Sv+ and A values the same modifierGroup sets (issue AC 2).\n  - The same Vampire Count in a force without that bloodline still reports the unmodified base values (issue AC 3).\n  - A non-numeric characteristic value written by a gated set modifier, such as an armour save 5+, reaches the display unchanged in form (issue AC 4).\n  - The regression is pinned by a test that exercises the path the user actually sees - the evaluator report and the component fed by it - and not only the legacy profileCollector (issue AC 8).\n  - The evaluator unit tests and the evaluator E2E suite pass, and no existing test is weakened or skipped to make the new ones green.\nDeliberately not yours, and not a gap — a later increment takes each of these: Every condition scope on a characteristic modifier is evaluated or recorded as uncoverable.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 research:inc-1.0 --fields testPlan` — the test plan.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 tests:inc-1.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 tests:inc-1.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "implement:inc-1.0",
              "at": "2026-08-12T14:12:35.008Z",
              "return": {
                "deviations": [
                  "The plan names no production change for this increment ('expect NO production change ... Write no production code unless a pin the test-author wrote comes out red'), and no pin came out red. I therefore wrote and changed no production code and no test. The only repository change of this step is the run-state record in backlog.json. This is the plan followed, not departed from, but it is recorded here because an implement step that lands no code is unusual enough to be checked.",
                  "The shared brief asks me to confirm the planned tests fail for the right reason before implementing. I could not: both new files (src/evaluator/evaluator.bloodlineProfileFixture.test.js, src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx) were already green on the unmodified branch, which the test-author itself recorded per case ('Passes against the unmodified codebase') and the research plan predicted from two throwaway probes. They are regression pins for behaviour that already holds (issue AC 8), not red-to-green drivers. I did not weaken, edit or add a test in response - editing tests is not mine."
                ],
                "commands": [
                  {
                    "command": "npx vitest run src/evaluator",
                    "exit": 0,
                    "note": "93 test files, 1694 tests, all passed, none skipped. Includes the two new evaluator pins from the test step (5 cases in evaluator.bloodlineProfileFixture.test.js) and the manifest-driven E2E runner over docs/testing/ (e2e.testcatalog.test.js, crossCatalog.test.js). Duration ~78 s."
                  },
                  {
                    "command": "npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx src/components/editor/UnitSelectionCard.evaluator.test.jsx src/components/editor/UnitSelectionCard.test.jsx",
                    "exit": 0,
                    "note": "3 test files, 34 tests, all passed, none skipped: gatedProfileCharacteristics 2, evaluator 4, UnitSelectionCard 28. Duration ~6 s."
                  }
                ],
                "blockers": [
                  "Nothing stopped this step. One thing the reviewer should weigh: the increment closes with zero production diff because the reported defect does not reproduce on the path issue 81 names (the evaluator report and UnitSelectionCard already carry the gated values, numeric and non-numeric alike), so criteria 1-4 were already satisfied and only criterion 8 (the regression pin) produced work - and that work belongs to the test step, not to mine.",
                  "Consequence for the increment after this one, not for me: if the user's observed symptom is real, it must live on a display path this increment declares out of scope (SelectionConfigurator.jsx, upgradeDetails.jsx, UnitChips.jsx) or on the scope sweep of AC 5-7. Neither is mine to investigate here."
                ],
                "questions": [],
                "summary": "Increment 1 of issue 81 needed no production code: both regression pins the test step wrote were already green, so the gated bloodline modifiers (WS +2/-2, A +1, Sv '4+', Sv+ '5+') demonstrably reach the evaluator report and the UnitSelectionCard statblock unchanged. Both command sets are green - the evaluator suite at 1694 tests and the three UnitSelectionCard files at 34 tests, exit 0 each."
              },
              "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on branch `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1`. Before anything else make sure you are on it: `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1` and check it out; if the remote does not have it, create it from `claude/profilwert-modifikatoren-bloodlines-zlw2p9` and push it with `-u`.\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 research:inc-1.0 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 tests:inc-1.0 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\n  - `npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx src/components/editor/UnitSelectionCard.evaluator.test.jsx src/components/editor/UnitSelectionCard.test.jsx`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 implement:inc-1.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 implement:inc-1.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `summary`\n"
            },
            {
              "label": "review:inc-1.0",
              "at": "2026-08-12T14:35:33.727Z",
              "return": {
                "findings": [],
                "findingCount": 0,
                "allDirect": true,
                "reason": "",
                "questions": [],
                "summary": "Both listed commands are green on the increment branch: `npx vitest run src/evaluator` (93 files, 1694 tests, exit 0) and `npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx src/components/editor/UnitSelectionCard.evaluator.test.jsx src/components/editor/UnitSelectionCard.test.jsx` (3 files, 34 tests, exit 0, nothing skipped); the increment adds only two new test files plus a suite doc and changes no production code and no existing test. One probe settled my doubt that the pins pass only because the test roster is hand-built: driving the reported case through the app's own construction and evaluation path (selectionFactory.createSelectionFromDef with the real catalogue entries, then evaluateAppRoster) in a throwaway worktree reports WS 9, Sv 4+, Sv+ 7, A 4 and the effective name 'Vampire Count of Clan Blood Dragon' for a Blood Dragon Vampire Count, i.e. the same modified values the committed tests assert, so the green is not an artefact of the simplified roster. Observation, needing no correction: the new src/components/editor/CLAUDE.md states that every case guard-asserts against the real report before asserting on the DOM, while the new file's own control case (without bloodline) asserts on the DOM alone."
              },
              "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on branch `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1`. Before anything else make sure you are on it: `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1` and check it out; if the remote does not have it, create it from `claude/profilwert-modifikatoren-bloodlines-zlw2p9` and push it with `-u`.\nReview round 0 of increment 1. The increment's diff is its branch against the merge-base with `claude/profilwert-modifikatoren-bloodlines-zlw2p9`: judge `git diff claude/profilwert-modifikatoren-bloodlines-zlw2p9...HEAD` (three dots), whole, and nothing outside it.\nThis run works docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md one increment at a time, and increment 1 is yours:\n  Gated profile characteristic modifiers reach the unit profile display — A characteristic modifier whose condition holds for a unit changes the value the user sees in the unit profile table, for numeric and non-numeric values alike, with the reported Vampire Counts bloodline case as the worked example.\nWhat it has to satisfy:\n  - The unit profile display shows the modified characteristic value whenever a profile modifier's condition holds for that unit (issue AC 1).\n  - With the frozen whfb6-definitive Vampire Counts fixture, a Vampire Count in a force that has chosen the Blood Dragon bloodline reports WS as the catalogue base value plus 2, and reports the Sv, Sv+ and A values the same modifierGroup sets (issue AC 2).\n  - The same Vampire Count in a force without that bloodline still reports the unmodified base values (issue AC 3).\n  - A non-numeric characteristic value written by a gated set modifier, such as an armour save 5+, reaches the display unchanged in form (issue AC 4).\n  - The regression is pinned by a test that exercises the path the user actually sees - the evaluator report and the component fed by it - and not only the legacy profileCollector (issue AC 8).\n  - The evaluator unit tests and the evaluator E2E suite pass, and no existing test is weakened or skipped to make the new ones green.\nDeliberately not yours, and not a gap — a later increment takes each of these: Every condition scope on a characteristic modifier is evaluated or recorded as uncoverable.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\n  - `npx vitest run src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx src/components/editor/UnitSelectionCard.evaluator.test.jsx src/components/editor/UnitSelectionCard.test.jsx`\nRead nothing out of docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 review:inc-1.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1 review:inc-1.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `questions`\n  - `summary`\n"
            }
          ]
        }
      ]
    },
    {
      "id": "inc-2",
      "title": "Every condition scope on a characteristic modifier is evaluated or recorded as uncoverable",
      "goal": "The closed list of scope values a condition on a characteristic modifier may name is derived from the format documentation and the XSD, every value in it is evaluated against the node it names rather than silently dropped, and every value is either covered by a test or recorded in the issue file with the reason it cannot be covered from the catalogue data in the repository.",
      "criteria": [
        "A gated modifier whose condition uses scope=\"unit\" is evaluated against the unit, not silently dropped (issue AC 5).",
        "Every scope a condition on a characteristic modifier may name is evaluated against the node it names, and none is silently dropped; the closed list of scope values is derived from docs/battlescribe-data-format.md and src/parser/schema/Catalogue.xsd rather than from the existing code, and the derivation is written down (issue AC 6).",
        "Each scope value in that closed list is either covered by a test or listed in docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md with the reason it cannot be covered from the catalogue data present in the repository (issue AC 7).",
        "The issue file also records what increment 1 established: the reported bloodline case is gated by a scope naming the unit's own selectionEntry id, that path is correct today and is now pinned, so the reported symptom is either already resolved or lives on a scope shape this increment covers.",
        "npm test passes and the evaluator E2E suite passes (issue AC 9).",
        "src/components/editor/CLAUDE.md describes the guard-assert convention as the test files in that directory actually follow it - the control case of UnitSelectionCard.gatedProfileCharacteristics.test.jsx asserts on the DOM alone, which the current wording does not admit."
      ],
      "depth": "full",
      "status": "todo",
      "note": "Now the whole of what is left, and the increment that decides whether this issue ships a production fix at all: increment 1 proved the entry-id scope path correct, so a scope value that is silently dropped is the only remaining place the reporter's symptom can live inside this issue's boundary. Carries the run-wide green gate (AC 9), claimed exactly once. Two boundaries the run has already established: authoring scenarios under docs/testing/ is the black-box e2e-testcase-author's track (ADR 0033) and no criterion here needs one - read docs/testing/modifier-characteristic-value/README.md to see which shapes its scenarios claim to leave uncovered, and record any correction in issue.md rather than rewriting that tree; and a version bump in package.json is only warranted if this increment lands a user-visible fix, which is not known before the sweep.",
      "branch": "claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2",
      "steps": [
        {
          "label": "research:inc-2.0",
          "at": "2026-08-12T14:56:07.381Z",
          "return": {
            "needsTests": true,
            "plan": "## What this increment builds\n\nTwo condition scopes that real catalogue data uses and the engine today drops on the floor (`primary-category`, `model-or-unit`), plus the written derivation of the closed scope list, plus the coverage record in the issue file. Nothing else in the evaluator changes.\n\n### The derivation of the closed list (AC 6), from the format doc and the XSD\n\n1. `src/parser/schema/Catalogue.xsd:426` types `scope` as `xs:string use=\"required\"` on the shared `QueryBase` — the schema constrains nothing and does not distinguish constraint from condition from repeat. So the closed list cannot come from the XSD; the XSD only says that one list serves all three query kinds.\n2. `docs/bsdata-catalogue-development-wiki/Data-structure-overview.md:377` (Condition) enumerates: `parent|ancestor|roster|force|primary category`, plus \"any ancestor, any Category, or any Force\" — i.e. five keywords and three id-shaped values.\n3. `docs/battlescribe-data-format.md` adds, from data evidence: `self` (§7.6 table), `unit` and `ancestor` semantics (§7.7 box `#scope-unit-ancestor`), `primary-catalogue` (§7.6 box `#scope-primary-catalogue`), and §13.1 line 1449 additionally names `primary-category` (4x, Forces of Chaos) and `model-or-unit` (2x, Lizardmen).\n4. Cross-check against the data: every `scope=\"…\"` occurrence of both frozen corpora (`src/evaluator/__fixtures__/whfb6-definitive/`, `src/__fixtures__/whfb6*/`) yields exactly nine literal keywords — `parent` 9634, `force` 2933, `roster` 1840, `self` 338, `unit` 337, `ancestor` 33, `primary-catalogue` 27, `primary-category` 4, `model-or-unit` 2 — and 13 distinct hex ids. No occurrence of a literal `scope=\"category\"`.\n\nThe closed list is therefore: the nine keywords above, the id case (entry id, group id, forceEntry id, category id), and \"anything else\" → fail-closed `unresolvedScope`. The literal `category` in `docs/battlescribe-data-format.md` §7.6/§13.1 is a doc artefact: it folds the wiki's *\"any [Category]\"* (a category **id**, already resolved by `query.js` as the nearest ancestor carrying that category) into a pseudo-keyword. Correct the doc; do not add a `CATEGORY` keyword to the engine.\n\n### Semantics of the two new keywords, derived from the data\n\n**`primary-category`** — an identity check, not a counting frame; the same shape as `primary-catalogue` and `ancestor`. Evidence (`Forces of Chaos (6th definitive edition).cat:12679, 13024, 13062, 13093`): all four sit on a `condition type=\"instanceOf\" field=\"selections\"` with `childId=\"e94b-6a54-8779-cd60\"` = the `.gst` categoryEntry **Rare**, inside an `and` conditionGroup next to `instanceOf scope=\"force\"` (Daemonic Legion (SoC) `93f4-7522-86e9-7d45`), and they gate a `modifierGroup` on an `infoLink type=\"profile\"` that `set`s four characteristics (T, Ld, BS, W) to `-`. The carrier is a shared mount entry (`Juggernaut of Khorne ba34-87a0-8cd2-c77d`), linked from five places whose primary categories discriminate exactly as the rule needs: `Daemonic Chariot of Khorne` carries `categoryLink Rare primary=\"true\"` (the gate holds — a draught beast has no separate T/W/Ld/BS line), while `BloodCrushers` is `Special primary=\"true\"`, `Lord of Chaos` is `Lord`, `Daemonic Herald` is `Heroes` (the gate does not hold). The mount entry itself carries only `categoryLink Khorne primary=\"false\"`, so the resolution has to **climb**.\n\nContract (mirrors `resolvePrimaryCatalogue`):\n\n| Situation | Result |\n| --- | --- |\n| `field` is not `SELECTION_COUNT` | `unsupportedField` diagnostic, 0 |\n| `targetId` equals the nearest effective primary category (node included) | 1 |\n| `targetId` is another category id | 0, no diagnostic |\n| `targetId === null` (percent denominator / `childId=\"any\"`) | 1 — the frame has exactly one primary category |\n| no node in the chain (node included, root excluded) carries a primary category | 0 **with** `unresolvedScope` |\n\nIt is answered **before** any frame or index work and is independent of `shared` and the include-flags: a primary category is not made narrower by an instance. All four occurrences carry `shared=\"true\" includeChildSelections=\"true\"`, so the flags are not discriminating in the data either.\n\n**`model-or-unit`** — a regular counting frame, the exact generalisation of `unit`: the nearest ancestor, the node **included**, whose raw `type` is `model` or `unit`; for an `entryLink` the raw type of its transitively resolved target counts, the same inheritance rule `isUnitDefinition` already applies. Evidence (`Lizardmen (6th definitive edition).cat:4939, 4967`): both are `instanceOf field=\"selections\" childId=\"7b73-1714-155f-8f67\"` = the categoryEntry **Red Crested Skink**, gating a `set hidden=\"false\"` on the hidden upgrades `Skavenpelt` and `Sign of Sotek`; the category is carried by the unit `Red Crested Skinks 64ec-…` and by the upgrade `Red Crests adfb-…`, and both conditions carry `includeChildSelections=\"true\"` — the counting-frame reading covers both encodings (the frame's own effective categories are booked under its own frame key in the BASE bucket, `countIndex.js` `indexNodeContribution`, so the frame itself counts; a nested carrier counts through the flag).\n\n### Code changes\n\n**`src/evaluator/model.js`**\n- `ScopeKeyword`: add `MODEL_OR_UNIT: 'model-or-unit'` and `PRIMARY_CATEGORY: 'primary-category'`. Extend the block comment: `MODEL_OR_UNIT` is a regular counting frame like `UNIT`; `PRIMARY_CATEGORY` joins `PRIMARY_CATALOGUE`/`ANCESTOR` as a frame that points at no tree node and is answered as an identity check. Name the data evidence (4x Forces of Chaos, 2x Lizardmen) the way the neighbouring entries do.\n- `ScopeKind`: add the two mirrored entries (`MODEL_OR_UNIT: ScopeKeyword.MODEL_OR_UNIT`, `PRIMARY_CATEGORY: ScopeKeyword.PRIMARY_CATEGORY`). This matters: `violationClassification.js` builds `SCOPE_KEYWORDS` from `Object.values(ScopeKeyword)` and returns `kind: scope` for any keyword, so `ScopeKind` has to stay the closed enumeration of what `classifyScope` can return. `src/i18n/violationMessages.js` `scopeGroupOf` maps everything but `roster`/`force` to `local` — no message key is needed.\n\n**`src/evaluator/query.js`**\n- Generalise the unit-type predicate: keep `UNIT_TYPE = 'unit'`, add `MODEL_TYPE = 'model'`, and turn `isUnitDefinition`/`nearestUnitAncestor` into a type-set form (e.g. `hasRawType(def, types)` + `nearestAncestorOfRawTypes(node, types)`), so `unit` and `model-or-unit` share one implementation and one inheritance rule for `entryLink`. Do not change what `unit` resolves to.\n- `resolveSharedFrame`: add `case ScopeKeyword.MODEL_OR_UNIT: return nearestAncestorOfRawTypes(ctx.node, MODEL_OR_UNIT_TYPES);`.\n- `query()`: add `if (scope === ScopeKeyword.PRIMARY_CATEGORY) return resolvePrimaryCategory(ctx, field, targetId);` next to the `PRIMARY_CATALOGUE` and `ANCESTOR` branches, i.e. before any frame or index work.\n- New `resolvePrimaryCategory(ctx, field, targetId)` beside `resolvePrimaryCatalogue`, implementing the table above. It climbs `for (let current = ctx.node; current !== null && !current.isRoot; current = current.parent)` and takes the first non-null `ctx.effective.primaryCategoryIdOf(current)`.\n- `EMPTY_EFFECTIVE` must gain `primaryCategoryIdOf: () => null` — it is the shared read-only fallback when no effective state is passed, and it currently only carries `categoryIdsOf`.\n- The phantom exemption in the `frame === null` branch (today `if (scope !== ScopeKeyword.UNIT || ctx.node.isPhantom !== true)`) must cover `model-or-unit` too — same reasoning as issue 086: an offer anchor is an engine-invented evaluation position, and real catalogues park hidden entries at army level and use them via `entryLink` inside units (the Lizardmen `Skavenpelt`/`Sign of Sotek` entries are exactly that shape). Use a small keyword set instead of a second `||` chain.\n- Extend the module header comment: name both new scopes in the list of frames, and keep the existing sentence structure.\n\n**No change to `fixpoint.js`, `countIndex.js`, `effectiveState.js` behaviour.** Rejected alternative, with the reason, because it looks necessary and is not: adding the primary category to `countRelevantKeyOf` (the fixpoint's count-relevant fingerprint) so that a `set-primary` propagates into a later round. It is not needed. `applyModifiersOfNodes` passes `effective: state` — the state being written **this** round — and `allNodes`/`realNodes` traverse pre-order (`evalTree.js:1087-1104`), so every node's ancestors are written before the node itself; the frame of `primary-category` is always the node or an ancestor, hence always fresh within the round. Per node, `applyCarrierModifiers(node.def)` runs before the info carriers, so a `set-primary` on the node itself also lands before its own profile gate. And `setPrimaryCategory` additionally ensures membership, which *is* in the fingerprint. Do **not** touch the fingerprint.\n- One JSDoc correction follows from this: `effectiveState.js:185-190` claims the counting and limit layer never reads `primaryCategoryIdOf`. It does now. Rewrite that paragraph: the query primitive reads it for `scope=\"primary-category\"`; the count-relevant fingerprint deliberately still leaves it out, because the frame is the node or an ancestor and the pre-order traversal writes it earlier in the same round.\n\n### Documentation changes\n\n**`docs/battlescribe-data-format.md`** (canonical, takes precedence over ADRs):\n- §7.6 constraint table, `scope` row (line 664): replace the literal `category` with the id case (\"eine Vorfahren-Id: Eintrag, Gruppe, `forceEntry` oder Kategorie\"), and point at the new box for the keywords that occur only on conditions.\n- §7.7 condition table, `scope` row (line 811): name the full closed list including `primary-category` and `model-or-unit`, each with a link to the new box.\n- New box after the `#scope-unit-ancestor` box, anchored `#scope-primary-category-model-or-unit`, written in the same voice as its two neighbours: what upstream says (the wiki names \"primary category\" without semantics and does not know `model-or-unit`; the XSD types `scope` as a bare string), the data evidence spelled out with file, line and ids as given above, the decided semantics, and the engine's contract (identity check vs counting frame, the fail-closed rows).\n- §13.1 line 1449: drop the literal `category`, add the id case explicitly, keep `primary-category`/`model-or-unit` and link the new box.\n- §15 gap table (near line 1515-1517): add one row for `primary-category`/`model-or-unit` in the shape of the existing `primary-catalogue` and `unit`/`ancestor` rows, naming this issue as the deciding one.\n\n**`docs/evaluator-architecture.md`**: extend `enum ScopeKeyword` (line 176-186) and `enum ScopeKind` (line 297) with the two values plus a one-line comment each, matching the existing comment style; add a `PRIMARY_CATEGORY` block to the §4.5 pseudocode next to `PRIMARY_CATALOGUE`/`ANCESTOR` (lines 547-571) and name `MODEL_OR_UNIT` in the `resolveScopeFrame` comment (line 575).\n\n**`docs/issues/81-.../issue.md`** — the record AC 7 asks for. Add, after the Description and before the Acceptance Criteria (or as a new section under Comments, whichever reads better in this file):\n1. *The closed list of condition scopes and where it comes from* — the four derivation steps above, verbatim in substance, including the occurrence counts and the statement that the XSD constrains nothing.\n2. *Coverage per scope value* — this table, filled with the final test names:\n\n| scope value | evaluated as | covered by |\n| --- | --- | --- |\n| `roster` | frame = roster root | `query.matrix.test.js`; new sweep case |\n| `force` | frame = enclosing force, category target lifts it army-wide | `query.matrix.test.js`; new sweep case |\n| `parent` | frame = carrier's parent, ahead of `shared` | `query.matrix.test.js`; new sweep case |\n| `self` | frame = carrying node | `query.matrix.test.js`, `constraints.selfScopeFrame.test.js`; new sweep case |\n| `unit` | frame = nearest ancestor incl. self with raw `type=\"unit\"` | `query.unitScope.test.js`; new sweep case (AC 5) |\n| `model-or-unit` | frame = nearest ancestor incl. self with raw `type` `model` or `unit` | new sweep cases |\n| `ancestor` | membership check over the strict ancestor chain | `query.ancestorScope.test.js`; new sweep case |\n| `primary-catalogue` | identity check on the force's army book | `query.primaryCatalogueScope.test.js`; new sweep case |\n| `primary-category` | identity check on the nearest effective primary category | new sweep cases |\n| entry / group / `forceEntry` id | frame = nearest ancestor with that definition id | `query.selfGatedInstanceOf.test.js`, `evaluator.bloodlineProfileFixture.test.js`; new sweep case |\n| category id | frame = nearest ancestor effectively carrying that category | `query.categoryScope.test.js`; new sweep case |\n| anything else | fail-closed 0 + `unresolvedScope` | `query.unitScope.test.js` (Kriterium 4); new sweep case |\n| `category` (literal) | **not** a keyword — the doc row folded the wiki's \"any Category\" (a category **id**) into a pseudo-keyword | **not coverable from the catalogue data in this repository**: neither frozen corpus contains a single `scope=\"category\"`; the doc is corrected instead, and the value is evaluated as a category id |\n\n3. *What increment 1 established* — the reported bloodline case is gated by a condition whose `scope` names the unit's own `selectionEntry` id (`6822-0110-a7c9-cbb0`, 10 occurrences in the Vampire Counts fixture), that path is correct today and is now pinned end to end by `src/evaluator/evaluator.bloodlineProfileFixture.test.js` and `src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx`; the reported symptom is therefore either already resolved or it lives on one of the scope shapes this increment covers.\n4. One line recording that `docs/testing/modifier-characteristic-value/README.md:112-127` still calls `scope=\"unit\"` and `instanceOf` \"not derivable from the format documentation\" — that rationale is stale since issue 086, but the scenario is black-box authorship (ADR 0033) and is deliberately **not** touched here.\n5. Tick acceptance criteria 5, 6, 7 and — once the closed command list is green — 9. Leave 1-4 and 8 as they are; they belong to increment 1.\n\n### Version bump\n\nThis is a user-visible fix (gated profile characteristics that never reached the display now do, in the `primary-category`/`model-or-unit` shapes). The project rule asks for a patch bump proposal before merge, and the merge is the caller's step, not this one — do not run `scripts/release.js` inside this increment.\n\n### Out of scope, deliberately\n\nThe legacy `src/roster/` solver, `SelectionConfigurator.jsx`/`upgradeDetails.jsx`/`UnitChips.jsx` (named out of scope by the issue), any scenario under `docs/testing/`, and the `category`-as-keyword reading.",
            "moduleMap": "- `src/evaluator/query.js` — the one place that resolves a scope to a frame and answers the count. Entry points: `query(ctx, field, scope, targetId, flags)`, `createQueryContext`, private `resolveFrame`/`resolveSharedFrame`/`resolvePrimaryCatalogue`/`resolveAncestor`/`nearestUnitAncestor`/`isUnitDefinition`, constant `EMPTY_EFFECTIVE`. All engine changes of this increment land here.\n- `src/evaluator/model.js` — engine vocabulary. `ScopeKeyword` (line 126) and `ScopeKind` (line 434) are the two frozen enumerations the closed scope list is reconciled against; `DiagnosticKind.UNRESOLVED_SCOPE`/`UNSUPPORTED_FIELD` are the fail-closed diagnostics used here.\n- `src/evaluator/effectiveState.js` — `EffectiveState.primaryCategoryIdOf(node)` (line 195) is the source `primary-category` reads; `countRelevantKeyOf`/`countRelevantFingerprint` (line 421/442) are the fixpoint's stability key, deliberately left unchanged. The JSDoc at line 185-190 becomes wrong with this change and is corrected.\n- `src/evaluator/modifiers.js` — applies modifiers per node and per info carrier; `conditionHolds` (line 205) calls `query` with the raw condition scope, and `applyModifiersOfNodes` (line 754) builds the query context with `effective: state` and the **selection** node, which is why an `infoLink` profile gate resolves ancestors correctly. Read-only here.\n- `src/evaluator/countIndex.js` — `targetsOf` (line 159) and `indexNodeContribution` (line 253) decide that a frame node counts under its own categories/def id in the BASE bucket (only the `null` target moves into `FRAME_OWN`). This is why an `instanceOf` against the frame's own category holds. Read-only here.\n- `src/evaluator/evalTree.js` — `allNodes`/`realNodes` (line 1100/1116) are pre-order, which is what makes the primary-category read fresh within a round. Read-only here.\n- `src/evaluator/violationClassification.js` — `classifyScope` (line 59) splits keyword from id via `Object.values(ScopeKeyword)`; adding keywords changes its classification for those two literals. Read-only, but the reason `ScopeKind` must be extended.\n- `src/i18n/violationMessages.js` — `scopeGroupOf` (line 59) maps every scope kind but `roster`/`force` to `local`; no new message key is needed.\n- `src/parser/schema/Catalogue.xsd:426` — `<xs:attribute name=\"scope\" type=\"xs:string\" use=\"required\"/>` on the shared `QueryBase`; read-only evidence that the closed list is not schema-derivable.\n- `docs/battlescribe-data-format.md` — canonical format reference; §7.6 table (line 664) and box, §7.7 table (line 811) and the `#scope-unit-ancestor` box (line 844), §13.1 row (line 1449), §15 gap table (line 1515-1517).\n- `docs/bsdata-catalogue-development-wiki/Data-structure-overview.md:334/372/377/413` — upstream scope enumerations for constraint, condition and repeat. Read-only submodule.\n- `docs/evaluator-architecture.md` — `ScopeKeyword` (line 176), `ScopeKind` (line 297), query pseudocode §4.5 (lines 536-588).\n- `docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md` — carries the derivation, the coverage table and the increment-1 record.\n- `src/components/editor/CLAUDE.md` — suite doc; the guard-assert bullet (lines 38-43) is wider than the tests in that directory actually are.\n- `src/evaluator/CLAUDE.md` — suite doc for the evaluator tests; unchanged by this increment.\n- `src/evaluator/__fixtures__/whfb6-definitive/Forces of Chaos (6th definitive edition).cat` — the four `primary-category` occurrences (12679, 13024, 13062, 13093) and the discriminating primary category links (8083 Special, 8411 Rare, 2722 Lord, 8505 Heroes). Read-only, never edited.\n- `src/evaluator/__fixtures__/whfb6-definitive/Lizardmen (6th definitive edition).cat` — the two `model-or-unit` occurrences (4939, 4967) and the Red Crested Skink category (line 10, links at 3483/4879). Read-only, never edited.\n- `src/evaluator/query.unitScope.test.js`, `query.ancestorScope.test.js`, `query.categoryScope.test.js`, `query.primaryCatalogueScope.test.js`, `query.selfGatedInstanceOf.test.js`, `query.matrix.test.js` — the existing per-scope pins the coverage table cites.\n- `src/evaluator/evaluator.bloodlineProfileFixture.test.js`, `src/components/editor/UnitSelectionCard.gatedProfileCharacteristics.test.jsx` — increment 1's pins; the report-reading pattern the new sweep file copies.\n- `src/evaluator/infoProjection.test.js`, `src/evaluator/modifierTargets.test.js` — the existing patterns for a synthetic `profileType`/`characteristicType`/`sharedProfiles`/`infoLink` catalogue.\n\n**Correction to the planner's codemap:** `src/evaluator/catalogReader.js`, `src/evaluator/countIndex.js`, `src/evaluator/fixpoint.js` and `src/evaluator/modifiers.test.js` are **not** touched. The reader already passes `scope` through verbatim (`readScope`, line 299), the index already books a frame's own categories under its own key, and the fixpoint needs no change (see the rejected alternative in the plan). `src/evaluator/model.js` and `src/components/editor/CLAUDE.md` are on the map and are touched; `src/evaluator/effectiveState.js`, `src/evaluator/violationClassification.js`, `docs/evaluator-architecture.md` and `src/parser/schema/Catalogue.xsd` are missing from it — the first two because the primary category is read there, the third because it carries the same two enumerations, the fourth as the read-only schema evidence.",
            "environment": "Node 24, dependencies already installed in the working tree; no service, no fixture download, no network. Every command runs from the repository root.\n\n- `npx vitest run src/evaluator/query.characteristicGateScopes.test.js` — the new sweep file alone, while writing it.\n- `npx vitest run src/evaluator` — the evaluator unit tests plus the manifest-driven E2E runner over `docs/testing/` (`e2e.testcatalog.test.js`, `crossCatalog.test.js`). This is the \"evaluator E2E suite\" of AC 9.\n- `npm run typecheck` — `tsc --noEmit`, checks the JSDoc types of the production JS this increment edits (tests are excluded).\n- `npm test` — `vitest run` over all of `src/` followed by `node e2e/ui.test.js` (the puppeteer app E2E). It subsumes the evaluator run; it is listed because AC 9 names it.\n\nThe closed list under \"What counts as done\" is the whole of what anyone runs; the repository's other scripts are outside it for this increment.",
            "testPlan": "### Whether\n\nTests are needed. Two scope keywords start being evaluated, and every scope value of the closed list has to be shown to reach the characteristic value of a profile.\n\n### The suite docs\n\n`src/evaluator/CLAUDE.md` exists and governs the new file — load it; it settles the JSDOM installation, the facade-only rule, the inline-synthetic-catalogue default, the `<module>.<topic>.test.js` naming, the German titles and the `KONTROLLE:` marker for cases that pin already-correct behaviour. Nothing I read contradicts it.\n\n`src/components/editor/CLAUDE.md` **does** contradict its own directory and is to be corrected as part of your step — no test file in that directory changes. Its bullet at lines 38-43 says \"Every case guard-asserts against the real report FIRST (…) before asserting on the rendered DOM\". The control case of `UnitSelectionCard.gatedProfileCharacteristics.test.jsx` (line 186, `KONTROLLE: zeigt WS \"7\" und Sv \"7\" ohne Blutlinie`) asserts on the DOM alone, with no report assertion at all, and that is right: it pins the unmodified baseline, where there is no computed assumption to guard. Reword the bullet so the directory it describes is the directory that exists — a case that asserts a value the evaluator *computed* guard-asserts against the report first; a control case that pins the untouched base value may assert on the DOM alone. Keep the existing justification sentence for the guard.\n\n### What (per criterion), and how\n\nOne new file, one framework, one command. All cases are `describe`/`it` with German titles, vitest, at the **facade** (`prepareDataset` + `evaluate` from `./evaluator.js`) — the rule under test is \"the gate decides the characteristic value that reaches the report\", and only the report shows a characteristic.\n\n**File:** `src/evaluator/query.characteristicGateScopes.test.js`\n**Command:** `npx vitest run src/evaluator/query.characteristicGateScopes.test.js`\n\n**The one synthetic dataset.** A `gameSystem` with a cost type and a `profileType` carrying one `characteristicType` (e.g. WS), and one catalogue that declares a `forceEntry`, three `categoryEntries` — RARE, ELITE, SKIRMISHER — a `sharedProfile` P with WS base `3`, and this entry tree:\n\n```\nforceEntry F\n└─ Regiment            type=\"unit\"    categoryLinks: RARE primary=\"true\", ELITE primary=\"false\"\n   ├─ Trooper          type=\"model\"   categoryLink: SKIRMISHER      (2 instances in the roster)\n   │  └─ Crest         type=\"upgrade\" infoLink → P, carries the gate of the model-or-unit cases\n   └─ Banner           type=\"upgrade\" infoLink → P, carries the gate of every other case\n      └─ Charm         type=\"upgrade\"\n```\n\nThe gate is always the same shape as the reported bug: `<infoLink id=… type=\"profile\" targetId=P><modifiers><modifier type=\"increment\" field=\"<WS characteristicType id>\" value=\"2\"><conditions>…</conditions></modifier></modifiers></infoLink>`. Vary **only** the condition XML through a catalogue factory (`catalogueWithGate(conditionXml)` — the `catalogueWith` pattern of `offer.hiddenGate.test.js`). Expected WS is the string `'5'` when the gate holds and `'3'` when it does not; `characteristic.value` is a string in the report (`evaluator.bloodlineProfileFixture.test.js`).\n\nA second factory variant places the gate on `Crest` instead of `Banner` for the model-or-unit cases. `infoProjection.test.js` and `modifierTargets.test.js` show the synthetic `profileTypes`/`characteristicTypes`/`sharedProfiles` skeleton.\n\n**Reading the report.** `report.capabilities` is a `Map` of path → capability. Do not hard-code a path: pick the single entry whose `defId` is the gate carrier and whose `anchorKind` is `AnchorKind.OCCUPIED`, assert there is exactly one, then take the `infoElements` entry with `kind === InfoElementKind.PROFILE` and the infoLink's id, then the characteristic by `name`. `evaluator.bloodlineProfileFixture.test.js` (`vampireCountSlot`/`profileEntryOf`/`characteristicValue`) is the pattern; adapt it to look up by `defId`.\n\n**Cases — one per scope value of the closed list, each a hit and a miss.** The miss is what proves the gate was evaluated rather than always-on; the hit proves it was not dropped.\n\n1. `roster` — `atLeast 1 selections scope=\"roster\" childId=<Trooper id>` → `'5'`. Miss: `childId=<Charm id>` with a roster that has no Charm → `'3'`.\n2. `force` — same pair with `scope=\"force\"`.\n3. `parent` — `atLeast 1 … scope=\"parent\" childId=<Trooper id>` at the Banner (frame = Regiment) → `'5'`. Miss: `childId=<Charm id>`, which hangs under the Banner and not under the Regiment, with `includeChildSelections=\"false\"` → `'3'`.\n4. `self` — `atLeast 1 … scope=\"self\" childId=<Charm id>` at the Banner → `'5'`. Miss: `childId=<Trooper id>` → `'3'`.\n5. `unit` (**AC 5**) — `instanceOf … scope=\"unit\" childId=<ELITE id>` at the Banner → `'5'`, the enclosing unit carries ELITE. Miss: `childId=<SKIRMISHER id>` with `includeChildSelections=\"false\"`… **do not** use SKIRMISHER here, the Trooper is a direct child of the frame and counts in the BASE bucket regardless of the flag; use a category id declared in the catalogue but carried by nobody → `'3'`. Add the fail-closed edge: the same condition on a carrier that has no enclosing unit (a `Banner` placed directly under the force in the roster) → `'3'` **and** a `unresolvedScope` diagnostic with `scope: 'unit'`.\n6. `model-or-unit` (**new**) — three cases, on the `Crest` carrier:\n   - hit: `instanceOf … scope=\"model-or-unit\" childId=<SKIRMISHER id>` → `'5'` (the enclosing **model** carries it);\n   - miss: `instanceOf … scope=\"model-or-unit\" childId=<ELITE id>` → `'3'` (ELITE sits on the unit above the model, outside the frame);\n   - the discriminating control against `unit`: the identical condition with `scope=\"unit\"` on the same carrier → `'5'`. These two together are the whole point: same condition, same carrier, different frame, opposite outcome.\n   - fail-closed edge: the same `model-or-unit` condition on a carrier with neither model nor unit above it (an upgrade directly under the force) → `'3'` **and** a `unresolvedScope` diagnostic with `scope: 'model-or-unit'`.\n7. `ancestor` — `instanceOf … scope=\"ancestor\" childId=<ELITE id>` at the Banner → `'5'`. Miss: `childId=<SKIRMISHER id>` — the Trooper carries it but is no ancestor of the Banner → `'3'`.\n8. `primary-catalogue` — `instanceOf … scope=\"primary-catalogue\" childId=<the catalogue's own id>` → `'5'`. Miss: an id no catalogue of the dataset carries → `'3'` (a documented plain non-hit, not a data error; no diagnostic is expected — assert its absence).\n9. `primary-category` (**new**) — three cases on the Banner:\n   - hit: `instanceOf … scope=\"primary-category\" childId=<RARE id>` → `'5'` (RARE is the Regiment's `primary=\"true\"` link, and the resolution climbs from the Banner past the Crest-less chain to it);\n   - miss, and this is the sharp one: `childId=<ELITE id>` → `'3'` — the Regiment **carries** ELITE but it is not primary, so \"primary category\" must not degrade into \"any category\";\n   - fail-closed edge: the same condition where no node in the chain carries a primary category (drop the `primary=\"true\"` attribute in a catalogue variant) → `'3'` **and** a `unresolvedScope` diagnostic with `scope: 'primary-category'`.\n   - one more edge, cheap and worth it: the same hit condition with `shared=\"false\"` → still `'5'`; a primary category is not made narrower by an instance.\n10. entry id as scope — `instanceOf … scope=\"<Regiment entry id>\" childId=\"any\"` at the Banner → `'5'`; this is the shape the reported bloodline bug uses. Miss: `scope=\"<Trooper entry id>\"`, a real entry that is not an ancestor of the Banner → `'3'` with a `unresolvedScope` diagnostic.\n11. category id as scope — `atLeast 1 selections scope=\"<ELITE id>\" childId=\"model\"` at the Banner → `'5'` (nearest ancestor carrying ELITE is the Regiment, which holds two models). Miss: `scope=\"<SKIRMISHER id>\"` → `'3'` with a `unresolvedScope` diagnostic.\n12. anything else — `scope=\"flock-of-seagulls\"` → `'3'` with a `unresolvedScope` diagnostic. Marked `KONTROLLE:`; `query.unitScope.test.js` pins the same rule at the primitive.\n\nCases 1-4, 5-hit, 7, 8, 10 and 12 pin behaviour that is already correct and are marked `KONTROLLE:` in their titles; cases 6 and 9 are the ones expected red before the production change.\n\n**Deliberately not tested, and why:**\n- The `category` literal as a scope keyword — no catalogue in this repository writes it, and the plan resolves it as a doc artefact rather than as engine behaviour. Recorded in `issue.md` instead (AC 7).\n- The phantom/offer-anchor exemption for `model-or-unit` (no diagnostic at a synthetic anchor without an enclosing model or unit). It mirrors a rule already decided and pinned for `unit` in issue 086, and constructing an anchor without an enclosing unit through the facade costs more than the noise-suppression rule is worth.\n- Cross-round propagation of a `set-primary` into a `primary-category` gate. The plan shows it cannot occur (pre-order traversal, frame is self-or-ancestor); a test for it could not fail and would pin nothing.\n- Anything under `docs/testing/` — scenario authorship belongs to the `e2e-testcase-author` (ADR 0033).\n- `src/roster/` legacy paths and the three display paths the issue puts out of scope.\n\n### What counts as done\n\nRun exactly these, from the repository root, in this order:\n\n1. `npx vitest run src/evaluator`\n2. `npm run typecheck`\n3. `npm test`\n\nNothing else. No linter, no `knip`, no `depcruise`, no screenshot run — this increment changes no UI.\n\n### What is already red\n\nI ran nothing, deliberately: no command would have told me anything I could not read. From reading: the tree is green as increment 1 left it, and the only expected red is the new file's cases 6 and 9 before `query.js` changes.\n\nTwo behaviour changes to watch when the list is first run, both on real catalogue data. Making `primary-category` resolve blanks four characteristics of the `Juggernaut of Khorne` profile inside Rare-primary carriers of `Forces of Chaos`, and making `model-or-unit` resolve un-hides `Skavenpelt` and `Sign of Sotek` in `Lizardmen` wherever a Red Crested Skink is present. The two E2E scenarios that load those catalogues (`docs/testing/force-repeat-bloodletters-flesh-hound-slots`, `docs/testing/greater-than-self-blessed-spawning-slots`) are about slot counting on Bloodletters and Saurus Warriors and touch neither entry, so I expect them green. If one of them, or a real-fixture unit test, turns red, that is a genuine behaviour change to report and record — not something to silence by narrowing the new scopes.",
            "checks": [
              "npx vitest run src/evaluator",
              "npm run typecheck",
              "npm test"
            ],
            "questions": [],
            "summary": "Researched the scope sweep for issue 81, increment 2: the closed condition-scope list derived from the XSD (which types `scope` as a bare string and so constrains nothing), the BSData wiki and docs/battlescribe-data-format.md, cross-checked against every `scope=` occurrence in both frozen corpora — nine keywords plus the id case. Two of them are dropped today, `primary-category` (4x Forces of Chaos, gating exactly this issue's shape: characteristic `set` modifiers on a profile infoLink) and `model-or-unit` (2x Lizardmen); the plan adds both to `query.js`/`model.js` with data-derived semantics, corrects the doc's phantom `category` keyword, and puts the per-scope coverage table into the issue file."
          },
          "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on its own branch: `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2`, off `claude/profilwert-modifikatoren-bloodlines-zlw2p9`.\nBefore anything else, on `claude/profilwert-modifikatoren-bloodlines-zlw2p9`: record the name with the backlog helper's `branch` subcommand, as `branch docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2`, commit that state change and push `claude/profilwert-modifikatoren-bloodlines-zlw2p9`. Then create the branch with `git checkout -b claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2`, push it with `-u`, and work on it from there.\nThis run works docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md one increment at a time, and increment 2 is yours:\n  Every condition scope on a characteristic modifier is evaluated or recorded as uncoverable — The closed list of scope values a condition on a characteristic modifier may name is derived from the format documentation and the XSD, every value in it is evaluated against the node it names rather than silently dropped, and every value is either covered by a test or recorded in the issue file with the reason it cannot be covered from the catalogue data in the repository.\nWhat it has to satisfy:\n  - A gated modifier whose condition uses scope=\"unit\" is evaluated against the unit, not silently dropped (issue AC 5).\n  - Every scope a condition on a characteristic modifier may name is evaluated against the node it names, and none is silently dropped; the closed list of scope values is derived from docs/battlescribe-data-format.md and src/parser/schema/Catalogue.xsd rather than from the existing code, and the derivation is written down (issue AC 6).\n  - Each scope value in that closed list is either covered by a test or listed in docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md with the reason it cannot be covered from the catalogue data present in the repository (issue AC 7).\n  - The issue file also records what increment 1 established: the reported bloodline case is gated by a scope naming the unit's own selectionEntry id, that path is correct today and is now pinned, so the reported symptom is either already resolved or lives on a scope shape this increment covers.\n  - npm test passes and the evaluator E2E suite passes (issue AC 9).\n  - src/components/editor/CLAUDE.md describes the guard-assert convention as the test files in that directory actually follow it - the control case of UnitSelectionCard.gatedProfileCharacteristics.test.jsx asserts on the DOM alone, which the current wording does not admit.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe planner's codemap is in the run state: read it with the backlog helper's `codemap` subcommand, as `codemap docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json`, before you begin.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 research:inc-2.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 research:inc-2.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `needsTests`\n  - `plan`\n  - `moduleMap`\n  - `environment`\n  - `testPlan`\n  - `checks`\n  - `questions`\n  - `summary`\n"
        },
        {
          "label": "tests:inc-2.0",
          "at": "2026-08-12T15:10:38.638Z",
          "return": {
            "cases": [
              {
                "case": "scope=\"roster\" — hit: atLeast 1 selections scope=roster childId=<Trooper> -> '5'; miss: childId=<Charm> with no Charm in roster -> '3'.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"roster\": zaehlt armeeweit ueber alle Kontingente",
                "demands": "The engine evaluates a scope=\"roster\" condition on a characteristic modifier's infoLink against the whole roster and reports the incremented WS ('5') when it holds, the base WS ('3') when it does not.",
                "got": "Both cases pass already (KONTROLLE): 'KONTROLLE: haelt WS bei \"5\"...' and the miss case both green in the current tree — increment 1's condition-evaluation fix already carries this scope through."
              },
              {
                "case": "scope=\"force\" — same hit/miss pair as roster, scoped to the own contingent.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"force\": zaehlt innerhalb des eigenen Kontingents",
                "demands": "A scope=\"force\" condition is evaluated against the enclosing forceEntry's own selections, not the whole roster.",
                "got": "Both cases pass already (KONTROLLE)."
              },
              {
                "case": "scope=\"parent\" — hit: direct sibling under the same parent; miss: a grandchild under the carrier itself, includeChildSelections=\"false\".",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"parent\": zaehlt die direkten Kinder des uebergeordneten Knotens",
                "demands": "A scope=\"parent\" condition counts only the direct children of the carrier's own parent.",
                "got": "Both cases pass already (KONTROLLE)."
              },
              {
                "case": "scope=\"self\" — hit: a child hanging directly under the carrier; miss: a node that is not the carrier's own child.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"self\": zaehlt die eigenen Kinder des Traegers",
                "demands": "A scope=\"self\" condition counts only the carrier's own direct children.",
                "got": "Both cases pass already (KONTROLLE)."
              },
              {
                "case": "scope=\"unit\" (issue AC 5) — hit: instanceOf against a category the enclosing unit carries -> '5'; miss: a category nobody carries -> '3'; fail-closed edge: carrier with no enclosing unit -> '3' + unresolvedScope.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"unit\": zaehlt gegen die umschliessende Einheit (Kriterium 5 der Meldung)",
                "demands": "A gated characteristic modifier whose condition uses scope=\"unit\" is evaluated against the enclosing unit, not silently dropped, and fails closed with a diagnostic when there is no enclosing unit.",
                "got": "All three cases pass already (hit marked KONTROLLE; miss and the fail-closed edge pass too) — this is AC 5, already satisfied by increment 1's general condition-evaluation fix plus the pre-existing 'unit' scope resolution query.unitScope.test.js pins at the primitive."
              },
              {
                "case": "scope=\"model-or-unit\" (new) — hit: nearest enclosing MODEL carries the category -> '5'; miss: only the unit above the model carries it -> '3'; control: same condition+carrier with scope=\"unit\" instead -> '5' (opposite of the model-or-unit miss); fail-closed edge: carrier with neither model nor unit above it -> '3' + unresolvedScope.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"model-or-unit\": zaehlt gegen das naechste Modell ODER die naechste Einheit (neu)",
                "demands": "scope=\"model-or-unit\" resolves to the nearest model-or-unit-type ancestor (self included), distinct from scope=\"unit\"'s unit-only frame, and fails closed with a diagnostic when neither exists.",
                "got": "Confirmed RED as planned: 'haelt WS bei \"5\", wenn das umschliessende MODELL die gesuchte Kategorie traegt' fails with `AssertionError: expected '3' to be '5'` (query.characteristicGateScopes.test.js:359) — the engine does not yet resolve this scope, so the condition never holds and the WS stays at base. The miss, the scope=\"unit\" control and the fail-closed edge already pass (they only exercise the existing 'unit' resolution)."
              },
              {
                "case": "scope=\"ancestor\" — hit: any strict ancestor carries the category; miss: only a sibling carries it (not an ancestor).",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"ancestor\": prueft die Vorfahrenkette (Mitgliedschaft, kein Zaehlrahmen)",
                "demands": "scope=\"ancestor\" is a membership check over the strict ancestor chain, not a counting frame.",
                "got": "Both cases pass already (KONTROLLE)."
              },
              {
                "case": "scope=\"primary-catalogue\" — hit: childId names the catalogue's own root id; miss: childId names a catalogue not loaded in the dataset -> plain non-hit, no diagnostic expected.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"primary-catalogue\": Identitaetspruefung gegen das Armeebuch des Kontingents",
                "demands": "scope=\"primary-catalogue\" is an identity check against the enclosing force's catalogue root id; an unloaded catalogue id is a quiet non-hit, not a diagnosed data error.",
                "got": "Both cases pass already (KONTROLLE); the miss case's absence-of-diagnostic assertion (`unresolvedScopeOf(report, 'primary-catalogue')` equals `[]`) holds."
              },
              {
                "case": "scope=\"primary-category\" (new) — hit: childId names the enclosing unit's primary categoryLink -> '5'; miss: childId names a category the unit carries but not as primary -> '3'; fail-closed edge: no node in the chain carries any primary category -> '3' + unresolvedScope; extra edge: same hit condition with shared=\"false\" still '5'.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "scope=\"primary-category\": Identitaetspruefung gegen die PRIMAERE Kategorie der Einheit (neu)",
                "demands": "scope=\"primary-category\" is an identity check against the nearest ancestor's primary categoryLink; carrying a category non-primarily must not count as a hit, and shared=\"false\" must not narrow a primary category.",
                "got": "Confirmed RED as planned on the hit and the shared=\"false\" edge: both fail with `AssertionError: expected '3' to be '5'` (query.characteristicGateScopes.test.js:439 and :470) — the engine does not yet resolve this scope. The miss and the fail-closed edge already pass (both only need the existing unresolvedScope fallback, which already fires for an unrecognised scope keyword)."
              },
              {
                "case": "Entry id as scope — hit: scope=<Regiment's own selectionEntry id> childId=\"any\" (the shape the reported bloodline bug uses) -> '5'; miss: scope=<a real entry id that is not an ancestor of the carrier> -> '3' + unresolvedScope.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "Eintrags-Id als scope: die \"selbst-gegatete\" Form, verallgemeinert auf selectionEntry-Ids",
                "demands": "A scope naming a real selectionEntry id resolves as an ancestor-membership check (the generalised self-gated form); an id that is real but not an ancestor of the carrier fails closed with a diagnostic rather than silently returning false.",
                "got": "Both cases pass already (KONTROLLE) — this pins the path increment 1 established for the reported bloodline case."
              },
              {
                "case": "Category id as scope — hit: atLeast 1 selections scope=<ELITE id> childId=\"model\" -> '5' (nearest ancestor carrying ELITE becomes the frame, its 2 models satisfy atLeast 1); miss: scope=<SKIRMISHER id>, which no ancestor carries -> '3' + unresolvedScope.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "Kategorie-Id als scope",
                "demands": "A scope naming a real categoryEntry id resolves to the nearest ancestor that carries it as the counting frame; failing to find such an ancestor fails closed with a diagnostic.",
                "got": "Both cases pass already (KONTROLLE)."
              },
              {
                "case": "Anything else — scope=\"flock-of-seagulls\" -> '3' + unresolvedScope; mirrors query.unitScope.test.js's Kriterium 4 at the primitive.",
                "file": "src/evaluator/query.characteristicGateScopes.test.js",
                "testName": "ein frei erfundenes Schluesselwort bleibt fail-closed diagnostiziert",
                "demands": "An unrecognised scope keyword that resolves to no real id fails closed with an unresolvedScope diagnostic rather than being silently dropped.",
                "got": "Passes already (KONTROLLE)."
              },
              {
                "case": "scope=\"category\" (the literal keyword) — the closed list's remaining member; the test plan marks it deliberately untested because no catalogue in this repository writes it, and records it as a doc artefact rather than engine behaviour to be pinned in issue.md (AC 7).",
                "file": "",
                "testName": "",
                "demands": "",
                "got": "Not written, per the plan: 'no catalogue in this repository writes it, and the plan resolves it as a doc artefact rather than as engine behaviour. Recorded in issue.md instead (AC 7).' Recording it in issue.md is outside this step's boundary (test files and the suite doc only) — the implementer step should add this entry to issue.md to satisfy AC 7."
              }
            ],
            "openQuestions": [
              "AC 7 requires the untestable 'category' scope keyword to be recorded in issue.md with its reason; the tests role may not edit issue.md, and the researcher's plan does not name who does — the implementer step should add this entry (text is spelled out above and in the new test file's header comment) before the increment can be called complete against AC 7.",
              "The issue file's required record of what increment 1 established (the bloodline case's gate is a scope naming the unit's own selectionEntry id, correct today and pinned) is outside this step's boundary too — the new KONTROLLE case 'Eintrags-Id als scope' pins that path, but writing the record into issue.md itself is implementer work."
            ],
            "questions": [],
            "summary": "Added src/evaluator/query.characteristicGateScopes.test.js with 28 cases covering all 12 scope-value groups the test plan named (roster, force, parent, self, unit — AC 5 —, model-or-unit, ancestor, primary-catalogue, primary-category, entry-id-as-scope, category-id-as-scope, and the unresolved-keyword fallback), derived the closed scope list from docs/battlescribe-data-format.md line 1449 and Catalogue.xsd in the file's header comment, and reworded the guard-assert bullet in src/components/editor/CLAUDE.md to admit a DOM-only control case. 25 of 28 cases pass already (regression guards on scopes increment 1's fix already carries); the 3 that fail are exactly the two new scopes the plan called out as expected red (model-or-unit hit, primary-category hit and its shared=\"false\" edge), each failing with `AssertionError: expected '3' to be '5'` — a missing-behaviour failure, not an import or typo error. `npx vitest run src/evaluator/query.characteristicGateScopes.test.js`: 28 cases, 3 failed, exit 1. `npx vitest run src/evaluator`: 94 files/1722 tests, 3 failed (same 3), exit 1. `npm run typecheck`: clean, exit 0. `npm test`: 300 files/3784 tests, 3 failed (same 3), exit 1 — the puppeteer e2e/ui.test.js did not run because the npm script short-circuits (`vitest run && node e2e/ui.test.js`) on the vitest failure; that failure is the expected red this step exists to prove."
          },
          "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on branch `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2`. Before anything else make sure you are on it: `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2` and check it out; if the remote does not have it, create it from `claude/profilwert-modifikatoren-bloodlines-zlw2p9` and push it with `-u`.\nThis run works docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md one increment at a time, and increment 2 is yours:\n  Every condition scope on a characteristic modifier is evaluated or recorded as uncoverable — The closed list of scope values a condition on a characteristic modifier may name is derived from the format documentation and the XSD, every value in it is evaluated against the node it names rather than silently dropped, and every value is either covered by a test or recorded in the issue file with the reason it cannot be covered from the catalogue data in the repository.\nWhat it has to satisfy:\n  - A gated modifier whose condition uses scope=\"unit\" is evaluated against the unit, not silently dropped (issue AC 5).\n  - Every scope a condition on a characteristic modifier may name is evaluated against the node it names, and none is silently dropped; the closed list of scope values is derived from docs/battlescribe-data-format.md and src/parser/schema/Catalogue.xsd rather than from the existing code, and the derivation is written down (issue AC 6).\n  - Each scope value in that closed list is either covered by a test or listed in docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md with the reason it cannot be covered from the catalogue data present in the repository (issue AC 7).\n  - The issue file also records what increment 1 established: the reported bloodline case is gated by a scope naming the unit's own selectionEntry id, that path is correct today and is now pinned, so the reported symptom is either already resolved or lives on a scope shape this increment covers.\n  - npm test passes and the evaluator E2E suite passes (issue AC 9).\n  - src/components/editor/CLAUDE.md describes the guard-assert convention as the test files in that directory actually follow it - the control case of UnitSelectionCard.gatedProfileCharacteristics.test.jsx asserts on the DOM alone, which the current wording does not admit.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nYour work order is the researcher's test plan. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 research:inc-2.0 --fields testPlan` — the test plan.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 tests:inc-2.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 tests:inc-2.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `cases`\n  - `openQuestions`\n  - `questions`\n  - `summary`\n"
        },
        {
          "label": "implement:inc-2.0",
          "at": "2026-08-12T15:27:28.593Z",
          "return": {
            "deviations": [
              {
                "planned": "The plan's last documentation item asks for a record in `docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md` (AC 7): the four-step derivation of the closed scope list, the per-scope coverage table, what increment 1 established, the note on the stale `docs/testing/modifier-characteristic-value/README.md:112-127` rationale, and ticking acceptance criteria 5, 6, 7 and 9.",
                "did": "Not done. My role is denied read access to `issue.md` (the read was refused with `the implementer may not read ... agents/implementer.md says so`), and an edit requires reading the file first, so I can neither place the sections at the position the plan names nor tick criteria whose current text I cannot see. Everything the record was to contain is written where I may write it: the derivation and the two new scopes' data evidence are now in `docs/battlescribe-data-format.md` (the new box `#scope-primary-category-model-or-unit`, the corrected §7.6/§7.7 scope rows, the corrected §13.1 row, the new §15 gap row), and the closed list is mirrored in `docs/evaluator-architecture.md`. The issue-file record is reported as a blocker for whoever may write that file.",
                "why": "A role boundary, not a judgement about the plan: the plan asks for an edit to a file this role is not allowed to open."
              },
              {
                "planned": "`SYNTHETIC_EXEMPT_SCOPES` as a plain frozen `Set` of the two keywords.",
                "did": "Same set, annotated `/** @type {ReadonlySet<string>} */`.",
                "why": "Without the annotation `tsc --noEmit` infers `Set<'unit' | 'model-or-unit'>` and rejects `.has(scope)` for the `string`-typed `scope` parameter (TS2345, `src/evaluator/query.js:546`). Behaviour is unchanged; the annotation only widens the declared element type."
              }
            ],
            "commands": [
              {
                "command": "npx vitest run src/evaluator",
                "exitCode": 0,
                "note": "94 test files, 1722 tests, all passed. Includes the increment's new sweep file `src/evaluator/query.characteristicGateScopes.test.js` (28 tests) and the manifest-driven E2E runner over `docs/testing/` (`e2e.testcatalog.test.js`, `crossCatalog.test.js`). Nothing skipped or excluded."
              },
              {
                "command": "npm run typecheck",
                "exitCode": 0,
                "note": "`tsc --noEmit` over the production JS (tests excluded per the project's tsconfig). Failed once with TS2345 on the new `SYNTHETIC_EXEMPT_SCOPES` set; fixed by the type annotation recorded under deviations, then clean."
              },
              {
                "command": "npm test",
                "exitCode": 0,
                "note": "`vitest run` over all of `src/` — 300 test files, 3784 tests, all passed — followed by the puppeteer app E2E `node e2e/ui.test.js`, which printed ALL UI TESTS PASSED SUCCESSFULLY. Nothing skipped or excluded."
              }
            ],
            "blockers": [
              "AC 7's record in `docs/issues/81-.../issue.md` is unwritten: the implementer role is denied read access to `issue.md`, and no edit is possible without reading it — a role that may write that file has to add the derivation, the coverage table, the increment-1 record and the ticks for criteria 5, 6, 7, 9.",
              "The version bump the project rule asks for before merge (a patch bump — this is a user-visible fix) is deliberately not run here; the plan assigns it to the merging caller."
            ],
            "questions": [],
            "summary": "The evaluator now resolves the two condition scopes real catalogue data uses and the engine silently dropped: `model-or-unit` as a counting frame (nearest ancestor, self included, of raw type `model` or `unit`, sharing one implementation with `unit`) and `primary-category` as a flag-independent identity check against the nearest ancestor's primary category, both fail-closed with `unresolvedScope` where nothing resolves; the closed nine-keyword scope list plus the id case is written up in `docs/battlescribe-data-format.md` and mirrored in `docs/evaluator-architecture.md`, and the doc's phantom `scope=\"category\"` keyword is corrected to the category-id case. All 28 planned sweep cases pass, and `npx vitest run src/evaluator`, `npm run typecheck` and `npm test` are all exit 0; the issue-file record for AC 7 is left as a blocker because this role may not read `issue.md`."
          },
          "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on branch `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2`. Before anything else make sure you are on it: `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2` and check it out; if the remote does not have it, create it from `claude/profilwert-modifikatoren-bloodlines-zlw2p9` and push it with `-u`.\nYour brief is the plan the researcher wrote and the tests that already exist. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 research:inc-2.0 --fields plan,moduleMap,environment` — what gets built and why, the files it touches, and the commands the plan asks anyone to run.\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 tests:inc-2.0 --fields cases` — the cases the test-author wrote.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\n  - `npm run typecheck`\n  - `npm test`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 implement:inc-2.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 implement:inc-2.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `summary`\n"
        },
        {
          "label": "review:inc-2.0",
          "at": "2026-08-12T15:35:25.020Z",
          "return": {
            "findings": [
              {
                "claim": "The issue file records nothing of what increment 1 established about the reported bloodline gate — it was never touched by this increment.",
                "reproduction": "Read docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md whole at ef034a4 (63 lines). Its Description describes the bloodline gate as a `condition type=\"instanceOf\"` on the Blood Dragon category (lines 22-24), and no line anywhere in the file says that the reported case is gated by a scope naming the unit's own selectionEntry id, that this path is correct today, that it is now pinned, or that the reported symptom is therefore either already resolved or lives on a scope shape increment 2 covers. `git diff claude/profilwert-modifikatoren-bloodlines-zlw2p9...HEAD --stat` lists eight files (docs/battlescribe-data-format.md, docs/evaluator-architecture.md, backlog.json, src/components/editor/CLAUDE.md, src/evaluator/effectiveState.js, src/evaluator/model.js, src/evaluator/query.characteristicGateScopes.test.js, src/evaluator/query.js) and issue.md is not among them.",
                "criterion": "Increment criterion 4: 'The issue file also records what increment 1 established: the reported bloodline case is gated by a scope naming the unit's own selectionEntry id, that path is correct today and is now pinned, so the reported symptom is either already resolved or lives on a scope shape this increment covers.'",
                "fix": "Add that record to docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md — a bullet under `## Comments` stating: increment 1 established that the reported bloodline case is gated by a scope naming the unit's own selectionEntry id, that this path is correct today and is now pinned by a test on the path the user sees, so the reported symptom is either already resolved or lives on one of the scope shapes increment 2 covers.",
                "fix_kind": "direct"
              },
              {
                "claim": "The written derivation of the closed scope list in the new test file contradicts the derivation this same increment wrote into the format documentation, and points at an issue-file record that does not exist.",
                "reproduction": "src/evaluator/query.characteristicGateScopes.test.js:11-16 quotes the closed list as `parent`, `roster`, `force`, `category`, `self`, `unit`, `ancestor`, `primary-catalogue`, `primary-category`, `model-or-unit`, citing 'docs/battlescribe-data-format.md ... Zeile 1449'. Lines 29-30 and 44-45 then declare the keyword `category` deliberately uncovered and refer the reader to '`issue.md`, Kriterium 7'. In the same diff, docs/battlescribe-data-format.md's Enum-Werte row for `constraint`/`condition`/`repeat` -> `scope` (now around line 1488, was 1449) states the opposite: 'Neun Schluesselwoerter - parent, roster, force, self, unit, ancestor, primary-catalogue, primary-category und model-or-unit ... Ein Literal `category` gibt es nicht', and the new §7.7 condition-scope row likewise omits `category`. The data agrees with the document: `grep -rho 'scope=\"category\"' src/__fixtures__ src/evaluator/__fixtures__ | wc -l` returns 0 (`model-or-unit` returns 2, `primary-category` returns 4). And issue.md carries no 'Kriterium 7' record of any kind, so the pointer resolves to nothing.",
                "criterion": "Increment criterion 2 (issue AC 6, 'the derivation is written down') and criterion 3 (issue AC 7, 'either covered by a test or listed in issue.md with the reason').",
                "fix": "In the header comment of src/evaluator/query.characteristicGateScopes.test.js: drop `category` from the quoted enumeration at lines 13-16, correct the cited line number to the row's current line, and delete both claims that `category` is a deliberately uncovered scope value recorded in issue.md (lines 29-30 and 44-45). The increment's own derivation puts `category` outside the closed list of keywords, and the existing case 'Kategorie-Id als scope' already covers the category-id form. With `category` out, all nine keywords plus the entry-id, category-id and unknown-keyword forms have a test, so AC 7 needs no uncoverable-value entry in the issue file.",
                "fix_kind": "direct"
              }
            ],
            "findingCount": 2,
            "allDirect": true,
            "reason": "The issue file was never touched: it still records nothing of what increment 1 established about the reported bloodline gate, and the new scope test file points at a 'Kriterium 7' record in it that does not exist, while listing `category` as a keyword of the closed scope list that the increment's own change to the format documentation says does not exist.",
            "questions": [],
            "summary": "All three listed commands are green on the increment branch: `npx vitest run src/evaluator` (94 files, 1722 tests, exit 0), `npm run typecheck` (exit 0) and `npm test` (300 vitest files, 3784 tests, plus the puppeteer app E2E, exit 0). The nine scope keywords plus the entry-id, category-id and unknown-keyword forms each have a hit case and a miss case reached through the evaluator facade, and `model-or-unit` and `primary-category` are newly resolved rather than dropped. One sandbox probe settled the one doubt the diff itself raises - whether a `set-primary` firing during evaluation reaches a `primary-category` gate given that `countRelevantFingerprint` omits the primary category: it does (primary flips from Rare to Elite, the gated WS goes 3 -> 5, no diagnostic), so that omission is not a defect. Blast radius beyond the criteria: `classifyScope` in violationClassification.js now labels a constraint scope `model-or-unit`/`primary-category` as its own kind instead of ENTRY_ID/CATEGORY_ID, and no production code outside that module reads a ScopeKind, so nothing found. Two findings, both about records the issue file does not carry."
          },
          "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on branch `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2`. Before anything else make sure you are on it: `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2` and check it out; if the remote does not have it, create it from `claude/profilwert-modifikatoren-bloodlines-zlw2p9` and push it with `-u`.\nReview round 0 of increment 2. The increment's diff is its branch against the merge-base with `claude/profilwert-modifikatoren-bloodlines-zlw2p9`: judge `git diff claude/profilwert-modifikatoren-bloodlines-zlw2p9...HEAD` (three dots), whole, and nothing outside it.\nThis run works docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md one increment at a time, and increment 2 is yours:\n  Every condition scope on a characteristic modifier is evaluated or recorded as uncoverable — The closed list of scope values a condition on a characteristic modifier may name is derived from the format documentation and the XSD, every value in it is evaluated against the node it names rather than silently dropped, and every value is either covered by a test or recorded in the issue file with the reason it cannot be covered from the catalogue data in the repository.\nWhat it has to satisfy:\n  - A gated modifier whose condition uses scope=\"unit\" is evaluated against the unit, not silently dropped (issue AC 5).\n  - Every scope a condition on a characteristic modifier may name is evaluated against the node it names, and none is silently dropped; the closed list of scope values is derived from docs/battlescribe-data-format.md and src/parser/schema/Catalogue.xsd rather than from the existing code, and the derivation is written down (issue AC 6).\n  - Each scope value in that closed list is either covered by a test or listed in docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md with the reason it cannot be covered from the catalogue data present in the repository (issue AC 7).\n  - The issue file also records what increment 1 established: the reported bloodline case is gated by a scope naming the unit's own selectionEntry id, that path is correct today and is now pinned, so the reported symptom is either already resolved or lives on a scope shape this increment covers.\n  - npm test passes and the evaluator E2E suite passes (issue AC 9).\n  - src/components/editor/CLAUDE.md describes the guard-assert convention as the test files in that directory actually follow it - the control case of UnitSelectionCard.gatedProfileCharacteristics.test.jsx asserts on the DOM alone, which the current wording does not admit.\nEvery other increment is settled; this is the last one, so the issue is complete once yours is.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\n  - `npm run typecheck`\n  - `npm test`\nRead nothing out of docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json — not with the helper, not by hand.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 review:inc-2.0 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 review:inc-2.0 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `findings`\n  - `findingCount`\n  - `allDirect`\n  - `reason`\n  - `questions`\n  - `summary`\n"
        },
        {
          "label": "implement:inc-2.1",
          "at": "2026-08-12T15:43:20.000Z",
          "return": {
            "deviations": [],
            "commands": [
              {
                "command": "npx vitest run src/evaluator",
                "exit": 0,
                "note": "94 test files, 1722 tests, all passed."
              },
              {
                "command": "npm run typecheck",
                "exit": 0,
                "note": "tsc --noEmit clean."
              },
              {
                "command": "npm test",
                "exit": 0,
                "note": "Full vitest run plus the puppeteer app E2E in e2e/; all passed."
              }
            ],
            "blockers": [],
            "questions": [],
            "summary": "Direct-fix round 1 for issue 81 increment 2: issue.md now records under ## Comments what increment 1 established about the reported bloodline gate, and the header comment of query.characteristicGateScopes.test.js no longer claims `category` is a scope keyword or points at a non-existent issue-file record. All three commands that count exit 0."
          },
          "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nThis increment is worked on branch `claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2`. Before anything else make sure you are on it: `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-2` and check it out; if the remote does not have it, create it from `claude/profilwert-modifikatoren-bloodlines-zlw2p9` and push it with `-u`.\nThis is correction loop 1 of 2 for this increment, and it is a direct-fix round: nobody planned it and nobody wrote a test for it, so the findings are your whole brief. It is in the run state, and these reads are how you get it, with the `steps` subcommand of the backlog helper your shared brief names:\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 review:inc-2.0 --fields findings` — the findings you correct.\nThe commands that count for this increment:\n  - `npx vitest run src/evaluator`\n  - `npm run typecheck`\n  - `npm test`\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 implement:inc-2.1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-2 implement:inc-2.1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `deviations`\n  - `commands`\n  - `blockers`\n  - `questions`\n  - `summary`\n"
        }
      ],
      "attempts": []
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-12T13:46:20.696Z",
        "return": {
          "questions": [],
          "summary": "Issue 81 is cut into two increments: inc-1 makes a gated profile characteristic modifier reach the unit profile display, worked and pinned on the reported Vampire Counts bloodline case (issue AC 1-4 and AC 8); inc-2 derives the closed list of condition scope values from the format doc and the XSD, ensures none is silently dropped on a characteristic modifier, and covers or records each one (issue AC 5-7, plus the run-wide green gate AC 9). The codemap of 23 files is written into backlog.json."
        },
        "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nOpen the run state for docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/issue.md: map the issue against the codebase, decide the cut, and write both into docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json with the `init` subcommand of the backlog helper your shared brief names, with workflow \"agile-loop\" and the whole codemap in the payload's `codemap` field.\nThis run works at most 8 increments, so a cut that needs more than that is a cut that is too fine.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json - decompose <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json - decompose <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `summary`\n"
      },
      {
        "label": "replan:inc-1",
        "at": "2026-08-12T14:38:37.368Z",
        "return": {
          "questions": [],
          "summary": "Increment 1 is landed on the issue branch and closed as done: it needed no production change, because the reported bloodline gate uses a scope naming the unit's own selectionEntry id and that path already works end to end, so it delivered the regression pins instead. That leaves inc-2, now the whole of the issue and the only place the reporter's symptom can still live inside its boundary: the closed scope list from the format doc and the XSD, no scope silently dropped on a characteristic modifier, and every scope covered or recorded as uncoverable. I sharpened inc-2 with two things increment 1 produced - the issue file must also record what increment 1 established, and the suite doc src/components/editor/CLAUDE.md overstates the guard-assert convention - and rewrote the codemap around scope resolution, dropping the files increment 1 proved untouched."
        },
        "prompt": "Issue directory: docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display\nIncrement inc-1 — Gated profile characteristic modifiers reach the unit profile display — has been worked. The review accepted it.\nLand it first: check out `claude/profilwert-modifikatoren-bloodlines-zlw2p9`, run `git fetch origin claude/profilwert-modifikatoren-bloodlines-zlw2p9--inc-1`, merge that branch and push `claude/profilwert-modifikatoren-bloodlines-zlw2p9`.\nWhat this increment turned up is what you re-cut against, and it is in the run state. Read it with the backlog helper your shared brief names:\n  - `steps docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json inc-1` — every step this increment recorded, whole.\n  - `index docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json` — the rest of the cut, with no step content in it.\n  - `codemap docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json` — the map as it stands.\nThen close that increment with `close` and re-cut what is still open with `init`, the whole codemap in its `codemap` field. 1 of at most 8 increments are spent.\nAnnounce this step before you begin it and record it before you return, the way your shared brief describes, with the backlog helper it names:\n  - `start docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json - replan:inc-1 <the prompt file>` — the prompt you were given, verbatim and whole, in a file outside the repository.\n  - `record docs/issues/81-profile-characteristic-modifiers-ignored-in-unit-display/backlog.json - replan:inc-1 <the return file> <the prompt file>` — your return, in a file outside the repository, with that same prompt file. Commit it with your work and push the commit.\nThe fields your return carries, and your page says what each one holds:\n  - `questions`\n  - `summary`\n"
      }
    ],
    "attempts": []
  }
}
